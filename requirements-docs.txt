# Documentation-site tooling (MkDocs Material → GitHub Pages, #288 / ADR 0008-adjacent).
# Build-only — installed by the `docs` GitHub Actions workflow, NOT by the app /
# backend (kept off the backend pip-audit + runtime surface, like the mutation tooling).
# mkdocs-material pulls mkdocs + pymdown-extensions transitively.
mkdocs-material==9.7.7
# Click-to-zoom on screenshots (MIT).
mkdocs-glightbox==0.5.2
# Old-URL redirects after the 2026-09 site reorganisation (MIT). Pinned BELOW
# 1.2.3 deliberately: 1.2.3 (2026-03) added a hard dependency on `properdocs`,
# a MkDocs fork that installs beside mkdocs and prints upgrade adverts during
# every build — an unreviewed second site generator on the docs supply chain
# (CONTRIBUTING rule 39). 1.2.2 depends on mkdocs alone. Re-evaluate before bumping.
mkdocs-redirects==1.2.2
# Versioned docs — deploys each version to the gh-pages branch (BSD-3-Clause).
mike==2.2.0
# Interactive API explorer (Swagger UI over the published OpenAPI spec).
mkdocs-swagger-ui-tag==0.8.1
