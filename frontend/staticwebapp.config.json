{
  "$schema": "https://json.schemastorage.azurewebsites.net/schemas/2021-08-01/staticwebapp.config.json",
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/assets/*", "*.{css,js,png,svg,ico,woff,woff2}"]
  },
  "routes": [
    {
      "route": "/api/*",
      "comment": "Proxied to the linked Container Apps backend (configured in the SWA resource as a linked backend). Same-origin from the browser, so no CORS needed on this path."
    }
  ],
  "responseOverrides": {
    "404": { "rewrite": "/index.html" }
  }
}
