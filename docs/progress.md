# DataQ v1 — Progress tracker

> Mirrors [context/DataQ_platform_roadmap.md](../context/DataQ_platform_roadmap.md) (the 100-task roadmap) with execution status.
> **Updated at the end of every PR** — the PR template has a checkbox to enforce.
> Source of truth for "what's done vs. what's left." CLAUDE.md §13 carries only the headline.

## Status legend

| Symbol | Meaning |
|---|---|
| ✅ | Done — PR merged to `main` |
| 🟡 | In progress — open PR or partially shipped |
| ⬜ | Not started |
| 🔵 | Deferred / scope-changed (with note) |

---

## Snapshot

| | |
|---|---|
| **Active since** | 2026-05-24 |
| **Current week** | **v1 DONE — `v1.0.0` tagged 2026-07-04; Week 8 of 8 closed (29/29 + exit gate MET); epic #177 + the W8 milestone CLOSED with the tag; retro at [retro-v1.md](retro-v1.md).** Final go-live items cleared 2026-07-03/04: **pre-tag QA** — the qa-verifier data-level workout went NO-GO on one blocker (NUL-byte input → unhandled 500, #567) → fixed same-day in [#570](https://github.com/TheurgicDuke771/DataQ/pull/570) (ApiModel body guard + URL middleware + MCP guard + the #371 handler fix) → re-run **GO** (21 NUL injection points 422, zero 500s); prod redeployed at `2fa05333` and re-probed 422 live; **live prod workout as a real non-admin demo user (Olivia): 15/15** — suite-scoped visibility, unshared-404-no-leak, edit-tier-can't-delete, `/admin` 403, live run `succeeded` — plus **webhook live-auth hostility 7/7 structured 401s** (both halves of #569 discharged live, issue closed); CodeQL alert-24 autofix merged (#572; its CI fail was the SchedulesPanel flake → #573). **Ops/renewals timers: consciously SKIPPED (decision of record 2026-07-04)** — the Sept-2026 credential cluster (Snowflake PAT ~09-26, Databricks PATs ×3 ~09-30, ADLS SAS 2027-06-28) is demo-harness-scoped only; expiry self-signals through DataQ's own alerting (#419 always-alerts operationally-failed runs), recovery is re-mint + KV update, and the G-i teardown guardrail covers the end state — no reminder infrastructure. **Checklist progress 2026-07-03 (afternoon):** ✅ #553 closed ([#562](https://github.com/TheurgicDuke771/DataQ/pull/562) — bare `pip-audit` green, zero ignores) · ✅ rule-4a mutation-spike pass (mutmut on `dashboard_service` **436/436 killed**; Stryker on the dashboard frontend slice 82.35% — survivors triaged in [#563](https://github.com/TheurgicDuke771/DataQ/issues/563); spike-config retarget [#564](https://github.com/TheurgicDuke771/DataQ/pull/564)) · ✅ prod deploy + live-smoke re-green (SHA-pinned `8dee4f4a` images; Flows A/B/C all `succeeded` as `dataq-admin` — Snowflake 3 pass + the seeded-breach check failing by design, UC 4/4, flat-file 2/2 on the recreated "Logistics Tracking" suite, the original having died with the #540 repro; non-interactive bearer mint enabled by pre-authorizing the Azure CLI on the API scope, [#565](https://github.com/TheurgicDuke771/DataQ/pull/565)) · ✅ both decisions recorded — **ADR 0026 deferred to post-v1** (Theme 3; PATs-first → service-principals-later shape confirmed, HTTP Basic auth rejected — decision record in [the ADR](adr/0026-auth-api-keys-and-principal-seam.md)) and **Databricks Free-Edition** (demo/eval OK; paid workspace before any commercial use — gap G-h) **plus the new pre-marketplace harness-teardown guardrail** (gap G-i: strip Flows A/B/C, the 5 harness connections, and demo users from any marketplace/customer-facing artifact; also in `deploy/README.md`). Exit gate **MET**: the ≥80% coverage gates are enforced in CI — backend `--cov-fail-under=80` ([#557](https://github.com/TheurgicDuke771/DataQ/pull/557); **98.3% lines / 1273 tests** on merged main, after closing the four sub-80% modules: alerting email 55→99% · slack 74→100% · flatfile live seams 74→100% · core/auth 77→97%) and frontend all-src `lines: 80` with CI running `pnpm test:coverage` ([#558](https://github.com/TheurgicDuke771/DataQ/pull/558); coverage now counts ALL of `src/` — 7 previously-invisible files tested; **87.8% lines / 334 tests** on merged main). The W8 batch also closed the register: CORS middleware activation tests ([#556](https://github.com/TheurgicDuke771/DataQ/pull/556), closes #385), the catalog↔GX contract test — fixture + frontend drift-guard + 28 backend cases against pinned GX, verified red on planted defects ([#559](https://github.com/TheurgicDuke771/DataQ/pull/559), closes #205), and the dashboard KPI enrichment — Avg. Duration card + real period-over-period deltas, clock-skew-guarded, verified live ([#560](https://github.com/TheurgicDuke771/DataQ/pull/560), closes #352). Every PR ran the /code-review agent battery pre-merge (4 confirmed defects caught: a non-hermetic `.env.app` test, a mutation-vacuous nav assertion, a session-aborting fixture parse, the inert antd flex/xxl gap). **Week 7 — Deployment, hardening & docs — COMPLETE (41/41, closed 2026-07-03; epic #176 closed, `WEEK7_TODO.md` retired)**. Weeks 1–6 complete. Cloud deploy DONE (ADR 0024/0025 — live on Azure Container Apps); the frontend then **cut over SWA → Container App** (sole public surface, api→internal ingress, MSAL→generic OIDC — ADR 0028 §5, #509–#511). Prod backend image **`:v10`** (#438), frontend **`:v2`**. **Post-deploy hardening:** Celery beat idempotent-lock fix (#407, closes #405) + Key Vault `AZURE_CLIENT_ID` fix (#408, closes #406) + URL-encode DB password (#421, closes #395) merged. Orchestration polling (ADF + Airflow), scheduled-suite dispatch, gap recovery, and periodic purge all live. **Out-of-roadmap landed since deploy:** Slack + email alert publishers (#413), column-aware failing-sample redaction (#417, extends #365), always-alert operationally-failed runs (#419), alerting upsert race fix (#420, closes #384), per-run check outcome in the runs table (#425), mypy gate on `backend/tests` (#418), and — pulling **ADR 0012 post-v1 Theme A forward** — **freshness & volume monitor-kinds end-to-end** (run engine #426 + authoring path & check-editor UI #437). Plus three post-v1 design docs: admin/access/UI notes (#422), DQ-intelligence (#430), compliance posture (#436). **Landed since (this session):** FastMCP server (8 tools at `/mcp`, Azure-AD validated, #460) · consistency hardening — trigger-dedup (#456) + stuck-run reaper (#458) · hardening/docs — prod-docs gate (#464), Swagger completeness + error-shape audit (#465), deployment guide + env-var reference (#468) · visual-fidelity pass (#459) · W1–6 deferred + not-started triages (#463/#467). **Landed 2026-07-01/02 (W7 close-out batch — the in-repo tails are done):** OTel request/task spans to App Insights (#525, A3; opencensus→OTel logs follow-up #524) · vault lazy-import test coverage (#523, A4) · #17 MCP polish (#522) · Playwright E2E expansion — lifecycle panels #526 + detail surfaces #527 (25 specs, admin allowlisted in CI) · opt-in live-smoke lane (#531 — `e2e-live/` project + `e2e_smoke.py` bearer mode + runbook checklist) · user-docs enrichment (#528 — notifications/scheduling/best-practices/feature-matrix; #532 filed) · MCP tool-expansion candidate tiers (#530 + issue #529, post-v1 Theme 13). **LIVE SMOKE RUN 2026-07-02 (the #531 lane, signed in as a real demo user):** browser lane 3/3 · `e2e_smoke.py` bearer-mode vs prod · **Flows A/B/C all verified green** (Snowflake ×3 / UC / flat-file) · **#525 spans confirmed in App Insights** (request + task traces, `dataq.request_id` join) · **MCP 4-query protocol smoke passed** vs live `/mcp` · **#492 CLOSED** — Action Group + metric alert on the harness factory wired; deliberate `pl_dataq_smoke_fail` failure visible in DataQ **4m14s** after fire (Common-Alert-Schema webhook → immediate-poll ingest, #534). Smoke fallout fixed same-day: UC run-path dialect regression (#535→#537), traceback-locals credential leak (#536→#538, **rotate the Databricks PAT**), suite-delete FK cascade (#540→#542); deploy-workflow frontend flake filed (#539). Prod rolled ×3 (SHA-pinned; final image carries all fixes). **W7 externals closed 2026-07-02:** team onboarding discharged (six demo Entra users cross-shared at every ADR-0027 tier on the deployed app — no separate session needed, solo-dev) and KV purge-protection **decided: left off** (destroy/re-apply flexibility for the demo-scoped vault; recorded in `deploy/README.md`). **Final close 2026-07-03:** MCP client-config E2E passed — all 8 tools end-to-end via a real VS Code `.vscode/mcp.json` against live `/mcp/` ([#550](https://github.com/TheurgicDuke771/DataQ/pull/550), + the new [mcp-setup.md](mcp-setup.md) client-setup page). |
| **Roadmap tasks done** | **187 ✅ / 189 (~99%)** _(the remaining 2 are the 🔵 scope-changed W2 browse/list pickers, deferred post-v1 — [#466](https://github.com/TheurgicDuke771/DataQ/issues/466))_ |
| **Out-of-roadmap PRs landed** | governance, tooling lock, Entire CLI, Dependabot triage (10 bumps + pyarrow direct-dep fix #202/#201), PR-3 cleanup + ADRs 0005/0006/0007/0010/0011/0012/0013/0014/0016/**0017** (Python 3.11→3.13 + Snowflake 3→4 CVE refresh, [#129](https://github.com/TheurgicDuke771/DataQ/issues/129)) + the Week-3 testing-discipline upgrade (adversarial harness + mutation spikes, CONTRIBUTING rule 4a) + **post-deploy hardening (#407 Celery beat fix + #408 KV credential fix + #421 DB-password URL-encode)** + **alerting expansion (#413 Slack+email publishers, #419 always-alert failed runs, #420 upsert race)** + **column-aware redaction (#417)** + **per-run check outcome in runs table (#425)** + **mypy gate on tests (#418)** + **freshness/volume monitor-kinds end-to-end (#426/#437 — ADR 0012 post-v1 Theme A pulled forward)** + post-v1 design docs (#422/#430/#436) |
| **Week-7 exit gate** | Production-ready v1 deployed to Azure, CI/CD live, team onboarded. — **MET**: app live on Azure Container Apps behind the sole-public frontend (ADR 0028 §5; api internal ingress); Deploy workflow validated end-to-end with retry/verify + preflight hardening (#544/#546); six demo Entra users onboarded across every ADR-0027 access tier; live smoke Flows A/B/C green + the #492 ADF alert fire→ingest in 4m14s (2026-07-02). Weeks 1–6 exit gates also met — see [CLAUDE.md §13](../CLAUDE.md); the deferred Week-1 live warehouse/file smoke was discharged 2026-07-02 (Flows A/B/C via the #531 lane). |
| **Week-8 exit gate** | ≥80% coverage gate enforced in CI across backend, API, frontend. — **MET** (2026-07-03): backend `--cov-fail-under=80` in `pyproject.toml` gates every pytest run incl. CI (#557; 98.3% at flip — the API layer rides the same gate, TestClient suites over `backend/app/api`); frontend `coverage.thresholds.lines: 80` over ALL of `src/` with CI's `Frontend — tests` job running `pnpm test:coverage` (#558; 87.8%). |
| **Next milestone** | **Post-v1 cycle planning.** v1.0.0 shipped 2026-07-04 (epic [#177](https://github.com/TheurgicDuke771/DataQ/issues/177) + the Week-8 milestone closed; every go-live checklist row ✅ — see Current week). The next cycle's input is the post-v1 backlog (53 issues, themed, [post-v1-roadmap.md](../context/post-v1-roadmap.md)); recommended opening sequence per that doc: Theme-1 `schema_drift` + `anomaly` monitor kinds (rides ADR 0012) → scale-aware execution (gap G-b, Theme 7) → incident/lineage design doc (G-d). (Superseded W8-cycle detail: **Week 8 — Unit testing** (exit gate: ≥80% coverage gate enforced in CI across backend, API, frontend). Week 7 closed **COMPLETE 41/41** (2026-07-03; milestone + epic #176 closed). Cloud deploy + post-deploy hardening DONE (`:v10`); **FastMCP server DONE** (#460); **hardening/docs DONE** — prod-docs gate (#464), Swagger completeness + error-shape audit (#465), deployment guide + env-var reference (#468); **consistency hardening DONE** (#456/#458); **visual-fidelity DONE** (#459); **cloud-neutral cutover DONE** (frontend SWA→Container App, api→internal ingress, MSAL→generic OIDC — ADR 0028 §5, #509–#511; follow-up #512 arm64 cross-build fixed #518). **W7 close-out batch DONE (2026-07-01/02):** OTel spans (#525) + vault-test A4 (#523) + #17 polish (#522) + E2E expansion (#526/#527) + live-smoke lane (#531) + docs enrichment (#528) + MCP-expansion candidates (#530/#529) — **no in-repo Week-7 work remains**. **Live smoke DONE 2026-07-02** (Flows A/B/C green, spans verified, MCP protocol smoke passed, #492 fired→ingested in 4m14s and closed; fix batch #534/#537/#538/#542 deployed). **Externals closed 2026-07-02:** live run paths (#531 lane) + Azure Monitor→ADF ×5 (#492) + team onboarding (demo-user matrix) + KV purge-protection decided left-off (`deploy/README.md`). **Final close 2026-07-03:** MCP client-config E2E (all 8 tools via a real VS Code config, #550).) |
| **Open issues** | **54 open** (verified against GitHub 2026-07-04): **1 × Week 8** — [#177](https://github.com/TheurgicDuke771/DataQ/issues/177) (epic; closes with the go-live checklist) — and **53 × Backlog (post-v1 / testing)**, mapped by theme in [post-v1-roadmap.md](../context/post-v1-roadmap.md) ([#563](https://github.com/TheurgicDuke771/DataQ/issues/563) mutation-spike survivors triage moved Week 8 → Backlog same day, before #562 closed #553). Four W8 issues closed 2026-07-03 (#385 by #556 · #205 by #559 · #352 by #560 · #128 with the close-out, the coverage gates being its remaining half). **The qa-verifier go-live workout (2026-07-03/04)** found + same-day-closed the one v1.0.0 blocker — a NUL byte in any free-text field passed Pydantic but 500'd at the Postgres INSERT — via [#570](https://github.com/TheurgicDuke771/DataQ/pull/570) (closes [#567](https://github.com/TheurgicDuke771/DataQ/issues/567) + the older [#371](https://github.com/TheurgicDuke771/DataQ/issues/371)); its non-blocking footguns are filed to Backlog as [#568](https://github.com/TheurgicDuke771/DataQ/issues/568) (threshold ordering unvalidated), [#571](https://github.com/TheurgicDuke771/DataQ/issues/571) (`checks_total` cosmetic 0), and [#573](https://github.com/TheurgicDuke771/DataQ/issues/573) (flaky `SchedulesPanel` test); its deferred-probes follow-up [#569](https://github.com/TheurgicDuke771/DataQ/issues/569) was opened and closed same session (both halves discharged live). **Zero open Week-1–7 issues** — every W7 item closed with the milestone (2026-07-03), incl. the W1 carryovers #168 (superseded by the ADR 0028 OIDC cutover), #169 (real KV, discharged by the deploy), #170 (prod-docs gate, #464), #92 (in-app webhook URL, #493/#496), #17 (MCP polish, #522), #288 (docs site, #528), #308/#309 (consistency hardening #456/#458), #348 (results filters), #382/#396/#397/#539 (deploy-workflow hardening, #544/#546), and the live-smoke fallout #535/#536/#540 (#537/#538/#542). Roadmap *features* are tracked as checkboxes per week (below), not GitHub issues — the register at the end of this file records the W8 closures. |
| **Open PRs** | **none open** — latest merged: [#572](https://github.com/TheurgicDuke771/DataQ/pull/572) (CodeQL alert-24 autofix) and [#570](https://github.com/TheurgicDuke771/DataQ/pull/570) (qa-verifier go-live blocker — NUL-byte input now 422s, closes #567/#371), both 2026-07-04; the W8 batch #556–#560 (squash-merged 2026-07-03 after agent review + green CI) and #562/#564/#565 remain the prior close-out set. |
| **Design gates** | all Week-3 design gates resolved (ADR 0005 severity weights, 0012 monitor-kind seam, 0016 severity derivation — migration shipped). Pending: two-connection model for `comparison` checks (ADR 0015, post-v1, non-blocking) |

---

## Out-of-roadmap work (foundation that's not in the 100-task list)

These were preconditions for executing the roadmap. Listed for completeness.

| Bundle | PRs | Status |
|---|---|---|
| **PR 0 governance** | #1–#24, #44, #55 — `.gitignore`, CLAUDE.md, CODEOWNERS, ADRs 0001–0004 + 0009, PR / issue templates, architecture diagram, Claude Code agents/skills/hooks/MCP, Entire CLI hooks + allowlist | ✅ |
| **PR 1 tooling lock** | #37 — conda env, Black, Ruff, mypy, pre-commit, CI workflow, Dependabot, `scripts/setup.sh`, frontend tooling | ✅ |
| **PR 2 hygiene follow-ups** | #44 (mermaid + tsconfig + ADR 0009 + CLAUDE.md status), #47 (precommit reorder), #48 (mermaid fix #2), #49 (precommit mypy deps), #52 (markdown linebreak preservation) | ✅ |
| **Architecture Q&A ADRs** | ADR 0010 (provider-agnostic infra seams — cloud portability) + ADR 0011 (extensibility seams — more datasources, `ResultPublisher`, dbt-as-orchestration-provider) + ADR 0013/0014 (marketplace/BYOL anti-lock-in; reconciliation `comparison` kind) + ADR 0016 (severity derivation). Record the now-vs-post-v1 timing per seam | ✅ |

### Live test/demo-data environment (v1-supporting harness — lives _outside_ this repo, ADR [0021](adr/0021-demo-test-data-environment-strategy.md))

The mechanism that finally discharged the standing **"live warehouse/file run — deferred Week-1 smoke"** caveat (2026-07-02). The harness (Terraform infra, retail mock-data generators, UC medallion SQL transforms — bronze→silver→gold + an AI feedback-sentiment stage, run from the Airflow DAG, no notebook) is **not git-tracked** — only this pointer + ADR 0021 live in the repo; its living doc is the harness `README.md` (the old `HARNESS_TODO.md` working tracker is retired). **Status: LIVE END-TO-END** — Terraform applied, mock-data generators seeded on a Container Apps cron, all four ADF/Airflow load modalities (incl. true Event-Grid push) running; 5 live prod connections + demo suites configured; the harness Databricks workspace **migrated to Free Edition 2026-07-02** (trial credits exhausted — same DAGs/suites, new workspace, verified green post-migration). The flow rows below track **DataQ's verified check-runs** against the live harness — **all three verified green 2026-07-02** via the #531 live-smoke lane.

| Workstream item | Home | Status |
|---|---|---|
| Terraform infra bootstrap (Azure + Snowflake + UC + ADF/Airflow on Container Apps) | external / local, not git-tracked | ✅ applied (45 resources; `plan` → no changes) |
| Retail data model + mock-data generators (Product/Inventory/Location/Sales/Returns/Logistics/Guest + discount/promo/tax/FX/price) | external harness | ✅ built + seeded (tier-S, 24 datasets, 18 seeded DQ issues; cron Jobs live) |
| Reference **Flow A** — file lands in ADLS → ADF/Airflow loads to Snowflake/UC → DataQ checks the warehouse table | external harness (exercises `OrchestrationProvider`, ADR 0004) | ✅ verified live 2026-07-02 — Snowflake suite runs green ×3 (scheduled + triggered + manual) on harness loads |
| Reference **Flow B** — file lands in UC RAW → medallion RAW→Silver→Gold → DataQ checks UC tables (implemented as SQL-from-DAG on the serverless warehouse, not a notebook; + an incremental `ai_analyze_sentiment` gold stage since 2026-07-02) | external harness (not git-tracked) | ✅ verified live 2026-07-02 — UC gold suite runs succeeded (after the #535 dialect fix, #537), incl. the trigger-bound feedback-sentiment suite |
| Reference **Flow C** — file lands in ADLS, stays flat → DataQ checks it in place (`FlatFileCheckRunner` + batch resolution) | external harness | ✅ verified live 2026-07-02 — batch-resolved flat-file suite run succeeded in place |
| **QA/QE agent** — test the DataQ framework module-by-module + E2E (new Claude Code subagent, like `migration-safety`) | repo (`.claude/agents/`) — Week 8 / ongoing | ✅ `qa-verifier` agent shipped ([#548](https://github.com/TheurgicDuke771/DataQ/pull/548)) — runs the full local gate battery + audits test quality + exercises the running app with data-level scenarios; ongoing use through Week 8 |
| **HashiCorp Vault** secret-management spike — second `SecretStore` impl, validates the ADR 0010/0013 seam (Key Vault = one impl) | post-v1 / backlog | ⬜ |

_All post-v1 / deferred work (design themes + the full `Backlog (post-v1 / testing)` issue list) is consolidated in **[context/post-v1-roadmap.md](../context/post-v1-roadmap.md)** — see the single [Post-v1 / deferred work](#post-v1--deferred-work) pointer at the end of this file._

---

## Week 1 — Foundation, auth & project scaffold

**Exit gate:** Azure AD auth working, GX + FastAPI connected to Snowflake DEV, Swagger live.

### Setup (4 tasks — 4/4 ✅)
- [x] ✅ Monorepo scaffold — FastAPI backend + React/Vite frontend + Celery + Redis — #37 + [PR 2a](https://github.com/TheurgicDuke771/DataQ/pull/39) + [PR 3c](https://github.com/TheurgicDuke771/DataQ/pull/63)
- [x] ✅ Docker Compose dev environment (all services wired up) — [PR 2a](https://github.com/TheurgicDuke771/DataQ/pull/39)
- [x] ✅ PostgreSQL DB + Alembic migrations baseline — [PR 2c](https://github.com/TheurgicDuke771/DataQ/pull/41)
- [x] ✅ Core data model: connections, suites, checks, runs, results, shares — [PR 2c](https://github.com/TheurgicDuke771/DataQ/pull/41) _(also added `users`, `pipeline_runs`, `trigger_bindings` per ADR 0004)_

### Auth (3 tasks — 3/3 ✅)
- [x] ✅ Azure AD SSO — MSAL token validation in FastAPI + login flow in React — [PR 3a](https://github.com/TheurgicDuke771/DataQ/pull/53) + [PR 3c](https://github.com/TheurgicDuke771/DataQ/pull/63) _(the MSAL frontend was later retired for a generic `oidc-client-ts` client, still validated against Azure AD — ADR 0028 §5, #509)_
- [x] ✅ Session timeout + silent token refresh handling — `acquireTokenSilent` wired in PR 3c; **superseded by the ADR 0028 cutover**: the generic `oidc-client-ts` client handles silent renew + expiry, validated live against real Entra SSO on the deployed app — [#168](https://github.com/TheurgicDuke771/DataQ/issues/168) closed
- [x] ✅ Key Vault integration for all credential storage — abstraction landed in [PR 3b](https://github.com/TheurgicDuke771/DataQ/pull/56); **real Azure vault provisioned with the W7 deploy** (ADR 0024 KV+UAMI; #406/#408 credential path; prod reads KV live) — [#169](https://github.com/TheurgicDuke771/DataQ/issues/169) closed

### GX baseline & API conventions (3 tasks — 3/3 ✅)
- [x] ✅ GX Core wired to Snowflake datasource (DEV env) — `SnowflakeCheckRunner` (GX 1.17) behind the `CheckRunner` seam — [PR 4b](https://github.com/TheurgicDuke771/DataQ/pull/76) + [PR 4c-ii](https://github.com/TheurgicDuke771/DataQ/pull/79) _(live-run smoke discharged 2026-07-02 — Flow A green ×3 via the #531 lane)_
- [x] ✅ GX result serialisation to DB (runs, results) — `run_service.execute_run` + NaN→null sanitizer — [PR 4c-i](https://github.com/TheurgicDuke771/DataQ/pull/78) + [PR 4c-ii](https://github.com/TheurgicDuke771/DataQ/pull/79)
- [x] ✅ Configure FastAPI with Pydantic models, route tags, and Swagger (`/docs`) + ReDoc (`/redoc`) — FastAPI + Pydantic wired from PR 2; Swagger completeness audited + guardrail-tested in [#465](https://github.com/TheurgicDuke771/DataQ/pull/465); the "disable in prod" gate shipped as [#464](https://github.com/TheurgicDuke771/DataQ/pull/464) (closes [#170](https://github.com/TheurgicDuke771/DataQ/issues/170))

**Week 1 total: 10 / 10 ✅ — COMPLETE** _(exit gate met at the time; the three deferred tails — silent-token-refresh, real Key Vault, prod-docs gate — all closed in Week 7: ADR 0028 OIDC cutover / ADR 0024 deploy / #464)_

---

## Week 2 — Connection manager — all datasource types (backend)

**Exit gate:** All connection types configurable and testable via API; credentials stored in Key Vault.

> **Auth-boundary discipline (per [ADR 0010](adr/0010-provider-agnostic-infrastructure-seams.md)):** new connection-CRUD endpoints (and every protected route from here on) depend on a generic internal "current user" dependency that returns DataQ's own `User` — they must NOT read MSAL token claims directly in route/service code. Cheap now; expensive to retrofit once dozens of endpoints exist. No new abstraction layer required, just the boundary.

### Snowflake & ADF (4 tasks — 4/4)
- [x] ✅ API: CRUD for Snowflake connections (DEV / QA / UAT), connection test endpoint — [PR 5](https://github.com/TheurgicDuke771/DataQ/pull/85) _(also introduced the `ConnectionAdapter` seam + registry per [ADR 0011](adr/0011-extensibility-seams-for-deferred-integrations.md), and `SecretStore.set` write-through — so PRs 6-8 are pure adapter additions)_
- [x] ✅ API: CRUD for ADF connections (subscription ID + service principal) — PR 6 _(`ADFConnectionAdapter` in the new `orchestration/` package — NOT `datasources/`, per CLAUDE.md §4; `test()` does SP token + factory GET via httpx. Enforces `(type, env)` uniqueness for orchestrator rows via a **partial unique index** `WHERE type IN ('adf','airflow')` per [#72](https://github.com/TheurgicDuke771/DataQ/issues/72) / ADR 0004 — datasources excluded, so Snowflake stays many-per-env. CRUD/API reused unchanged: pure adapter + registry + migration addition.)_
- [x] ✅ Connection re-auth endpoint — refresh expired Key Vault token — `POST /connections/{id}/reauth` (`svc.reauth_connection`): rotates the credential through `SecretStore.set` **and** verifies it via the same adapter probe as `/test`, in one step (the gap PATCH+`/test` leave open). Rotation persists before the probe, so a bad new credential surfaces as 502 `connection_test_failed`; a store-write failure is 502 `connection_secret_write_failed` with the old credential untouched. 6 TestClient tests (rotate+verify ok, failed-verify-but-rotation-persists, write-fail 502, 404, secret-required 422). Type-agnostic — applies to all six connection types
- [x] ✅ Review `connections.secret_ref` nullability — decide based on Airflow basic-poll / unauthenticated S3 cases ([PR #41 nit](https://github.com/TheurgicDuke771/DataQ/pull/41)) — **decision: keep nullable.** It's NULL for the transient flush→secret-write window (create), for credential-less auth (managed-identity/IAM-role, W7, ADR 0010/0011), and for unauthenticated sources. v1 types are all secret-bearing, but presence is enforced in the **service layer** (`test_connection` → 502 without a credential), not the schema — so W7 credential-less modes need no later migration. Recorded as a comment on the `secret_ref` column in `db/models.py`

### ADF webhook receiver (Azure Monitor → DQ platform) (5 tasks — 5/5 ✅)
- [x] ✅ `POST /api/v1/orchestration/events/adf` — receive Azure Monitor payload, validate shared secret (constant-time, ADR 0006), return 200 — PR 7 _(unified `OrchestrationProvider` seam landed: `orchestration/base.py` Protocol + `RunUpdate` DTO + provider registry; ADF reference impl per ADR 0004 — service code dispatches by provider, never branches on ADF)_
- [x] ✅ Parse webhook payload — `AdfProvider.parse_event` extracts `factoryName`/`pipelineName`/`runId`/`status`/`firedDateTime` → `RunUpdate`, ADF→`PIPELINE_RUN_STATUSES` normalisation — PR 7 _(exact Common-Alert-Schema field mapping validated at Week-7 deploy smoke)_
- [x] ✅ Follow-up ADF REST API call on webhook receipt — fetch run details — PR 8 _(`AdfProvider.fetch_run_detail` GETs the ARM `pipelineruns/{runId}` for authoritative status/timing/message; `orchestration_service.ingest_event` enriches **best-effort** before upsert — any failure (no creds, transport) falls back to the parsed event so a valid webhook is never dropped)_
- [x] ✅ Upsert pipeline run status into `pipeline_runs`; correlate with suite run — idempotent upsert (PR 7) + **trigger-on-success skeleton** (PR 8): a `succeeded` run matching enabled `trigger_bindings` creates queued `Run` rows (`triggered_by="<provider>:<pipeline>:<run_id>"`, idempotent on replay); failures never trigger (ADR 0004). **Every deferred piece has since landed:** `run_suite` dispatch ungated in Week 5 ([#215](https://github.com/TheurgicDuke771/DataQ/issues/215), PR-C0a); `trigger_bindings` CRUD ([#172](https://github.com/TheurgicDuke771/DataQ/pull/190)) + its Suite Triggers UI ([#216](https://github.com/TheurgicDuke771/DataQ/issues/216), PR #247); 10-min polling beat ([#171](https://github.com/TheurgicDuke771/DataQ/pull/189)); race-proof dedup index ([#456](https://github.com/TheurgicDuke771/DataQ/pull/456)). Verified live end-to-end 2026-07-02 (trigger-on-success fired the bound suites on real harness pipeline runs).
- [x] ✅ Shared secret config in Key Vault → `ADF_WEBHOOK_SECRET` env var — `settings.adf_webhook_secret_name` resolved via `SecretStore` (→ `KV_SECRET_ADF_WEBHOOK_SECRET` in dev) — PR 7

### Airflow orchestration (added per ADR 0004; not in original roadmap) (3 tasks — 3/3)
- [x] ✅ `POST /api/v1/orchestration/events/airflow` — `AirflowProvider.parse_event` (signed-callback JSON → `RunUpdate`, state→`PIPELINE_RUN_STATUSES`) + **HMAC-SHA256** auth over the raw body (`X-DataQ-Signature`, [ADR 0007](adr/0007-airflow-callback-model.md)); reuses `ingest_event`. Generalised `_resolve_connection` to match on a provider-declared `resource_config_key` (`base_url` for Airflow, `factory_name` for ADF — no provider branching); enrichment is skipped for Airflow (callback is authoritative). `airflow-webhook-secret` config added
- [x] ✅ Airflow `on_success_callback` / `on_failure_callback` helper snippet for users' DAGs — stdlib-only, fail-safe `dataq_airflow_callback.py` under `integrations/airflow/` (+ setup README): HMAC-signs the raw body → `X-DataQ-Signature` and POSTs `dag_id`/`run_id`/`state`/`base_url` to the receiver. Round-trip tests assert the snippet's signature **and** payload are accepted by `_authenticate_airflow` + `AirflowProvider.parse_event` (producer↔consumer agreement) — completes the Airflow event-receiver loop (callback producer ↔ HMAC receiver)
- [x] ✅ Airflow connection type — webserver URL + token/basic auth (token v1 default) — `AirflowConnectionAdapter` (REST `dagRuns`-probe `test`), one-line registry add; orchestrator `(type,env)` guard already covers it ([ADR 0007](adr/0007-airflow-callback-model.md))

### Flat file — ADLS Gen2 & S3 (4 tasks — 3/4 ✅, 1 🔵 scope-changed)
- [x] ✅ API: CRUD for ADLS Gen2 connections — `AdlsConnectionAdapter` (account URL + container; SAS auth, container-properties `test` via `azure-storage-blob`), one-line registry add. **SAS only in v1**; `managed_identity` config rejected with a "deferred to Week 7" message (needs an ambient Azure identity + the `secret_ref`-nullability change) — [PR #100](https://github.com/TheurgicDuke771/DataQ/pull/100)
- [x] ✅ API: CRUD for S3 connections — `S3ConnectionAdapter` (bucket + region; access-key auth, `head_bucket` `test` via `boto3`), one-line registry add. **Access-key only in v1**; `iam_role` config rejected with a "deferred to Week 7" message (per the same `secret_ref`-nullability decision)
- [ ] 🔵 GX `pandas_abs` / `pandas_s3` datasource wiring — connect, list containers, list files — **scope-changed (superseded by the explicit-target design):** connect + flat-file read shipped via `AdlsConnectionAdapter`/`S3ConnectionAdapter` + `FlatFileCheckRunner` (pandas reads CSV/Parquet), and `flatfile.list_files` already enumerates blobs to drive batch resolution. The interactive **container browser** (list-containers UI) is deferred post-v1 — [#466](https://github.com/TheurgicDuke771/DataQ/issues/466) (Theme 8)
- [x] ✅ File asset config model: container, batching regex, file format — delivered as `run_target.BatchSpec` (W3/W5): `pattern` (batching regex) + `strategy` + `prefix` + `file_format`. **CSV/Parquet** in v1; **JSON** flat files deferred post-v1 ([#466](https://github.com/TheurgicDuke771/DataQ/issues/466), Theme 8)

### Unity Catalog / Databricks (3 tasks — 2/3 ✅, 1 🔵 scope-changed)
- [x] ✅ API: CRUD for Databricks connection — `UnityCatalogConnectionAdapter` (workspace URL + warehouse id + PAT; `SELECT 1` `test` via `databricks-sql-connector`), one-line registry add. PAT-only (secret-bearing, no `secret_ref`-nullability deferral). The `UnityCatalogCheckRunner` (DQX swap-in, CLAUDE.md §5) is the Week-3 run path, not built here
- [ ] 🔵 GX Spark / JDBC datasource wiring for Unity Catalog — connect, list catalogs / schemas / tables — **scope-changed (superseded by the explicit-target design):** connect + run path shipped via `UnityCatalogConnectionAdapter` + `UnityCatalogCheckRunner`, which reads the table into a GX **DataFrame** asset using the SQL connector (not Spark/JDBC — CLAUDE.md §5). The interactive **3-level catalog → schema → table picker** (needs the Databricks catalog-listing API) is deferred post-v1 — [#466](https://github.com/TheurgicDuke771/DataQ/issues/466) (Theme 8)
- [x] ✅ UC auth test endpoint — validate PAT + SQL Warehouse reachability — the `SELECT 1` probe in `UnityCatalogConnectionAdapter.test`, surfaced through the generic `POST /connections/{id}/test`

**Week 2 total: 17 / 19 ✅ + 2 🔵 — COMPLETE** _(the 2 🔵 scope-changed rows are the GX-datasource "browse/list" tasks: connection + check-run paths shipped via the adapters + flat-file/UC runners; only the interactive container-browser / 3-level catalog picker is deferred post-v1, [#466](https://github.com/TheurgicDuke771/DataQ/issues/466) — the file-asset config model shipped as `run_target.BatchSpec`. All six connection types have adapters (Snowflake + ADF + Airflow + ADLS Gen2 + S3 + Unity Catalog); the ADF upsert+correlate row completed via the W5 dispatch ungate + #456 dedup, verified live 2026-07-02.)_

---

## Week 3 — Suite & check API — all datasource types (backend)

**Exit gate:** Full check CRUD API across Snowflake, flat files and Unity Catalog; column profiler live.

### Suite & check backend (4 tasks — 4/4 ✅)
- [x] ✅ API: CRUD for suites and GX expectations (Snowflake path) — **suites** (PR-B1): `suite_service` + `/suites` CRUD (`connection_id` validated then immutable; delete cascades to checks). **checks** (PR-B2): `check_service` + nested `/suites/{id}/checks` CRUD surfacing `kind` + `warn/fail/critical_threshold` + GX `expectation_type`/`config`. v1 monitor-kind guard (only `expectation`; reserved kinds → 422, ADR 0012); checks scoped to their suite (cross-suite access → 404); thresholds are `Decimal` in (exact `Numeric` storage) / `float` out (clean JSON). 24 TestClient tests; all four modules 100%. Share-based access filtering deferred to the suite-sharing task; **DQ-dimension classification** deferred + tracked ([#124](https://github.com/TheurgicDuke771/DataQ/issues/124))
- [x] ✅ API: suite sharing — assign users with owner / editor / viewer roles — **sharing API + authz core** (PR-E1): `suite_authz.require_permission` (404-hides a suite with no access, 403s an insufficient level) + `share_service` + `/suites/{id}/shares` CRUD. Schema vocab `view`/`edit`/`admin` + implicit owner=`created_by`; **admin can delete + manage shares** (per decision); grant-to-owner/unknown → 422; manage needs `admin`, list needs `view`. **Enforcement** (PR-E2): `require_permission` applied across the suite endpoints (GET=view, PATCH=edit, DELETE=admin) + all check endpoints (reads=view, writes=edit); `list_suites` scoped to owned-or-shared. Lands the access control deferred in B1/B2. ~26 TestClient tests across the matrix acting as different users (viewer reads-not-writes, editor writes-not-deletes, admin deletes, outsider→404, list scoping); shares/suites/checks routes + share_service all 100%
- [x] ✅ API: suite export to JSON + import from JSON — `suite_io_service` + `GET /suites/{id}/export` (view) / `POST /suites/import` (any authed user, like create). Document is **connection-agnostic** — omits all DB identity (`id`/`connection_id`/`created_by`/timestamps), so it's a reusable template; import **re-binds** to a freshly chosen `connection_id` and owns the new suite as the importer. Round-trippable: thresholds are `Decimal` in/out (exact). Import is **atomic** — every check kind is validated before any row is written (bad doc → 422, nothing persisted); unknown `version` → 422; missing connection → 422. Checks emitted in stable creation order (diffable). Reuses `check_service.validate_kind` (no dup). 7 TestClient tests (no-identity-leak, view-gated, owned-by-importer, export→import→export round-trip, unknown-connection/version/kind-atomic); `suite_io_service` + `suites.py` 100%
- [x] ✅ API: check dry-run endpoint — validate against live data, return preview result — `POST /suites/{id}/checks/dryrun` (`dryrun_service`): runs **one ad-hoc check** against the suite's connection synchronously and returns a preview (severity `status` + `metric_value` + sanitized `observed/expected`) **without persisting** any Run/Result. Reuses the severity derivation (ADR 0005/0016) + JSON sanitiser. `require_permission` **edit** (authoring); table passed in the body (checks don't carry a target table yet). v1 → 422: non-`expectation` kind, non-Snowflake connection (runner dispatch generalises Week 5); execution failure → 502 (adapter exception never echoed). No `sample_failures` in the preview (PII; follow-up). 7 TestClient tests (mocked runner); `dryrun_service` + `checks.py` 100%

### Severity threshold tiers (warn / fail / critical) (4 tasks — 4/4 ✅)
> **Day 1 design decision: severity weights — ✅ settled in [ADR 0005](adr/0005-severity-tier-weights.md) (warn 0.5 / fail 1.0 / critical 2.0; health = 100×(1−Σpenalty/(N×2.0))).**
- [x] ✅ Add `warn_threshold`, `fail_threshold`, `critical_threshold` fields to check model — nullable `Numeric` columns on `Check` (NULL → plain pass/fail) — migration `9c59b6a44f33`
- [x] ✅ Alembic migration — threshold columns + `status` enum (`pass`, `warn`, `fail`, `critical`) **+ the monitor-kind / metric columns below (one migration)** — the one-shot Week-3 schema seam `9c59b6a44f33` (tested up→down→up; `results.status` retargeted from `passed/failed/skipped` with a data-update-before-CHECK-swap; `run_service` now writes `pass`/`fail` binary-fallback per ADR 0005). `alembic check` clean (no model drift)
- [x] ✅ Post-processing in GX result handler — derive `warn` / `fail` / `critical` from observed value (PR-C) — `services/severity.py` (`extract_metric` + `derive_status`), wired into `run_service._build_result`. Thresholds band the GX **unexpected-%** as `metric_value` (higher=worse, ordered, unset-tier skipped); thresholds-as-policy override GX `success`; binary fallback when no thresholds / no metric. **Settled in [ADR 0016](adr/0016-severity-derivation-semantics.md)** (incl. A→B reversibility: raw `observed_value` retained → switch is additive `direction` column + backfill, never destructive). `duration_ms` stays NULL (per-check timing not separable from GX's suite-level `validate()`). 16 unit + 1 integration test; both modules 100%
- [x] ✅ Update check CRUD + run result response schemas with threshold fields + status values — check-CRUD thresholds (PR-B2) + result response now carries `status` (`pass`/`warn`/`fail`/`critical`) + `metric_value` (probe `CheckResultResponse`, PR-C)

### Monitor abstraction & metric storage — do-now seams (3 tasks — 3/3 ✅)
> **Day 1 design decision: `check.kind` discriminator + numeric metric storage — ✅ settled in [ADR 0012](adr/0012-monitor-kind-seam.md); rides the same migration.** Keeps v1.x auto-monitors (freshness / volume / schema-drift / anomaly — post-v1 Theme A) from forcing a check/result schema rewrite. v1 implements `expectation` only.
- [x] ✅ Add `kind` discriminator to check model (`'expectation'` default; `freshness`/`volume`/`schema_drift`/`anomaly` reserved) — `checks.kind` `NOT NULL DEFAULT 'expectation'` + CHECK over the 6 reserved kinds (incl. `comparison`, ADR 0014) — migration `9c59b6a44f33`
- [x] ✅ Generalise run path to dispatch by `check.kind` (`expectation` → GX `CheckRunner`; others raise `NotImplementedError`) — PR-D: `run_service._specs_for_checks` dispatches by kind; a non-`expectation` check raises `NotImplementedError` → the run goes terminal `failed` **without invoking the adapter** (never silently run as a GX expectation). Composes with the Week-5 `connection.type` runner selection (`kind` picks the monitor, type picks the adapter). `run_service` 100%; test fixtures now set `kind` to mirror DB rows
- [x] ✅ Add `metric_value` (NUMERIC) + `duration_ms` (INT) to results — SQL-aggregatable metric for Week-6 trends + v1.1 anomaly; per-check runtime for cost surface — nullable columns on `Result` — migration `9c59b6a44f33`

### Column profiler (3 tasks — 3/3 ✅)
- [x] ✅ Column profiler endpoint (Snowflake) — nulls, distinct count, min / max, top values — `profile_service` + `POST /suites/{id}/profile` (require_permission **edit**, suite-scoped so the connection is access-gated). Reads-only, persists nothing: one aggregate query (row count + null/distinct/min/max per column) + one top-N-values query per column, then `assemble_profile`. **SQL-injection-safe** — queries are built with the **SQLAlchemy Core expression language** (`select`/`table`/`column`, dialect-quoted) so there's no raw-string SQL sink (no S608/B608/CodeQL `py/sql-injection`); identifiers are additionally allowlist-validated (`validate_identifier`, strict `^[A-Za-z_][A-Za-z0-9_$]*$`) as defence-in-depth + a clean early 422, and `top_n` is `int()`-coerced. v1 → 422: non-Snowflake type (dispatch generalises Week 5), bad identifier, no schema; execution failure → 502 (adapter exception never echoed). min/max/top-values NaN-sanitised. 28 tests (22 pure: identifier allowlist incl. injection strings, compiled-SQL builders, assembly, div-by-zero, NaN; 6 endpoint via fake conn: stats, injection-422, unsupported-type-422, 502, edit-gated, no-schema-422); `suites.py` 100%, `profile_service` 90% (live `_open_connection` is the deferred warehouse seam)
- [x] ✅ Column profiler endpoint (ADLS / S3) — same stats via Pandas on sampled file — extended `profile_service` into a type dispatcher (`profile_connection`): SQL types → in-warehouse aggregation (unchanged), flat-file types (`adls_gen2`/`s3`) → download a **sample** of the file into Pandas and compute the same stats (`profile_dataframe`). Same `POST /suites/{id}/profile` endpoint, now polymorphic: SQL targets pass `table`/`schema`, flat-file targets pass `path` (+ optional `file_format`, else inferred from extension); response carries whichever identity applies. CSV + Parquet. **pandas "load less data" levers** (per the user's scale.html prompt): column projection (CSV `usecols`, Parquet `columns=` via pyarrow schema) so profiling 3 of N columns doesn't read all N, + row sampling (`_SAMPLE_ROWS`), + **zero-copy Arrow-backed dtypes** on the Parquet read (`dtype_backend="pyarrow"` — parquet is already Arrow, kept zero-copy; the stat helpers + `_to_native` are Arrow-scalar/`pd.NA`-safe). Stats are vectorised pandas (no Python row loops — enhancingperf N/A; CoW is a no-op for a read-only profiler + a process-global flag, so not toggled; pyarrow CSV engine rejected — it's incompatible with `nrows`/callable-`usecols`). NaN/Timestamp→JSON-safe. v1 → 422: unsupported type (UC still pending), missing target, unknown format, missing column; read failure → 502 (exception never echoed). Reuses `ColumnProfile`/`ProfileResult`. 24 new tests (pure `profile_dataframe`/`infer_file_format`/`_to_native`/projection-parse + endpoint via fake `_read_dataframe`); `suites.py` 100%, `profile_service` 85% (only the live `_open_connection`/`_download_bytes` network seams uncovered — the live seam; smoke discharged 2026-07-02). _Deferred: streaming/range reads (whole object still downloaded before sampling) + out-of-core (Dask) — future work if profiling cost bites._
- [x] ✅ Column profiler endpoint (Unity Catalog) — via Databricks SQL Warehouse — added `unity_catalog` to the profiler's **SQL** branch (reuses `profile_table` + the SQLAlchemy Core query builders + `assemble_profile` — no duplicate aggregation logic). `_open_connection` now dispatches the engine by type (`_engine_args`): Snowflake URL or a `databricks://token:…@host?http_path=…` URL via the installed `databricks` SQLAlchemy dialect. UC's **3-level namespace** (`catalog.schema.table`) is handled by building the table with a `quoted_name(f"{catalog}.{schema}", quote=False)` so the dialect emits three dotted parts rather than quoting the dotted string as one — verified by compiling the real builders against the Databricks dialect. Injection-safe: every part (catalog/schema/table/column) is allowlist-validated (no raw-string SQL, no CodeQL py/sql-injection surface). Endpoint gained `catalog` (request + response); the dispatcher requires it for UC (`422 profile_target_invalid` if absent). 12 new tests (catalog-aware builders compiled to SQL, `_engine_args` URL dispatch, UC endpoint stats via the same fake conn, missing-catalog 422, bad-catalog-identifier 422); `suites.py` 100%, `profile_service` 86% (only the live `_open_connection`/`_download_bytes` network seams uncovered — the live seam; smoke discharged 2026-07-02). **Completes the column profiler across all 4 datasources.**

### Flat file check specifics (2 tasks — 2/2 ✅)
- [x] ✅ Check types for flat files: schema validation, row count, null checks, ~~freshness by filename date~~ — `FlatFileCheckRunner` (`datasources/flatfile.py`): downloads the file (S3/ADLS) into a pandas DataFrame and runs the suite's GX expectations against GX's **pandas DataFrame asset**, so all GX expectation-kind checks (schema/row-count/null/value checks) run against flat files. Extracted the shared GX machinery — snake_case→GX-class translation, GX-result→`SuiteOutcome` mapping, and the suite/validation-definition `run_expectations` flow — into `datasources/gx_runner.py` (reused by the Snowflake runner and the future UC runner; no duplication). Flat-file IO (`download_bytes` + full-file `read_dataframe`) moved to `flatfile.py` behind primitives (raw config + resolved secret, not the ORM — matches `build_snowflake_runner`), and the column profiler now shares it. Because GX runs **in-process on the DataFrame**, the run path is fully unit-tested with a canned frame (real GX execution, not mocked) — only the network download is the deferred-smoke seam. 16 new tests; `gx_runner` 100%, `flatfile` 76% (only `download_bytes` uncovered). _Connection-type → runner dispatch (Snowflake vs flat-file) is the Week-5 `run_suite` wiring; **freshness-by-filename** is the reserved `freshness` monitor kind (deferred, ADR 0012), not a GX expectation._
- [x] ✅ Batch resolution — resolve batching regex to matched files, pick latest or specific batch — `resolve_batch` (`datasources/flatfile.py`): a batch `pattern` is a regex whose **first capture group is the batch key**; `latest` selects the greatest key (lexicographic — ISO dates sort chronologically), falling back to the storage modified-time when the pattern has no group; `specific` selects the file whose key equals a given `batch`. `BatchNotFoundError` on no match / unknown batch. The filter+select logic is pure and fully unit-tested (8 tests); object listing (`list_files` — S3 `list_objects_v2` paginated / ADLS `list_blobs`) is the live seam, and `resolve_batch_file` composes list+resolve. _Wiring a check's target to a resolved batch path is the Week-5 `run_suite` dispatch._

### Unity Catalog check specifics (2 tasks — 2/2 ✅)
- [x] ✅ UC table check path — UC table → GX DataFrame datasource → run suite — `UnityCatalogCheckRunner` (`datasources/unity_catalog.py`): reads the target table from the Databricks SQL Warehouse into a pandas DataFrame (`pd.read_sql_table` via the databricks SQLAlchemy dialect — reflection quotes identifiers, so no hand-built SQL) and runs the suite's GX expectations against GX's **pandas DataFrame asset** — the "GX DataFrame datasource" shape (§5), the same shape Databricks Labs DQX consumes, so v1.1 swaps GX→DQX behind this one interface. Reuses the shared `gx_runner` (`run_expectations` + result mapping) exactly like the flat-file runner; the `catalog` is pinned in the connection URL (`build_databricks_url`, now shared with the column profiler — no duplication) so the 2-level `schema.table` resolves to `catalog.schema.table`. Because GX runs **in-process on the DataFrame**, the run path is unit-tested with a canned frame (real GX execution); only the reflect+read (`_read_table`) is the deferred-smoke seam. `build_unity_catalog_runner` mirrors `build_snowflake_runner` (raw config + resolved PAT, not the ORM). 14 tests; `unity_catalog` 91% (only `_read_table` uncovered). _Connection-type → runner dispatch is the Week-5 `run_suite` wiring._
- [x] ✅ Integration tests across all three datasource types — `backend/tests/integration/test_datasource_runs.py`: end-to-end suite runs through the **real** `run_service.execute_run` + **real Postgres** persistence, with **real GX execution** for the flat-file (`FlatFileCheckRunner`) and Unity Catalog (`UnityCatalogCheckRunner`) paths on an in-memory DataFrame (only the file download / table read is stubbed) — exercising the whole chain: `check.kind` dispatch → runner → GX validation → severity derivation → `Result` rows. Snowflake runs through a canned outcome (live warehouse is the deferred-smoke seam; its GX mapping is covered in `datasources/test_snowflake.py`). Asserts run `succeeded`, both Results persisted, severity derived (null-check → `fail`, row-count → `pass` with observed value). Plus a parametrized **adversarial-frame** run (numpy + pyarrow hostile columns via the shared battery) proving a column-agnostic monitor still runs + persists over a file whose data column is hostile. 7 tests.

**Week 3 total: 18 / 18 ✅ — COMPLETE**

---

## Week 4 — Connection manager UI + check editor UI (frontend)

**Exit gate:** Users can configure any connection type and author checks end-to-end in the UI.

### Frontend tooling coordinated bumps (added — not in original roadmap) (1 task — 1/1 ✅)
- [x] ✅ Vite 8 coordinated bump — `vite` ^6→^8.0.16 + `@vitejs/plugin-react` ^5→^6.0.2 + `vitest` ^3→^4.1.8 + `@vitest/coverage-v8` ^4.1.8 in lockstep — [PR #119](https://github.com/TheurgicDuke771/DataQ/pull/119) (Fixes [#65](https://github.com/TheurgicDuke771/DataQ/issues/65); supersedes Dependabot #111 + closed [#57](https://github.com/TheurgicDuke771/DataQ/pull/57)). plugin-react v6 peers `vite ^8`, vitest v4 peers `vite ^6||^7||^8` — done early to drain the frontend dep backlog; format/lint/typecheck/test/build all green

### Frontend polish from PR-3c review (added — not in original roadmap) (3 tasks — 3/3 ✅)
- [x] ✅ Wrap `MsalProvider` subtree in an antd `AntApp` / React error boundary so render-time failures don't fall back to plain text — `AntApp` already wrapped the tree; added a class `ErrorBoundary` (antd `Result` fallback + Reload) inside it covering everything after first paint — [PR #210](https://github.com/TheurgicDuke771/DataQ/pull/210)
- [x] ✅ Bundle code-splitting — `React.lazy` per route + Suspense + `manualChunks` vendor split (react / antd / msal / vendor) — initial `index` chunk ~1.1 MB → **6.7 kB**; pages load on navigation — [PR #210](https://github.com/TheurgicDuke771/DataQ/pull/210) _(the residual >500 KB chunk is antd itself, now isolated as one long-cache vendor)_
- [x] ✅ Tighten `Settings.model_config` `extra="ignore"` → `"forbid"` (the [PR #39 nit](https://github.com/TheurgicDuke771/DataQ/pull/39)) — unblocked by splitting the env files ([#209](https://github.com/TheurgicDuke771/DataQ/issues/209)): root `.env` now carries infra/compose-only keys (`POSTGRES_*`, `VITE_*`), app config moved to a new `.env.app` (the only file Settings reads + the api/worker/migrate `env_file`), `.env.app.example` template + `setup.sh`/`docker-compose.yml` repointed. `forbid` now catches typo'd/stale keys without the infra keys crashing startup

### Connection manager UI (6 tasks — 6/6 ✅)
- [x] ✅ Connection cards — Snowflake (3 envs), ADF, ADLS/S3, Databricks sections with status badges — grouped-by-type cards with env tag + credential badge + Test action — [PR #191](https://github.com/TheurgicDuke771/DataQ/pull/191)
- [x] ✅ Add connection drawer — type-specific form fields per connection type — spec-driven `ConnectionTypeFields` from `CONNECTION_FORM_SPECS` (all six types; Snowflake password/key-pair auth toggle) — [PR #196](https://github.com/TheurgicDuke771/DataQ/pull/196) (+ Snowflake key-pair backend [PR #193](https://github.com/TheurgicDuke771/DataQ/pull/193))
- [x] ✅ Connection health page — bulk test, live status, re-auth surface — "Test all" header action tests every connection concurrently; each card shows a live health badge (testing / healthy / unreachable) and surfaces an inline **Re-authenticate** link on failure — [PR #208](https://github.com/TheurgicDuke771/DataQ/pull/208) _(folded into the connections list rather than a duplicate route)_
- [x] ✅ Connection re-auth UI — surface expired tokens, inline refresh action — `ReauthModal` (single-secret rotate+verify) + edit drawer + delete via the card actions menu — [PR #198](https://github.com/TheurgicDuke771/DataQ/pull/198)
- [x] ✅ ADLS/S3 connection form — account URL, SAS toggle — covered by the spec-driven drawer ([PR #196](https://github.com/TheurgicDuke771/DataQ/pull/196)) _(container browser + managed-identity/IAM-role modes deferred with the backend, ADR 0010/0011)_
- [x] ✅ Databricks connection form — workspace URL, PAT, warehouse id — covered by the spec-driven drawer ([PR #196](https://github.com/TheurgicDuke771/DataQ/pull/196)) _(live SQL-Warehouse picker deferred; warehouse id is a text field)_

### Check editor UI (9 tasks — 9/9 ✅)
- [x] ✅ Suite list + detail two-panel layout, environment badge on each suite — selectable suites list ←→ detail (connection chip + env tag + checks) — [PR #200](https://github.com/TheurgicDuke771/DataQ/pull/200)
- [x] ✅ Form-based check editor — catalog-driven expectation picker + dynamic typed config fields + create/edit/delete — `expectationCatalog.ts` + `CheckDrawer` ([PR #203](https://github.com/TheurgicDuke771/DataQ/pull/203)). GX expectations are datasource-agnostic in v1, so one catalog serves all four types
- [x] ✅ Flat file check editor — file path + format selector — the datasource-shaped **run-target editor** on the suite drawer ([PR #246](https://github.com/TheurgicDuke771/DataQ/pull/246)) authors a flat-file target (`path` + optional CSV/Parquet `file_format`), so ADLS/S3 suites become runnable; the generic catalog editor ([PR #203](https://github.com/TheurgicDuke771/DataQ/pull/203)) already covers flat-file expectations. _(Container/regex **batch-resolution** inputs remain a post-v1 polish — needs the flat-file listing surface.)_
- [x] ✅ Unity Catalog check editor — catalog / schema / table inputs — the run-target editor ([PR #246](https://github.com/TheurgicDuke771/DataQ/pull/246)) authors a UC target (`catalog` + `table` + optional `schema`), making UC suites runnable; expectations covered by the generic catalog editor. _(A **cascading enumerated** 3-level picker is optional polish — blocked on a Databricks catalog-listing API.)_
- [x] ✅ **Monaco SQL editor for custom check** — a "Custom SQL" category authoring a GX `UnexpectedRowsExpectation` (ADR 0019): a lazy-loaded, locally-bundled Monaco editor (`SqlEditorField`, its own `monaco` Vite chunk so the eager bundle is untouched) + a client-side mirror of the backend read-only/single-statement guardrail (`customSql.ts`) for inline feedback. Gated to SQL datasources (Snowflake / Unity Catalog) in both the create page and the edit drawer via `expectationsByCategoryFor(connectionType)`. _(backend unblocked by ADR 0019 / [PR #258](https://github.com/TheurgicDuke771/DataQ/pull/258); Snowflake keyword autocomplete deferred — Monaco ships SQL syntax highlighting only)_
- [x] ✅ Column profiler panel — inline in check editor, loads on table / file selection — `ColumnProfilePanel` (shared by the create page `CheckNew` + the edit drawer `CheckDrawer`): a collapsed-by-default panel that profiles one column of the suite's run target (#215) via `POST /suites/{id}/profile` (no persistence) and renders null count/fraction, distinct count, min/max, and top values — so the author can ground a range / allowed-set / null threshold in real data before saving. The profiled column **pre-fills from the check's `column` config field** (render-phase prop-sync, like `DryRunPreview`) but stays editable to explore neighbouring columns; `table`/`schema`/`catalog` (SQL) or `path`/`file_format` (flat file) are pulled from the suite target. Disabled with a reason until the suite has a table/file target + a column is entered; backend limits (no credential / unreachable / unsupported type) surface as a clean error alert. 4 vitest (disabled-states / pre-filled-profile with request-shape assertion / error). _(PR-D2; `ColumnProfilePanel.tsx` + `api/suites.ts` `profileColumns`)_
- [x] ✅ **Check dry-run button** — preview pass/fail inline before saving — `DryRunPreview` component (shared by the create page `CheckNew` + the edit drawer `CheckDrawer`): runs the in-progress check against the suite's live target via `POST /suites/{id}/checks/dryrun` and renders the severity outcome (status tag + `metric_value` + observed/expected), **no persistence**. Reuses `buildCheckPayload` (so the preview runs exactly the saved check's config/thresholds) + the severity colour map; `table`/`schema` come from the suite's run target (#215, fetched via new `getSuite`). Disabled with a reason until an expectation is picked + the suite has a table target; backend limits (Snowflake-only, no-credential/unreachable) surface as a clean error alert (dev fail-softs to a 502 with fake creds). 4 vitest (disabled-states / success-outcome with request-shape assertion / error) + an e2e button-enabled assertion. _(PR-D1; `DryRunPreview.tsx` + `api/suites.ts` `getSuite`/`dryRunCheck`)_
- [x] ✅ **Check version history drawer — see previous config before overwriting** — a `check_versions` table snapshots a check's editable state (name/expectation/config/thresholds + author) on create, on every *real* update (no-op PATCHes skipped via `session.is_modified`), and per imported check; `GET /suites/{id}/checks/{cid}/versions` (view-gated, newest-first, author resolved server-side). The **History** button on `CheckDrawer` opens `CheckHistoryDrawer` — newest-first list, latest tagged **Current**, each row showing expectation label + pretty-printed config + `Warn ≥/Fail ≥/Critical ≥` thresholds + author/timestamp. View-only; **restore deferred to [#283](https://github.com/TheurgicDuke771/DataQ/issues/283)**. Backend migration verified up/down; 7 backend + 6 frontend tests. _(#280; backend [PR #281](https://github.com/TheurgicDuke771/DataQ/pull/281) + frontend [PR #282](https://github.com/TheurgicDuke771/DataQ/pull/282); ADR 0019 custom-SQL configs stay readable via pretty-print)_
- [x] ✅ Severity tier toggle in check editor — three-threshold UI — optional warn/fail/critical inputs banding the unexpected-% (ADR 0016), blank = binary — [PR #203](https://github.com/TheurgicDuke771/DataQ/pull/203)

### Access & admin UI (3 tasks — 3/3 ✅)
- [x] ✅ Suite sharing panel — add / remove users, assign roles inline — **Share** button on the suite detail opens `SharePanel` (drawer): lists collaborators by name (from the enriched `ShareRead`) and, for owner/admin, adds/changes/removes them. Add picker is a debounced `GET /users/search` type-ahead filtering out the owner + already-shared users; permission is the view/edit/admin ladder. Gated on **`SuiteRead.my_permission`** — a new field stamping the caller's effective level (owner/admin/edit/view), populated on create/get/update + batched across the list via `suite_authz.effective_permissions` (one shares `IN`-query, no N+1) — so a non-owner admin gets the manage controls and a viewer a read-only list. Backend enablers landed first: **PR-E2** (`GET /users/search` behind a FastAPI-free `user_service` — case-insensitive email/display_name substring, 2-char min, limit≤50, LIKE-escaped — + `ShareRead` enriched with `email`/`display_name` via a joined read-only `Share.user`). 6 component tests (list-by-name, empty, search→grant with owner/existing filtered, change, revoke, read-only non-admin) + backend `my_permission` + `/users/search` tests. _(PR-E2 + PR-E3)_
- [x] ✅ Admin page — list all suites, all users, access overview — **workspace-admin control centre** shipped (backend + frontend, #289, closes the long-standing backend block). New **workspace-admin** authz primitive (config `WORKSPACE_ADMIN_EMAILS` allowlist, case-insensitive on `user.email` — generic identity axis, no Azure/Entra claim read; **no migration**) distinct from the per-suite `view < edit < admin < owner` ladder. Backend: `admin_service` (`list_all_suites` / `list_all_users` / `list_all_access`, deliberately **unscoped** — bypasses the owned-or-shared scoping `list_suites` applies) + `GET /admin/{suites,users,access}` gated by `require_workspace_admin` (real **403**); `/me` now exposes `is_workspace_admin`. Frontend: `/admin` page (Suites · Users · Access tabs) + `Forbidden` 403 page + admin-only nav item, gated on `/me`'s server-driven flag via a shared `MeProvider`/`useMe` (one `/me` fetch, reused by Profile; resets on sign-out). 23 tests (backend: scoping-bypass, counts, case-insensitive, non-admin 403, `/me` flag; frontend: forbidden gate, all three tabs, load error, MeProvider sign-out reset). _(This pulled the Week-7 prototype-adoption tasks "workspace-admin list-all endpoint" + "Admin control centre page" forward.)_
- [x] ✅ Suite export / import UI (download JSON, upload JSON) — **Export** button on the suite detail header downloads `<suite>.json` from `GET /suites/{id}/export` via a transient-anchor blob (`utils/download.ts` — `downloadJson` + `toFilenameStem`); **Import** button on the suites header opens `ImportSuiteDrawer` — drag/drop a `.json`, client-side parse + shape-check (fail-fast on non-suite/malformed JSON), pick a target connection, `POST /suites/import`. The parsed document is handed back **untouched** so thresholds round-trip exactly (string-or-number Decimal encoding never coerced). 11 tests (download lib: stem slug + blob/anchor/revoke; drawer: valid→import-with-faithful-document, non-suite/malformed-JSON rejection, connection-gated Import). _(PR-E1; backend ready from Week 3)_

### GX-Cloud-style UI redesign (added — not in original roadmap) (4 tasks — 4/4 ✅)
> Model the UI on [GX Cloud](https://greatexpectations.io/gx-cloud/): dedicated **full-page create flows** that **classify** what you're creating, keeping the lighter drawers for *edit*. Plan: `~/.claude/plans/fancy-moseying-raccoon.md`. Decided: in-app results page (Grafana deferred to an optional post-v1 ops add-on, never the per-suite authz path — ADR 0018 pending); full-page create + drawer edit; connections/checks redesign now (backends ready), results after the Week-5 run-enablement backend.
- [x] ✅ **Connections — dedicated, classified add page** — `/connections/new` splits the type picker into **Data sources** vs **Orchestration** sections (CLAUDE.md §4), then the spec-driven form; the list page is grouped the same way. `CONNECTION_KIND` is the single source for the split. Edit/re-auth/delete stay on the drawer. _(PR-A1; `ConnectionNew.tsx` + sectioned `Connections.tsx`)_
- [x] ✅ **Connections — `ConnectionDrawer` trimmed to edit-only** — create now lives on the page; the drawer drops the type/env Selects + secret + create branch, shows type/env read-only, and the list owns a simpler `editing: Connection | null` state. _(PR-A2)_
- [x] ✅ **Checks — suite selection as a route + categorized expectation picker** — `/suites/:suiteId` makes the selected suite deep-linkable and survives the check-editor round-trip; the catalog gains a `category` (Column values / Table shape) and the picker groups by it (antd optgroups). Reserved Freshness/Volume/Schema-drift categories (ADR 0012) land with the dedicated page. _(PR-B1)_
- [x] ✅ **Checks — dedicated `/suites/:suiteId/checks/new` page** — category → expectation → config/thresholds, with reserved monitor-kind categories (Freshness/Volume/Schema-drift, ADR 0012) shown disabled. Shared form logic extracted to `checkForm.ts` (conversions) + `checkFormFields.tsx` (field components), reused by the page and the now edit-only `CheckDrawer`. The natural home for the [#215](https://github.com/TheurgicDuke771/DataQ/issues/215) target table/path field. _(PR-B2; `CheckNew.tsx`)_

**Week 4 total: 26 / 26 ✅ — complete** _(connection manager UI complete (6/6); check editor core shipped + **dry-run preview** (PR-D1) + **column profiler panel** (PR-D2) + the datasource-shaped **run-target editor** (PR #246 — closes the flat-file + Unity Catalog target inputs, making suites runnable); **check version-history drawer** (#280; backend #281 + frontend #282 — view-only, restore deferred to #283); **suite export/import UI** (PR-E1); **suite sharing panel** (PR-E2 backend + PR-E3 UI, gated on `SuiteRead.my_permission`); **GX-Cloud redesign complete (4/4)** — classified connection + check create pages, edit-only drawers, suite-route/categorized picker (PR-A1/A2/B1/B2); PR-3c polish 3/3 (Settings `extra="forbid"` shipped via the `.env`/`.env.app` split, #209); **Monaco custom-SQL editor** (ADR 0019 — custom-SQL as a GX `UnexpectedRowsExpectation`; backend #258 + frontend #259); **admin control centre** (#289 — workspace-admin authz + `/admin/{suites,users,access}` + the `/admin` page & 403, pulling the Week-7 prototype-adoption admin tasks forward))_

---

## Week 5 — Execution engine + scheduling

**Exit gate:** Async runs with live progress across all datasource types; scheduling operational.

### Async execution backend (10 tasks — 10/10 ✅)
- [x] ✅ Celery + Redis background task runner for GX scan execution — `run_suite` task + `run_service` — landed early via [PR 4a](https://github.com/TheurgicDuke771/DataQ/pull/74) + [PR 4c-i](https://github.com/TheurgicDuke771/DataQ/pull/78) + [PR 4c-ii](https://github.com/TheurgicDuke771/DataQ/pull/79)
- [x] ✅ Generalise `run_suite` worker dispatch — select the `CheckRunner` by `connection.type` via the runner registry (`build_check_runner`), replacing the Snowflake-hardcoded wiring in `worker/tasks.py`; the seam that makes the flat-file / UC run paths route correctly and post-v1 RDBMS adapters a drop-in — [PR #146-fix](https://github.com/TheurgicDuke771/DataQ/issues/146) _(added per [ADR 0011](adr/0011-extensibility-seams-for-deferred-integrations.md); profiler dispatch collapsed onto a parallel registry in the same effort, [#147](https://github.com/TheurgicDuke771/DataQ/issues/147))_
- [x] ✅ **Check target table/path** — modeled as a **per-suite** datasource-shaped `Suite.target` (JSONB, shaped like the column-profiler request: `table`/`schema`/`catalog`/`path`/`file_format`), since `execute_run` runs all of a suite's checks against one batch (the GX data-asset-per-suite shape). `services/run_target.resolve_target` resolves it per connection type to the runner's `(table, schema, catalog)` triple (flat-file path rides the table-shaped `CheckRunner`'s `table` slot); `validate_target` is the write-time 422 guard on suite create/update. Migration `b1f2c3d4e5a6` (additive nullable column, tested up/down on Postgres). **Dispatch ungated**: `_trigger_suites` now hands each created run to `run_suite.delay` (lazy import — worker↔service cycle), broker-fail marks the run `failed`; `run_suite` resolves the target from the suite (targetless → clean `failed`), so the worker no longer takes a `table` arg; stale `until_week3` wording removed. 13 resolver units + worker/orchestration/suite-API target tests. _(Re-tracked from Week 3; [#215](https://github.com/TheurgicDuke771/DataQ/issues/215); PR-C0a)_
- [x] ✅ **Manual run trigger + run/result read API** — `POST /suites/{id}/run` queues a `Run` and dispatches it via the shared `run_dispatch.dispatch_run` helper (**edit**-gated — the capability ladder grants "trigger runs" at edit); the suite target (#215) is resolved **up front** so a targetless/misconfigured suite fails fast with 422 instead of a queued→failed run, and a broker outage marks the run `failed` + returns 503 (same contract as the probe). New `api/v1/runs.py` read surface for the results page: `GET /runs?suite_id=&status=&limit=` and `GET /runs/{id}` (run + joined `Result`s) are **suite-scoped** — the list filters to accessible suites and the detail gates on `require_permission(view)`, so per-suite sharing + existence-hiding hold (exactly why direct-Postgres/Grafana is rejected as the primary surface); `GET /pipeline_runs?provider=&status=` is the orchestration monitoring feed (auth-only, not suite-scoped). Read helpers live in `run_service.list_runs/get_run/list_results` + `orchestration_service.list_pipeline_runs`. 14 DB-backed API tests (trigger happy/targetless-422/edit-403/no-access-404/broker-503; list scoping+filters+limit; detail+results / 404s; pipeline filters + auth). _(Run-enablement gap; PR-C0b; gates the Week-6 Results page PR-C1)_
- [x] ✅ Run progress API — poll endpoint returning per-check live status — `GET /runs/{id}/progress` (suite-scoped): run lifecycle status + per-check resolution (status or null=pending) + a status histogram, the compact shape the live-progress UI polls. DB-driven (worker writes status + result rows); GX's atomic batch means per-check rows land at completion, so this reports lifecycle + final resolution (documented). **[PR #301](https://github.com/TheurgicDuke771/DataQ/pull/301)** (A1)
- [x] ✅ Cancel run endpoint — gracefully terminate in-progress Celery task — `POST /runs/{id}/cancel` (edit-gated, suite-scoped, 409 if terminal): marks `cancelled` + best-effort `revoke` of the captured `runs.celery_task_id` (migration `d5e6f7a8b9c0`). Cooperative — no SIGKILL mid-GX: worker skips a cancelled run at pickup + `execute_run` won't overwrite a mid-run cancel. Deterministic for queued, best-effort for in-flight. Folded **#227** (one dispatch-failure shape in `run_dispatch`, aligning probe's `finished_at`). **[PR #302](https://github.com/TheurgicDuke771/DataQ/pull/302)** (A2)
- [x] ✅ Run history retention policy — configurable purge of results older than N days — **PII-minimisation sweep** (not a row delete: ADR 0012 wants `metric_value` trends to outlive samples). Daily Celery-beat `purge_sample_failures` → `run_service.purge_expired_sample_failures` scrubs `sample_failures` (the only potentially-PII result column) to SQL NULL + stamps `sample_failures_purged_at` on results older than the configurable `sample_failures_retention_days` (default 30). Honoured the pre-built seam (the column + setting + the three "retention sweep" comments existed with no writer). Targets only real sample *objects* via `jsonb_typeof` (the JSONB column stores Python `None` as JSON `'null'`, so `IS NOT NULL` over-matches every passing/errored row); naturally idempotent (scrubbed → SQL NULL → excluded); `retention_days <= 0` disables. 6 DB-backed sweep tests + 1 task-wiring unit test. **(A8)**
- [x] ✅ Flat file run path — resolve batch, load via Pandas, execute GX suite — batch-aware target (regex `pattern` + `latest`/`specific` strategy + `prefix`) resolved live via `run_target.materialize_path` → `flatfile.resolve_batch_file` (Week-3 seam); literal `{path}` unchanged. The pandas load + GX execution already lived in `FlatFileCheckRunner` (Week 3). **[PR #298](https://github.com/TheurgicDuke771/DataQ/pull/298)** (A4)
- [x] ✅ UC run path — submit job to Databricks SQL Warehouse, execute GX suite — `UnityCatalogCheckRunner` reads the target table via the Databricks SQL Warehouse into a pandas DataFrame + runs GX in-process (DQX swap-in shape, §5), wired through the runner registry (#146) + per-suite target (#215); shipped Week 3, worker catalog-threading coverage closed in A5. Live SQL-Warehouse read was the deferred smoke — discharged 2026-07-02 (Flow B green via the #531 lane). **[PR #299](https://github.com/TheurgicDuke771/DataQ/pull/299)** (A5)
- [x] ✅ **Scheduling backend** — cron-driven suite runs make "scheduling operational" (the backend half of the Execution-UI scheduled-runs table). New `schedules` table (migration `f7a8b9c0d1e2`, additive; cascade-deleted with the suite) holding a 5-field `cron` + IANA `timezone` (DST-aware) + precomputed `next_run_at` (indexed) per suite. `services/cron.py` wraps `croniter` + `zoneinfo` (validate cron/tz → 422; **no-backfill** `next_fire` = next fire strictly after the base, so a downtime gap fires once, not per missed slot). `schedule_service` CRUD mirrors `trigger_binding_service` (suite-permission-gated: `edit` to mutate, `view` to read; `next_run_at` recomputed on cadence-change / re-enable). `api/v1/schedules.py` (POST/GET/GET{id}/PATCH/DELETE). New Celery-beat tick `dispatch_due_schedules` (60s) scans `enabled, next_run_at<=now` with `FOR UPDATE SKIP LOCKED` (no double-fire across overlapping ticks), resolves the suite target up front (invalid → skip + advance, never a queued→failed run), queues a `Run` (`triggered_by=schedule:<id>`) + dispatches via the shared `run_dispatch` (broker-fail → `mark_dispatch_failed` #227). `cron.validate_cron` probes a fire so an impossible-calendar cron (Feb 30 — `croniter.is_valid` passes it) is a clean 422, not a 500-on-create / crashed dispatcher tick; the dispatcher self-disables such a row instead of hot-looping. 37 tests (cron units incl. DST spring-forward/fall-back + no-backfill + impossible-date; DB-backed CRUD/authz/422 API; dispatcher due/future/disabled/invalid-target/invalid-cron/no-backfill/broker-fail). **(A7)**

> **Operational result statuses ([#122](https://github.com/TheurgicDuke771/DataQ/issues/122) — DONE, closed):** both halves shipped. **`error`** ([PR #297](https://github.com/TheurgicDuke771/DataQ/pull/297)) — a check GX can't *evaluate* (per-expectation exception, e.g. a missing column) persists as `error` (no severity, no `metric_value`) instead of being conflated with a data `fail`, never failing its siblings or the run. **`skip`** ([PR #298](https://github.com/TheurgicDuke771/DataQ/pull/298), with the flat-file batch run path) — a genuinely-absent batch maps `BatchNotFoundError` → `run_service.skip_run` (every check `skip`, run still succeeds), not a failure. Both values were already in the `RESULT_STATUSES` CHECK (Week-3 schema seam); these emit them. Unit + real-GX + Postgres-integration tests for each.

### ADF + Airflow hybrid monitoring (webhook + polling fallback) (4 tasks — 4/4 ✅)
- [x] ✅ Celery beat task — poll ADF REST API every 10 min for **succeeded** runs (skip pipelines updated by webhook recently) — provider-agnostic `poll_orchestration_runs` beat task through the `OrchestrationProvider` seam — [PR #171](https://github.com/TheurgicDuke771/DataQ/pull/189)
- [x] ✅ Celery beat task — poll Airflow REST API `dagRuns` every 10 min — same provider-agnostic beat task (ADF + Airflow share it) — [PR #171](https://github.com/TheurgicDuke771/DataQ/pull/189) _(added per ADR 0004; not in roadmap)_
- [x] ✅ Gap recovery logic — on startup + every 30 min, fetch last hour of run statuses — provider-agnostic `recover_orchestration_gaps` beat task reuses the `_poll_orchestration_runs` pipeline with a 1-hour `lookback` (vs the 10-min poll's 15-min window); idempotent upsert + `skip_updated_since` make overlap harmless. New 30-min beat entry + a one-off on worker boot via the `worker_ready` signal (enqueued by name, broker-failure swallowed). **[PR #307](https://github.com/TheurgicDuke771/DataQ/pull/307)** (B2)
- [x] ✅ `GET /api/v1/orchestration/pipelines` — latest status per pipeline/DAG, provider-agnostic — one row per distinct `(provider, pipeline_or_dag_id, env)` carrying its most-recent run, via Postgres `DISTINCT ON` (no provider branching); auth-only gated (monitoring, not suite-scoped); `provider`/`env`/`limit` filters. Recency = `COALESCE(started_at, created_at)` so a fresh failure event with no start time isn't masked by an older fully-timed run (`/code-review` catch). **[PR #305](https://github.com/TheurgicDuke771/DataQ/pull/305)** (B1); follow-up [#306](https://github.com/TheurgicDuke771/DataQ/issues/306) (validate provider/env → 422 on both read endpoints)

> **`trigger_bindings` CRUD** (provider-agnostic suite-run triggers — `provider`/`pipeline_or_dag_id`/`env` → `suite_id`) landed early via [PR #172](https://github.com/TheurgicDuke771/DataQ/pull/190), unblocking the trigger-on-success path skeletoned in Week 2 PR 8.

### Execution UI (4 tasks — 4/4 ✅)
> The roadmap's 5th Execution-UI task — **recent-runs audit table** — was **moved to Week 6** (counted there). Why: the [DataQ Design System](#week-6--results-dashboard--alerting) prototype makes the **Enhanced Monitoring Dashboard** (`/dashboard`, the [Week-6 task](#week-6--results-dashboard--alerting)) the page-home for the recent-runs widget, and its row drill-down target is the Week-6 routed run-detail page (`/results/:runId`, [#268](https://github.com/TheurgicDuke771/DataQ/issues/268) — promotes `Results.tsx`'s in-page `openRun` state to a route). Building a standalone table on `/results` now and rebuilding it on `/dashboard` in W6 is throwaway work; the dashboard also gates on the chart-library decision first. Deferred to build it **once, in its final home** — see Week 6.
- [x] ✅ Run now panel — suite picker, env / datasource, notification target — `RunNowPanel` (`components/runs/`): a **cross-suite** run launcher (modal off a **Run now** button in the Results header), distinct from the suite-detail **Run** button (#245) which runs one suite in context. A searchable suite `Select` filtered to the `owner`/`admin`/`edit` ladder the backend `POST /suites/{id}/run` enforces (view-only suites never appear); a read-only `Descriptions` readout of the picked suite's **env** (tag) + **datasource** (connection name + type) + **target** (one-line `catalog.schema.table` / flat-file `path`, mirroring `suiteTarget.ts`). A targetless suite blocks **Run** with an inline reason rather than a backend 422. On trigger it hands off to the shared `LiveRunProgress` drawer (modal closes; run watched + cancellable). **Notification target ships as a clearly-labelled disabled placeholder** ("coming in Week 6") — no notification backend exists yet (Teams/`ResultPublisher` alert routing is Week 6), so it doesn't jump the gun. Self-contained (owns its data fetch + progress drawer) so the Week-6 UI rework can drop it onto the dedicated Execution page unchanged. 3 component tests (no-runnable empty state, env/datasource/target render + trigger, targetless-suite block).
- [x] ✅ Live run progress UI — check-by-check status with spinner + cancel button — `LiveRunProgress` drawer (`components/runs/`): opens on the run id returned by the manual **Run** button (#245) instead of bouncing to `/results`, and polls `GET /runs/{id}/progress` (#301) on a self-scheduling timeout (not `setInterval` — no overlapping fetches) until the run is terminal (`succeeded`/`failed`/`cancelled`), then stops. Shows the run-status tag + a completed/total `Progress` bar + a per-check list (a `Spin`+"pending" while a check's `status` is null, its severity `Tag` once resolved). An editor (`canRun`) gets a **Cancel** button (`POST /runs/{id}/cancel` #302) on a non-terminal run; cancel reflects the terminal state immediately + the next poll confirms it. On terminal, a "View full results →" link to `/results`. Body keyed by run id so opening a new run restarts polling; `pollMs` is injectable for deterministic tests. 7 component tests (pending/resolved render, poll-until-terminal-then-stop, cancel happy path, cancel hidden when terminal / non-editor, fetch-error alert). **(A3)**
- [x] ✅ Scheduled runs table — create, pause, delete cron schedules — `SchedulesPanel` (`components/suites/`): suite-detail card over the A7 schedules CRUD (#314) — an antd table (cron · timezone · next run · last run · status · delete) + an add form (cron `Input` + IANA-timezone `Select`, default UTC). **Pause/resume** is the `enabled` toggle (`PATCH /schedules/{id}`); **delete** is `DELETE /schedules/{id}`; **create** is `POST /schedules`. Edit-gated (`canManage` = `canRun`): viewers see a read-only `enabled`/`paused` tag, no controls — matching the backend gate. Timezone list comes from `Intl.supportedValuesOf('timeZone')` (no hardcoded table; UTC-first). Cron/timezone validated server-side → 422 surfaced inline. `api/schedules.ts` + 6 component tests (list, empty, create-with-UTC-default, pause-toggle, delete, viewer-read-only). **(A6)**
- [ ] 🔵 Recent runs audit table with drill-down link to results — **moved to Week 6** (counted there), folded into the Enhanced Monitoring Dashboard (`/dashboard`) where the prototype homes the recent-runs widget. Rationale in the group note above + the Week-6 task.
- [x] ✅ **Suite Triggers panel** — UI over the `trigger_bindings` CRUD (#172): bind/unbind a pipeline/DAG (`provider` + `pipeline_or_dag_id` + `env`) to a suite so it runs on the pipeline's success. **Suite-level**, not a check field (orchestration providers are never a datasource/check attribute, CLAUDE.md §4); inline `TriggersPanel` card on the suite detail — list + enable/disable toggle + remove + add form (ADF/Airflow · pipeline-or-DAG id · env), edit-gated (read-only for viewers). `api/triggerBindings.ts` + 6 component tests. _(PR #247; closes [#216](https://github.com/TheurgicDuke771/DataQ/issues/216))_

**Week 5 total: 18 / 18 ✅ — COMPLETE** _(exit gate met: async runs with live progress across all datasource types + scheduling operational. Early credit: Celery task runner in PR 4; worker runner-dispatch #146; ADF + Airflow 10-min polling #171; plus `trigger_bindings` CRUD #172 out-of-band; check target-table + dispatch ungate #215 (PR-C0a); manual run trigger + run/result read API (PR-C0b); **Suite Triggers UI #216 (PR #247)**. **This stretch (Phase 1–4):** `error` status #297, flat-file batch + `skip` #298, UC run-path coverage #299, run progress API #301, cancel run + #227 #302, `GET /orchestration/pipelines` #305, gap-recovery beat #307, **scheduling backend (A7)**, **live run-progress UI (A3)**, **scheduled-runs UI (A6)**, **run-history retention (A8)**, **run-now panel**. +3 tasks re-tracked here: check target-table #215 (slipped from W3), Suite Triggers UI #216, and the run-enablement read API (PR-C0b); +1 added: the scheduling backend (A7), the backend the roadmap's scheduled-runs UI task implies. The **manual Run button** (PR #245) + the **run-target editor** (PR #246) also shipped earlier this week. **Closeout cleanup:** profiler `null_fraction` DRY ([#147](https://github.com/TheurgicDuke771/DataQ/issues/147), PR #329) + `RUN_BAR_STATUS` refactor ([#317](https://github.com/TheurgicDuke771/DataQ/issues/317), PR #328); profiler N+1 perf split to backlog [#327](https://github.com/TheurgicDuke771/DataQ/issues/327). **Moved to Week 6:** recent-runs audit table — see the Execution-UI note above + the Week-6 task.)_

---

## Week 6 — Results dashboard + alerting

**Exit gate:** Full results dashboard live across all source types; alerts firing with suppression.

### Results dashboard (11 tasks — 11/11 ✅)
> **Build as an in-app React page** (the GX-Cloud-style redesign Phase C), **not Grafana** — reading Postgres directly would bypass DataQ's per-suite sharing + PII redaction; Grafana is deferred to an optional post-v1 ops add-on ([ADR 0018](adr/0018-results-surface-and-grafana-deferral.md), accepted). The Week-5 run-enablement backend it gates on is **done**: `runs.py` read endpoints (`GET /runs`, `GET /runs/{id}`, `GET /pipeline_runs`) + `POST /suites/{id}/run` (PR-C0b); [#215](https://github.com/TheurgicDuke771/DataQ/issues/215) target (PR-C0a). The `results.metric_value`/`duration_ms` columns were seam-built for these trend charts (ADR 0012).
- [x] ✅ **Results page scaffold** (PR-C1) — the in-app `/results` page + sidebar nav (Connections · Suites · **Results** · Profile). **Runs tab**: a runs table (suite name, status, triggered-by, started, duration) with a status filter, each row opening a **run-detail drawer** that drills into the per-check results (check name + expectation, severity tag, `metric_value`, observed value). **Pipeline runs tab**: the orchestration monitoring feed (`pipeline_runs`) with a provider filter. Pure presentation helpers (`resultsFormat.ts` — duration/timestamp formatters, status→colour maps) are unit-tested; `api/runs.ts` mirrors the C0b schemas. Demo seed (`demo_data.py`) now lands runs + results (pass/pass/warn/fail spread) + two pipeline-runs + suite targets so the page has real content. 10 vitest + 2 Playwright (`results.spec.ts`). _The rich widgets below build on this scaffold._
- [x] ✅ Health score stat cards + 7-day trend chart — `/dashboard` `MetricCard` row (Data Integrity / Pass-rate / Total Runs / Active Connections — only backend-backed KPIs, dropped Anomalies + Time-to-Resolution for KPI honesty; 24h/7d/30d Segmented → `window_days`) + the recharts stacked-bar **Quality Trends** chart (succeeded vs failed runs/day, zero-filled), both on `GET /dashboard/summary` (PR #333, Phase 2.1–2.3)
- [x] ✅ Per-suite pass / fail progress bars — warn / fail / critical breakdown — per-suite health `Progress` bars (state-coloured, worst-first; "No data" for null-score suites) beside Quality Trends, on the `summary` per-suite performance block (PR #333, Phase 2.4)
- [x] ✅ Results filter bar — env, datasource type, suite, date range, status — run **status** + pipeline **provider** filters shipped (PR-C1); **env / datasource / suite / date-window** added to the runs tab (PR-C2). Env + datasource joined via each suite's connection; date filter uses preset windows (24h/7d/30d, no dayjs dep). _(folds in the **Datasource type filter** task below — ADLS+S3 collapse into one "Flat file" category)_
- [x] ✅ Failed check drill-down — sample failing rows from GX result — per-check drill-down (routed run-detail page) + **redacted** sample failing rows surfaced ([#365](https://github.com/TheurgicDuke771/DataQ/pull/365), closes [#226](https://github.com/TheurgicDuke771/DataQ/issues/226); row-level PII redaction, ADR 0018)
- [x] ✅ Per-check historical trend chart — run-detail expandable result row → recharts line of `metric_value` over runs (points coloured by severity, lazy per-row fetch), on `GET /suites/{sid}/checks/{cid}/history` (PR #333, Phase 2.6; home = run-detail page, per-check in context)
- [x] ✅ Orchestration status panel — pipeline/DAG status, polls every 30s, correlated DQ result — pipeline-runs feed + status shipped (PR-C1); **30s auto-poll** + a **DQ-run** column correlating each pipeline run to the run(s) it triggered via the `triggered_by` marker `<provider>:<pipeline>:<run_id>` added (PR-C2)
- [x] ✅ Datasource type filter — Snowflake / flat file / Unity Catalog toggle — shipped as part of the Results filter bar above (PR-C2; new `DATASOURCE_CATEGORY` map collapses ADLS+S3 → "Flat file")
- [x] ✅ CSV + PDF export of results — **CSV + JSON** download menu on the run-detail header (RFC-4180 CSV; JSON = run meta + checks; sample rows never in the payload, PII). **PDF deferred to backlog** (extra dep, little gain over CSV/JSON) (PR #333, Phase 1.2)
- [x] ✅ **Recent runs audit table with drill-down link to results** _(moved from Week 5)_ — cross-suite recent-runs widget on `/dashboard` (`GET /runs`, limit 8), each row deep-linking to `/results/:runId` + "View all" → Results (PR #333, Phase 2.5)
- [x] ✅ Severity badge colours — green / amber / red / dark red — severity-tier (pass/warn/fail/critical) + run-status tag colour maps in `resultsFormat.ts`, rendered as antd Tags on the runs table + run detail (PR-C1)
- [x] ✅ Health score weighting — apply warn/fail/critical penalty weights — health score computed in SQL per ADR 0005 weights (warn 0.5 / fail 1.0 / critical 2.0) in `dashboard_service`, surfaced on the dashboard summary + per-suite bars (PR #333, Phase 2.1)

### Alerting (6 tasks — 6/6 ✅)
- [x] ✅ `ResultPublisher` seam — run outcomes dispatched from the worker's post-`execute_run` completion point through a `ResultPublisher` interface (Teams is the v1 impl, not a hardcoded call); PII redaction policy on `sample_failures` applied at the seam (`alerting/builder.redact` via `run_service.redact_sample_failures`). No-op publisher when unconfigured. Post-v1 JIRA/TestRail/Xray slot in as additive impls (ADR 0011). **[PR #366](https://github.com/TheurgicDuke771/DataQ/pull/366)**
- [x] ✅ Teams adaptive card payload — `alerting/card.render_teams_message` (Adaptive Card 1.4): suite, datasource, target, pass/fail tally, per-failing-check observed-vs-expected (redacted sample counts), success card for clean `always` heartbeats. `TeamsPublisher` posts it. **[PR #367](https://github.com/TheurgicDuke771/DataQ/pull/367)**
- [x] ✅ Severity-aware alert routing — `alerting/routing.route_for`: warn = quiet, fail = standard, critical = @channel escalation banner; the one send/urgency policy point. **[PR #368](https://github.com/TheurgicDuke771/DataQ/pull/368)**
- [x] ✅ Alert dedup — `alerting/dedup`: fire on the first failure of a streak + on escalation, suppress unchanged repeats (per-`check_id`, derived from the previous terminal run — no new state); recovery resets the baseline; `always` bypasses dedup. **[PR #369](https://github.com/TheurgicDuke771/DataQ/pull/369)**
- [x] ✅ Alert suppression / snooze — silence a specific check for N hours (`checks.alert_snoozed_until` migration + `POST/DELETE /suites/{sid}/checks/{cid}/snooze` + `alerting/suppression`; a partial snooze still alerts on the live checks). **[PR #370](https://github.com/TheurgicDuke771/DataQ/pull/370)**
- [x] ✅ Notification config UI — Teams webhook per suite + alert on fail/warn/always. Backend: `suite_notifications` table + `notification_service` + `GET/PUT/DELETE /suites/{id}/notifications` (webhook write-only via SecretStore; `route_for` policy gate) **[PR #373](https://github.com/TheurgicDuke771/DataQ/pull/373)**; UI: suite-detail `NotificationsPanel` **[PR #378](https://github.com/TheurgicDuke771/DataQ/pull/378)**

### Prototype adoption — full screen set as dedicated-page flows (added — not in original roadmap) (10 tasks — 10/10 ✅) — tracked in epic [#175](https://github.com/TheurgicDuke771/DataQ/issues/175); ADR [0022](adr/0022-week6-prototype-adoption-and-chart-library.md)
> Adopt the **DataQ Design System** prototype (claude.ai/design project *"DataQ Design System"*, reverse-engineered from the shipped indigo/antd app — its `readme.md` is the spec) — **the full 13-screen set this week**, not a subset (ADR [0022](adr/0022-week6-prototype-adoption-and-chart-library.md); the earlier 6-task scope under-counted it — Profile content, **Settings** (pulled in from Week 7), the Admin layout-reconcile, and the New-Connection source-select split were missing). The **foundation already matches** the product (`theme.ts` palette, `resultsFormat.ts` status model, the 56px-header/220px-sider shell), so this is **not a re-skin** — it adopts the prototype's **structure**: every create/edit flow becomes a dedicated, deep-linkable **page** (the **Share** drawer stays the *only* drawer), login lands on an **Enhanced Monitoring Dashboard**, and standard **error pages** ship. **Where the prototype differs from the shipped app (drawer-vs-page, layout), the prototype wins.** Components are reproduced with **real antd** — the prototype's framework-free recreations + `_ds_bundle.js` are reference only (per the design README). Post-v1 items (marketing, dark mode) + the polish passes (KPI honesty, visual-fidelity) stay in Week 7 / Post-v1 below. The intentionally-dropped prototype bits (the **"View as" role switch**, the **component bundle**, the **teal palette**) are recorded in Post-v1 as *not adopted*.
>
> **Design source — how to open the prototype (read before picking up any task in this track):** the claude.ai/design project **"DataQ Design System"** — `projectId` **`317fec67-2b8a-498c-8f6b-523750916a8d`** (owner Arijit). Read it with the **`/design-sync`** skill / **`DesignSync`** tool over the claude.ai login: `DesignSync method=list_files projectId=…`, then `method=get_file path=<…>`. Key paths: **`readme.md`** = the canonical spec (voice · color · type · spacing · status model · component/template manifest); **`guidelines/*.card.html`** = foundation specimen cards; **`templates/app/*.jsx`** = framework-free recreations of each screen; **`components/<group>/*.jsx`** = the component recreations; **`tokens/*.css`** = raw token values; **`uploads/DESIGN.md`** = the *superseded teal* brief (ignore for palette). ⚠️ The hosted share link `https://api.anthropic.com/v1/design/h/SVkQJqee7xsc2FNWVOsnIQ` returns **404** — reach the prototype via DesignSync, not the link. The `.jsx` / `_ds_bundle.js` are **reference only** — build in real antd reading `theme.ts`.
- [x] ✅ **Nav / shell restructure** — primary sider **Dashboard · Connections · Suites · Results · Profile** (with icons) + bottom-pinned footer group **Admin · Settings · Documentation** (Documentation = disabled placeholder); `/` → `/dashboard` landing; `/settings` shell registered (admin-gated like Admin), footer Admin/Settings shown only to workspace admins, routes always registered (deep-link → Forbidden) (PR #333, Phase 0.2). _Design ref: `templates/app/AppShell.jsx`._
- [x] ✅ **Enhanced Monitoring Dashboard page** (`/dashboard`, `DashboardScreen`) — the landing screen hosting the dashboard widgets (KPI MetricCards, recharts Quality-Trends chart, per-suite performance bars, recent-runs table — see the Results-dashboard group above). **Chart library = `recharts`** (ADR [0022](adr/0022-week6-prototype-adoption-and-chart-library.md); lazy-loaded into its own chunk so it ships only on the `/dashboard` route — vendor chunk 388→42 kB; clears `pnpm audit` + bundle gate). Backend `GET /dashboard/summary` aggregates (health score + pass rate + run trend + per-suite perf), suite-authz-scoped, null KPIs on empty workspace (KPI honesty) (PR #333, Phase 0.1 + 2.1–2.5). _Design ref: `templates/app/DashboardScreen.jsx`._
- [x] ✅ **New Suite → dedicated page** (`/suites/new`) — extracted a shared `SuiteForm` (name/description/connection picker + datasource-shaped run-target inputs) from `SuiteDrawer`; the routed `/suites/new` page reuses it and, on create, continues to `/suites/:id/checks/new` so a fresh suite is never left empty. `Suites.tsx` swaps `setDrawer({open:true})` for `navigate('/suites/new')`. _(Phase 4.1, commit `1f437fa`; `SuiteForm.tsx` + `SuiteNew.tsx`.)_
- [x] ✅ **Edit flows → dedicated pages** — connection edit `/connections/:id/edit` → `ConnectionEdit` (reuses `ConnectionForm`); suite edit `/suites/:id/edit` → `SuiteEdit` (reuses `SuiteForm`); check edit `/suites/:id/checks/:checkId/edit` → `CheckEdit`. **Removed** the `ConnectionDrawer` / `CheckDrawer` / `SuiteDrawer` components + their tests; parents drop `editing` / `drawer` state for `navigate()`. Create + edit share one form per entity (`ConnectionForm` / `SuiteForm` gained an edit mode; check form helpers were already shared); added `getCheck`. Each edit view is keyed on its route id so a param-only navigation reloads cleanly (review fix `ddbdc1d`). Version history stays the one read-only drawer (alongside Share). (Dedups [#204](https://github.com/TheurgicDuke771/DataQ/issues/204).) _(Phase 4.2, commit `95c7cd2`.)_
- [x] ✅ **Results drill-down → dedicated route** (`/results/:runId`, `RunDetail`) — promoted the in-page `openRun` drawer to a routed run-detail page with back-nav, stat cards (status / checks-passed / triggered-by / started / duration), per-check results table (severity tags + `metric_value` + observed), the per-check trend expandable row, and CSV/JSON download. Deep-linkable + refreshable (fetch by id); row click in the runs table navigates here (PR #333, Phase 1.1–1.2). _Design ref: `templates/app/ResultsScreen.jsx` → `RunDetailScreen`._
- [x] ✅ **Standard error pages (4xx / 5xx)** — `components/feedback/ErrorState` catalog switchable across 400/401/403/404/429/500/502/503/504; wired into the router catch-all (routed `NotFound`/404) + the `ErrorBoundary` fallback (500); `Forbidden` (403) folded onto it. Server errors show the request id; recovery action follows the tone. **[PR #377](https://github.com/TheurgicDuke771/DataQ/pull/377)**
- [x] ✅ **New Connection → source-select + config (2-page flow)** — `ConnectionNew` now leads with a categorized **Select Source** step (**datasources first**: Warehouses · Lakehouses · Cloud Storage, then **Orchestration last** — it's optional, suites also run on a cron/on-demand; each category has a lead-in note distinct from the per-source blurb; cards gain the type avatar + a one-line blurb + a chevron) → the spec-driven **Configuration** step (`ConnectionForm`). Grouping is presentation-only (`connectionSources.ts`); the load-bearing datasource-vs-orchestration split still flows through `CONNECTION_KIND`. _(Phase 4.3, initial commit `94af936`; datasources-first reorder + per-category notes added in commit `a390d2f` (PR #353); `connectionSources.ts` + `ConnectionNew.tsx`.)_
- [x] ✅ **Profile page — real content** (`/profile`, `ProfileScreen`) — replaced the placeholder `Home` with an identity card (avatar, name, email, role tag) + a Workspace facts card (auth = Azure AD/MSAL, role, last seen) rendered only from `/me` (KPI honesty), plus an **Alert channels** card. Since notifications are configured **per suite** (not per user), the card states that honestly and links to Suites rather than fake per-user toggles. **[PR #374](https://github.com/TheurgicDuke771/DataQ/pull/374)**
- [x] ✅ **Workspace Settings page** (`/settings`, `SettingsScreen`) — _(moved in from Week 7)_ — tabbed shell **General · Secrets · Notifications · Danger zone**. No settings backend in v1, so only honest content ships: General workspace facts, Secrets (credentials in the configured secret store — no hardcoded Azure names, ADR 0010/0013), Notifications (points at per-suite config), Danger zone (labelled placeholder). **[PR #375](https://github.com/TheurgicDuke771/DataQ/pull/375)**
- [x] ✅ **Admin page — reconcile layout to prototype** — `Admin.tsx` reconciled to the prototype: **KPI MetricCards (Suites / Members / Access grants) over stacked tables (All suites + Members & access), no tabs.** Presentation only, same `/admin/{suites,users,access}` endpoints (#289), same authz gate (a non-admin still fetches nothing). **[PR #376](https://github.com/TheurgicDuke771/DataQ/pull/376)**

**Week 6 total: 28 / 28 ✅ — COMPLETE** _(exit gate met: full results dashboard across all source types + alerts firing with suppression. **Results dashboard (12/12):** scaffold (PR-C1) · Enhanced Monitoring Dashboard + run-detail route + CSV/JSON export + per-check trend (#333) · filter bar + **datasource-type filter** (counted as its own roadmap task) + orchestration poll/correlation (#347) · recent-runs audit table (moved in from Week 5) · redacted sample failing rows (#365, closes #226). **Alerting (6/6):** `ResultPublisher` seam (#366) · Teams adaptive card (#367) · severity routing (#368) · dedup (#369) · suppression/snooze (#370) · per-suite notification config backend (#373) + UI (#378). **Prototype adoption (10/10):** nav/shell + dashboard page (#333) · new-suite page + edit-flows→pages + source-select split (#350) · results drill-down route (#333) · Profile (#374) · Settings (#375) · Admin reconcile (#376) · error pages (#377). All **merged to `main`** as the stacked PR chain #366→#378. **Week-7 early-credit:** Azure deploy scaffolding (#379); follow-ups #380/#381 + the live run-progress histogram fix #390 (closes #316) merged after.)_

---

## Week 7 — Deployment, hardening & docs

**Exit gate:** Production-ready v1 deployed to Azure, CI/CD live, team onboarded.

### Week 1–6 carryover into Week 7 (deferred-task triage)

The `🟡`/`🔵` partial/deferred items from Weeks 1–6, triaged with their current disposition. Most were already tracked as the Week-1-carryover issues or as Week-7 roadmap items; this consolidates them so none is dropped.

| Wk | Deferred item | Status | Disposition |
|---|---|---|---|
| W1 | Silent token refresh — `InteractionRequiredAuthError` fallback after `acquireTokenSilent` | ✅ **done** | [#168](https://github.com/TheurgicDuke771/DataQ/issues/168) **closed** — superseded by the ADR 0028 §5 cutover (generic `oidc-client-ts` silent renew), validated live against real Entra SSO |
| W1 | Provision real Azure Key Vault + validate `DefaultAzureCredential` end-to-end | ✅ **done** | [#169](https://github.com/TheurgicDuke771/DataQ/issues/169) **closed** — discharged by the deploy (ADR 0024 KV+UAMI; #408 credential path; prod reads KV live) |
| W1 | Disable Swagger `/docs`+`/redoc` in production (prod-docs gate) | ✅ **done** | [#170](https://github.com/TheurgicDuke771/DataQ/issues/170) **closed** by [#464](https://github.com/TheurgicDuke771/DataQ/pull/464) (prod-docs gate) |
| W2 | ADF pipeline-run upsert **+ correlate** (trigger-on-success) | ✅ **done** | landed in W5 via [#215](https://github.com/TheurgicDuke771/DataQ/issues/215) (run dispatch ungated); the W2 `🟡` marker is stale |
| W2/W4 | Surface the ADF webhook URL (don't hand-assemble a secret-bearing URL) | ✅ **done** | [#92](https://github.com/TheurgicDuke771/DataQ/issues/92) **closed** — in-app webhook URL surface (Settings → Webhooks, #493/#496) |
| W6 | App Insights **span** instrumentation (request/task traces, slow-query hook) | ✅ done (#525) | request/task spans live — OTel-neutral core (`core/tracing.py`, ADR 0010) + Azure exporter-only; slow-query hook + opencensus→OTel logs → [#524](https://github.com/TheurgicDuke771/DataQ/issues/524) |
| W1 | Live warehouse/file run smoke (Reference Flows B/C) | **discharged 2026-07-02** | W7 live-smoke ran via the #531 lane — Flows A/B/C verified green against the live harness (Snowflake ×3 / UC / flat-file) |

_Not W1–6 carryover: the Week-8 rows below were largely covered early by per-slice tests landed with the feature PRs; the W8 audit (2026-07-03, #556–#560) confirmed each against the ≥80% bar and flipped the gates._

### DevOps & deployment (10 tasks — 10/10 ✅)
- [x] ✅ Containerise FastAPI + React + Celery + Redis — backend `Dockerfile` (multi-stage `python:3.13-slim` + pip, ADR 0025) + `frontend/Dockerfile` + self-hosted `dataq-app-redis` (password-auth) on ACA; React UI ships via the SWA (`dataq-app-web`). All live ([PR #394](https://github.com/TheurgicDuke771/DataQ/pull/394))
- [x] ✅ Push images to container registry — **GHCR, not ACR** (ADR [0023](adr/0023-container-image-registry-ghcr.md)); CI pushes `ghcr.io/theurgicduke771/dataq-backend:<tag>` via `GITHUB_TOKEN`; public package → ACA pulls anonymously (no stored registry credential)
- [x] ✅ Deploy to Azure Container Apps (API + Celery worker + migrate job) + Azure Static Web App (React UI) — **DONE** ([#394](https://github.com/TheurgicDuke771/DataQ/pull/394), ADR 0024): in-repo Terraform stood up `dataq-app-{api,worker}` + `dataq-app-migrate` on the shared `dataq-cae` env, SWA linked as same-origin `/api` backend (CORS middleware in place), Key Vault (UAMI) + App Insights + Log Analytics, distinct `dataq` DB + least-priv `dataq_app` role on the shared Postgres (no hardcoded creds — secret refs), AAD SSO app registrations wired
- [x] ✅ CI/CD pipeline — lint, test, build, deploy. The `Deploy` workflow (`workflow_dispatch`) is **validated end-to-end** (build → push GHCR → `alembic upgrade head` migrate job → ACA API/worker roll → SWA deploy, all green on `v6`). The first real run exposed two `deploy.yml` bugs — migrate job ran uvicorn not alembic ([#401](https://github.com/TheurgicDuke771/DataQ/issues/401)), frontend `pnpm/action-setup` version unset ([#402](https://github.com/TheurgicDuke771/DataQ/issues/402)) — fixed in [#403](https://github.com/TheurgicDuke771/DataQ/pull/403). **GHCR package→repo connect done** (Actions-access grant; CI's `GITHUB_TOKEN` now pushes — ADR [0023](adr/0023-container-image-registry-ghcr.md)). _(Push-trigger-on-merge stays off for now — manual dispatch only.)_
- [x] ✅ **Post-deploy bug: Celery beat crash on startup (closes [#405](https://github.com/TheurgicDuke771/DataQ/issues/405))** — the embedded `worker -B` beat process was re-nulling `self.lock` inside `AzureLogHandler.createLock()` on Python 3.13 (a recurrence of the #393 lock issue in the beat fork), silently killing **all** periodic tasks — orchestration polling (ADF + Airflow), scheduled-suite dispatch, gap recovery, and the sample-failure purge. Fix: override `createLock` in `backend/app/core/logging.py` to be idempotent (only ever create a missing lock, never null/swap one) + a network-free regression test in `backend/tests/core/test_logging.py`. — **[#407](https://github.com/TheurgicDuke771/DataQ/pull/407)**
- [x] ✅ **Post-deploy bug: deployed app couldn't read Key Vault (closes [#406](https://github.com/TheurgicDuke771/DataQ/issues/406))** — `AzureKeyVaultStore` called `DefaultAzureCredential()` with no args, but the api+worker containers run a USER-assigned managed identity and `AZURE_CLIENT_ID` was unset in the Container Apps env, so every `get_secret` call failed (`SecretNotFoundError` / `DefaultAzureCredential failed`). This blocked connection tests, suite runs, AND orchestration polling. Fix: add `AZURE_CLIENT_ID = azurerm_user_assigned_identity.app.client_id` to `local.app_env` in `deploy/terraform/azure/containerapps.tf` (shared by the api and worker). — **[#408](https://github.com/TheurgicDuke771/DataQ/pull/408)**
- [x] ✅ **Redeploy after post-deploy fixes — now at image `:v10`** — `image_tag` default tracks the live prod image: `:v7` (#405 beat-lock + #406 KV `AZURE_CLIENT_ID`, [#409](https://github.com/TheurgicDuke771/DataQ/pull/409)) → `:v8` (Slack+email alerting #413) → `:v9` (column-aware redaction #417 + the #383/#384/#395/#423 hardening batch, [#414](https://github.com/TheurgicDuke771/DataQ/pull/414)) → **`:v10`** (freshness/volume monitors #426 + authoring UI #437 + runs-table outcome #425, [#438](https://github.com/TheurgicDuke771/DataQ/pull/438)). Each bumped in `deploy/terraform/azure/variables.tf`. Verified live: beat clean, api healthy (401, auth-enforced), orchestration polling reads KV secrets.
- [x] ✅ **Cloud-neutral cutover: frontend Static Web App → Container App (ADR [0028](adr/0028-cloud-neutral-image-runtime-config-generic-oidc.md) §5)** — the deployed frontend is now `dataq-app-frontend`, the **sole public surface**, running the one generic nginx image with runtime `DATAQ_AUTH_*` OIDC config (**MSAL retired** → generic oidc-client-ts; validated live as Olivia → dashboard). The **api moved to internal ingress** (reached only via the frontend proxy `/api` + `/mcp` + `/healthz`); the SWA is destroyed. Chain **[#509](https://github.com/TheurgicDuke771/DataQ/pull/509)** (cutover) → **[#510](https://github.com/TheurgicDuke771/DataQ/pull/510)** (`lifecycle ignore_changes` on container images so applies never roll prod back + on the api `identifier_uris` so applies never strip the token audience) → **[#511](https://github.com/TheurgicDuke771/DataQ/pull/511)** (three ACA gotchas: nginx must proxy **HTTP/1.1** or ACA 426s; api ingress **HTTP + `allow_insecure_connections`**; orphaned SWA-**EasyAuth** on the api disabled — 401'd everything post-SWA-destroy). Prod frontend `:v2`. Follow-up **[#512](https://github.com/TheurgicDuke771/DataQ/issues/512)** (multi-arch frontend QEMU build impractically slow/flaky) **fixed by [#518](https://github.com/TheurgicDuke771/DataQ/pull/518)** — `--platform=$BUILDPLATFORM` cross-build of the arch-neutral Vite build; frontend publish 45-min-stall → ~1min, native-arm64 `:latest` restored. AWS/GCP deploy IaC stays post-v1 ([#505](https://github.com/TheurgicDuke771/DataQ/issues/505)).
- [x] ✅ Application Insights integration — logs/exceptions live (conn-string-gated `AzureLogHandler`, #393/#405 Py3.13 fixes) **+ request/task spans** ([#525](https://github.com/TheurgicDuke771/DataQ/pull/525)): vendor-neutral OTel core (`core/tracing.py`, [ADR 0010](adr/0010-provider-agnostic-infrastructure-seams.md)) with the Azure exporter only — module-scope FastAPI instrumentation, producer+consumer Celery instrumentation, anchored URL exclusions (webhook `?token=` never reaches attributes), query-string scrub, `dataq.request_id` span↔log join key. Slow-query hook + opencensus→OTel **log** migration → [#524](https://github.com/TheurgicDuke771/DataQ/issues/524)
- [x] ✅ Real-vault integration test for `AzureKeyVaultStore` lazy-import branch ([#523](https://github.com/TheurgicDuke771/DataQ/pull/523) — module-level SDK patching exercises the real import/construct/cache path; `secrets.py` 100%)

### Azure Monitor webhook setup (post-deployment) (5 tasks — 5/5 ✅; closed [#492](https://github.com/TheurgicDuke771/DataQ/issues/492), 2026-07-02)
> v1 deploys a **single prod environment** (no pre-prod stage exists), so the pre-prod pair collapses into the prod pair; the harness ADF factory (`dataq-harness-adf`, ADR 0021) is the monitored factory.
- [x] ✅ Action Group (pre-prod) — **N/A by design**: no pre-prod environment in v1
- [x] ✅ Alert Rule (pre-prod) — **N/A by design**: no pre-prod environment in v1
- [x] ✅ Action Group (prod) — `dataq-adf-ag` → webhook to the in-app-generated ADF events URL (Settings → Webhooks, #493/#496) + owner email
- [x] ✅ Alert Rule (prod) — `dataq-adf-failed-runs` metric alert on `dataq-harness-adf` (`PipelineFailedRuns > 0`, 1-min frequency, Common Alert Schema ON, pipeline-Name dimension); the schema carries no runId, so the receiver treats it as an `AlertPing` → immediate-poll ingest ([#534](https://github.com/TheurgicDuke771/DataQ/pull/534))
- [x] ✅ Smoke test — deliberate `pl_dataq_smoke_fail` failure (Fail-activity pipeline, kept in the factory for re-smoking) visible in DataQ **4m14s** after fire — inside the 5-min target

### FastMCP — MCP server (12 tasks — 12/12 ✅) — [#460](https://github.com/TheurgicDuke771/DataQ/pull/460), ADR [0008](adr/0008-mcp-server.md)
> All 8 shipped as MCP **tools** (not resources): an LLM invokes tools from natural language, whereas resource-templates with required args aren't reliably auto-called (ADR 0008). Each is a thin wrapper reusing the service layer + per-suite authz + sample redaction.
- [x] ✅ Install FastMCP, scaffold `backend/app/mcp/`, mount at `/mcp` (`http_app` + `combine_lifespans`)
- [x] ✅ Wire Azure AD token validation into FastMCP's auth provider — `JWTVerifier` (same tenant/audience/scope as REST; guest + mandatory-`oid` parity); fail-closed (unmounted) without auth
- [x] ✅ `list_suites` (tool)
- [x] ✅ `get_suite_results` (tool; reuses `redact_sample_failures`)
- [x] ✅ `get_health_score` (tool)
- [x] ✅ `get_adf_pipeline_status` (tool; correlated DQ run scoped to accessible suites)
- [x] ✅ `trigger_suite_run` (tool)
- [x] ✅ `get_run_status` (tool)
- [x] ✅ `create_check` (tool)
- [x] ✅ `profile_column` (tool)
- [x] ✅ LLM-optimised docstrings for all 8 tools (20 tests: tool shape + authz + both auth modes)
- [x] ✅ E2E test with Claude Desktop — 4 canonical NL queries — **protocol level PASSED 2026-07-02** (the 4-query smoke ran against live `/mcp` with a real Azure AD token: initialize/list/call all green, so the `JWTVerifier` audience assumption is validated) + **real-client-config E2E PASSED 2026-07-03** ([#550](https://github.com/TheurgicDuke771/DataQ/pull/550)): a VS Code workspace `.vscode/mcp.json` (Copilot agent-mode shape) against live `/mcp/` exercised **all 8 tools end-to-end** — triggered run succeeded (3 pass / 1 seeded fail), created check cleaned up via REST, live Snowflake column profile. Client setup guide now lives at [mcp-setup.md](mcp-setup.md); ongoing in-client NL spot-checks ride routine use (MCP expansion: post-v1 Theme 13, #529)

### Hardening & docs (6 tasks — 6/6 ✅)
- [x] ✅ E2E test coverage for critical paths — full-stack E2E landed early ([#128](https://github.com/TheurgicDuke771/DataQ/issues/128)): API smoke (`backend/scripts/e2e_smoke.py`, 12/12) + browser smoke (`frontend/e2e/`, Playwright, 25 specs, CI `frontend-e2e` job) cover auth/dev-bypass + the read + authoring paths. **Live run paths verified 2026-07-02** via the [#531](https://github.com/TheurgicDuke771/DataQ/pull/531) live-smoke lane: Flows A/B/C green (Snowflake ×3 / UC / flat-file), browser lane 3/3, `e2e_smoke.py` bearer-mode vs prod.
- [x] ✅ Error handling audit — consistent error shapes across all endpoints — **verified** ([#465](https://github.com/TheurgicDuke771/DataQ/pull/465)): all four exception handlers (DataQError / HTTPException / RequestValidationError / unhandled) emit the same `{error:{code,message,detail}}` envelope via `_envelope`. (Validation `ctx` serialization edge tracked in #371.)
- [x] ✅ Ensure all FastAPI endpoints have `summary`, `description`, `tags`, `response_model` — [#465](https://github.com/TheurgicDuke771/DataQ/pull/465): added the one gap (`/me` summary+docstring) + a guardrail test asserting every `/api/*` op carries a summary + tags (DELETEs are 204, no `response_model`)
- [x] ✅ README + deployment guide + env-var reference — [#468](https://github.com/TheurgicDuke771/DataQ/pull/468): `deploy/README.md` runbook + `.env.app.example` completed to all 37 Settings keys + root-README pointers; prod-docs gate disables Swagger in prod ([#170](https://github.com/TheurgicDuke771/DataQ/pull/464))
- [x] ✅ Team onboarding session + feedback collection — **discharged 2026-07-02**: solo-dev project, so no separate live session — the six demo Entra users (Olivia/Vera/Adam/Ethan/Nora/Wesley) are onboarded on the deployed app with seven cross-shared suites at owner/edit/view + one workspace-admin (ADR 0027), exercising every access tier end-to-end
- [x] ✅ Document MCP connection config (Claude Desktop / Claude.ai / Copilot / Cursor) in README — [#460](https://github.com/TheurgicDuke771/DataQ/pull/460) (the README "MCP (AI assistant access)" section)

### Consistency & transaction hardening (added — from the ACID/consistency review) (4 items — 4/4 ✅)
> Follow-ups surfaced by the ACID / SCD review (2026-06-20). Single-DB ACID is sound (Postgres + unit-of-work + idempotent `ON CONFLICT` upserts); these close the deliberate gaps. Not in the original 100-task roadmap — tracked here so they aren't lost.
- [x] ✅ **Trigger-dedup race** — fixed in [#456](https://github.com/TheurgicDuke771/DataQ/pull/456) (closes [#308](https://github.com/TheurgicDuke771/DataQ/issues/308)): a **partial unique index** on `runs(suite_id, triggered_by)` scoped to orchestration markers (`adf:`/`airflow:`) + `INSERT … ON CONFLICT DO NOTHING` in `_trigger_suites`. Manual/probe/schedule markers legitimately repeat (positive predicate degrades safely). Follow-up [#457](https://github.com/TheurgicDuke771/DataQ/issues/457) (drift guard for a future provider).
- [x] ✅ **Stuck-run reaper** — fixed in [#458](https://github.com/TheurgicDuke771/DataQ/pull/458) (closes [#309](https://github.com/TheurgicDuke771/DataQ/issues/309)): a beat janitor fails runs left non-terminal past `STUCK_RUN_THRESHOLD_MINUTES` (default 60); **fails** rather than re-dispatches (a null `celery_task_id` doesn't prove the task wasn't published), and deliberately doesn't alert (an over-threshold `running` run is indistinguishable from slow-but-alive — avoids an irreversible false alarm).
- [x] ✅ **History/audit strategy (ADR)** — decided in [ADR 0020](adr/0020-history-and-audit-strategy.md): per-entity **Type-4** snapshot tables where config history is needed (no SCD-2; credentials never snapshotted; cascade-delete accepted; soft-delete + cross-entity audit log deferred). **First implementation: `connection_versions`** (connection name/config history, snapshot on create + name/config update; secret-only rotation records nothing). [#310](https://github.com/TheurgicDuke771/DataQ/issues/310) _(audit-log + soft-delete remain deferred there)_
- [x] ✅ **Minor robustness (no separate issue)** — concurrent check-edit collision now → **409** `check_edit_conflict` not an unhandled 500 (`update_check` catches the `IntegrityError` on the `uq_check_versions_check_version` backstop, rolls back, re-raises a `CheckEditConflictError`); explicit `rollback` added to `get_db`'s `except`; the "read-modify-write is only as safe as its unique constraint / `FOR UPDATE`" convention documented in CONTRIBUTING (rule 32). Full-stack 409 regression test.

### Prototype adoption — final polish passes (added — not in original roadmap) (4 tasks — 4/4 ✅) — tracked in epic [#176](https://github.com/TheurgicDuke771/DataQ/issues/176)
> Closes out the prototype after the Week-6 structural pass (the full screen set — incl. **Settings** — now lands in Week 6, ADR [0022](adr/0022-week6-prototype-adoption-and-chart-library.md)): what remains here is the **KPI honesty pass** + the final **visual-fidelity pass** to the design-system guidelines. Admin needed a real **workspace-admin** authz concept (today `admin` is per-suite only) — the prototype's client-side "View as" switch is **not** used (real authz is server-side, CLAUDE.md §11); that backend + page shipped early via #289. **To open the prototype, see the *Design source* note in the Week-6 prototype-adoption group** (project `317fec67-2b8a-498c-8f6b-523750916a8d` via the `DesignSync` tool).
- [x] ✅ **Backend: workspace-admin list-all endpoint** — **shipped early in Week 4 (#289)** to unblock the Admin page. `GET /admin/suites` · `/admin/users` · `/admin/access` behind a **workspace-admin** authz check (config `WORKSPACE_ADMIN_EMAILS` allowlist, distinct from the per-suite `view < edit < admin < owner` ladder; no migration). Deliberately unscoped (bypasses owned-or-shared). `/me` exposes `is_workspace_admin`.
- [x] ✅ **Admin control centre page** (`/admin`) — **shipped early in Week 4 (#289)**. Suites / Users / Access tabs on the new endpoint, with a **real 403** `Forbidden` page for non-admins (server-driven via `/me`'s `is_workspace_admin` — *not* a client "View as" toggle), admin-only nav, shared `MeProvider`/`useMe`. _(Week-6 reconciles this layout to the prototype — see that week's Admin task.)_
- [x] ✅ **Dashboard KPI honesty pass** — **verified already satisfied** (built honestly in the Week-6 dashboard, #333): the Dashboard renders only backed metrics (health score, pass-rate, run counts, `metric_value` trends); the prototype's "Total Anomalies" + "Avg. Time to Resolution" tiles and RecentRuns "Anomalies" column are omitted with rationale comments. No code change needed.
- [x] ✅ **Visual-fidelity pass to the design spec** — [#459](https://github.com/TheurgicDuke771/DataQ/pull/459): self-hosted **Inter** + **JetBrains Mono** via `@fontsource` (latin subset, `fontFamilyCode` wired), the 2px indigo `:focus-visible` ring (scoped so it doesn't double antd's), semantic `--dq-*` CSS tokens (dark-mode-ready), and formalized motion tokens (120/160ms ease + 0.5px press nudge). Empty/loading states were audited and already comprehensive (Spin/Alert/Empty across Connections/Suites/Results).

### Out-of-roadmap Week-7 work landed (not in the original 100-task roadmap)
> Tracked here for honesty — alerting/redaction/results hardening on the deployed app, plus ADR 0012 post-v1 monitor-kinds pulled forward. None of these count toward the v1 roadmap denominator above.
- [x] ✅ **Slack + email alert publishers** behind the `ResultPublisher` seam (extends Week-6 Teams-only alerting) — [#413](https://github.com/TheurgicDuke771/DataQ/pull/413)
- [x] ✅ **Always-alert operationally-failed runs** regardless of severity threshold — [#419](https://github.com/TheurgicDuke771/DataQ/pull/419) (closes #383)
- [x] ✅ **Alerting upsert first-write race fix** in `upsert_config` — [#420](https://github.com/TheurgicDuke771/DataQ/pull/420) (closes #384)
- [x] ✅ **Column-aware failing-sample redaction** — surfaces non-PII values while keeping PII redacted (extends #365) — [#417](https://github.com/TheurgicDuke771/DataQ/pull/417)
- [x] ✅ **Per-run check outcome in the runs table** — [#425](https://github.com/TheurgicDuke771/DataQ/pull/425) (closes #423)
- [x] ✅ **mypy gate on `backend/tests`** + 78 pre-existing type-error fixes — [#418](https://github.com/TheurgicDuke771/DataQ/pull/418)
- [x] ✅ **Freshness & volume monitor-kinds end-to-end** (ADR [0012](adr/0012-monitor-kind-seam.md) post-v1 Theme A, pulled forward) — run engine [#426](https://github.com/TheurgicDuke771/DataQ/pull/426) + authoring path & check-editor UI [#437](https://github.com/TheurgicDuke771/DataQ/pull/437) (SQL-datasource-gated; freshness requires a positive age threshold — silent-green guard; rides the existing `check.kind`/`metric_value` columns, no migration). Schema-drift/anomaly/comparison still reserved (422).
- [x] ✅ **Post-v1 design docs** — admin/access/UI notes ([#422](https://github.com/TheurgicDuke771/DataQ/pull/422)), DQ-intelligence layer ([#430](https://github.com/TheurgicDuke771/DataQ/pull/430)), compliance posture GDPR/CCPA/HIPAA ([#436](https://github.com/TheurgicDuke771/DataQ/pull/436), files #431–#435 backlog)

**Week 7 total: 41 ✅ / 41 — COMPLETE (2026-07-03)** _(the ledger grew from the roadmap's 40 to 41 rows as post-deploy work was folded in. **Done:** cloud deploy ✅ ×3 (#394) · CI/CD pipeline ✅ (#403) · redeploy ✅ `:v10` (#438) · post-deploy hardening ✅ ×2 (#407/#408) · real-vault validated in prod (KV reads live, #406/#408) · history/audit ADR ✅ (#310) · workspace-admin endpoint + Admin page ✅ (#289) · **FastMCP server ✅ ×11** (#460) · **hardening/docs ✅ ×4** — error-shape audit + Swagger completeness (#465), README/deploy-guide/env-ref (#468), prod-docs gate (#464), MCP config doc (#460) · **consistency hardening ✅ ×3** — trigger-dedup (#456) + stuck-run reaper (#458) + minor-robustness/409 (#470) · **prototype polish ✅ ×2** — KPI honesty (verified) + visual-fidelity (#459). **+ 2026-07-01 close-out batch ✅:** **App Insights request/task spans** (#525 — OTel-neutral core + Azure exporter-only, module-scope FastAPI + producer/consumer Celery instrumentation, secret/PII-safe span attributes; opencensus→OTel log migration → #524) · **vault lazy-import coverage** (#523 — `secrets.py` 100%, A4 tail of #169) · **#17 MCP polish** (#522, closes #17) · **E2E expansion** — suite lifecycle panels (#526: schedules/triggers/notifications + seeded ADF pipelines) + detail surfaces (#527: run-detail redacted sample, dashboard content, custom-SQL/freshness/volume check-editor variants, admin/settings; 25 specs green, admin allowlisted in CI) · **opt-in live-smoke lane** (#531 — `e2e-live/` Playwright project gated on `E2E_LIVE_BASE_URL` + captured-OIDC session, `e2e_smoke.py` `DATAQ_BEARER` support, runbook checklist; never in CI) · **docs enrichment** (#528 — notifications/scheduling/best-practices/feature-matrix pages + deepened guides; dry-run beyond Snowflake filed as #532) · **MCP tool-expansion candidates** (#530, Theme 13 + issue #529). **+ 2026-07-02 external close-out ✅:** live run paths verified via the #531 lane (Flows A/B/C green) · Azure Monitor→ADF webhook ×5 (#492 — Action Group + metric alert on the harness factory, deliberate-failure smoke 4m14s; pre-prod pair N/A, single-env v1) · team onboarding (discharged via the six cross-shared demo users, ADR 0027 tiers) · KV purge-protection decided **left off** (recorded in `deploy/README.md`). **+ 2026-07-03 final close ✅:** MCP client-config E2E — all 8 tools end-to-end via a real VS Code `.vscode/mcp.json` against live `/mcp/` (#550, incl. the new [mcp-setup.md](mcp-setup.md) docs page + trailing-slash guidance). **Settings page moved to Week 6**, ADR 0022. The denominator (41 = 10+5+12+6+4+4 section rows) counts deployment + post-deploy DevOps + the added consistency (4) + prototype-polish (4) groups; the **out-of-roadmap alerting/redaction/results/monitor-kind work (8 items above)** is excluded by design.)_

---

## Week 8 — Unit testing

**Exit gate:** ≥80% coverage gate enforced in CI across backend, API, frontend.

### Backend unit tests (pytest) (12 tasks — 12/12 ✅; audit closed 2026-07-03 — the four sub-80% modules brought to 97–100% in #557)
- [x] ✅ Auth service — token validation, session expiry, Key Vault credential retrieval — `core/test_auth.py` (current-user dep, dev-bypass gating) + `core/test_secrets.py` (KV retrieval — `secrets.py` 100%, #523); token expiry is delegated to the `fastapi-azure-auth` verifier — audit closed ✅ 2026-07-03 (#557)
- [x] ✅ Connection service — CRUD operations, test endpoint logic per datasource type — **all six types covered**: `services/test_connection_service.py` (`connection_service.py` 100%, PR 5) + per-adapter suites `datasources/test_{snowflake,adls,s3,unity_catalog,registry}.py` + `orchestration/test_{adf,airflow}.py` + `api/test_connections.py` — audit closed ✅ 2026-07-03 (#557/#558)
- [x] ✅ Suite service — CRUD, share assignment, export / import serialisation — shipped with W3 (`api/test_suites.py`, `api/test_shares.py`, export/import round-trip; suite/share/io services were 100% at landing) — audit closed ✅ 2026-07-03 (#557/#558)
- [x] ✅ Check service — expectation builder, SQL validator, dry-run logic, threshold tier evaluation — `api/test_checks.py` + `services/test_custom_sql.py` (ADR 0019 guardrails) + dry-run TestClient tests (W3) + `services/test_severity.py` (16 units) + `datasources/test_monitors.py` (freshness/volume, #426) + the **catalog↔GX contract test** (`test_catalog_gx_contract.py`, [#559](https://github.com/TheurgicDuke771/DataQ/pull/559) — closes #205) — audit closed ✅ 2026-07-03 (#557/#558)
- [x] ✅ Column profiler service — null count, distinct count, min/max per datasource type — `services/test_profile_service.py` (64 tests across W3: SQL builders/injection, pandas flat-file, UC catalog-aware; module 85–90%, live seams excluded) — audit closed ✅ 2026-07-03 (#557/#558)
- [x] ✅ Execution service — `run_suite` dispatch + `run_service.execute_run` + GX adapter + NaN sanitizer tested early ([PR 4b.1](https://github.com/TheurgicDuke771/DataQ/pull/77) + [PR 4c-i](https://github.com/TheurgicDuke771/DataQ/pull/78) + [PR 4c-ii](https://github.com/TheurgicDuke771/DataQ/pull/79)); progress polling / cancel / retention purge / scheduling / reaper all shipped **with tests** in Week 5–7 (#301/#302/A7/A8/#458 — `services/test_run_{service,dispatch,target,retention,reaper}.py`, `test_cron.py`) — gate flipped ✅ (#557)
- [x] ✅ Alerting service — Teams webhook dispatch, dedup logic, snooze / suppression — the full `tests/alerting/` battery shipped with W6/W7 (#366–#373, #413, #419/#420): builder/card/dedup/dispatch/registry/routing/suppression/teams/slack-email — audit closed ✅ 2026-07-03 (#557/#558)
- [x] ✅ ADF service — parse + upsert + `fetch_run_detail` + enrichment + trigger-on-success covered (`AdfProvider` parse/fetch unit tests incl. ARM mapping/http-error; `orchestration_service` DB tests incl. replay idempotency, enrichment fail-soft, trigger gating — modules 100%) — PR 7 + PR 8; AlertPing/Common-Alert-Schema path added with #534 — gate flipped ✅ (#557)
- [x] ✅ ADF polling service — succeeded run fetch, skip-if-recently-updated, gap recovery — shipped with tests in W5 (#171 polling beat + #307 gap recovery; `services/test_orchestration_service.py` covers window/skip semantics + immediate-poll #534) — audit closed ✅ 2026-07-03 (#557/#558)
- [x] ✅ Result service — health score calc, historical trend aggregation, export generation — `services/test_dashboard_service.py` (ADR 0005 SQL health score, trends, per-suite perf) + run/read helpers in `test_run_service.py` + check-history endpoint tests (#333); export is client-side CSV/JSON (tested in vitest) — audit closed ✅ 2026-07-03 (#557/#558)
- [x] ✅ MCP service — each of 8 tools returns correct shape; auth rejection; `trigger_suite_run` returns valid run_id — landed with [#460](https://github.com/TheurgicDuke771/DataQ/pull/460): `tests/mcp/` 20 tests (tool shape ×8, per-suite authz, both auth modes, fail-closed unmounted)
- [x] ✅ **Secret service** — 12 tests → lazy-import branch closed in [#523](https://github.com/TheurgicDuke771/DataQ/pull/523): `secrets.py` **100%** — [PR 3b](https://github.com/TheurgicDuke771/DataQ/pull/56) + #523

### API layer tests (pytest + httpx) (6 tasks — 6/6 ✅; audit closed 2026-07-03 — the API layer rides the #557 backend gate; +#556 CORS activation)
- [x] ✅ Auth endpoints — unauthorised → 401 asserted across every router's TestClient suite + `api/test_me.py` (identity, `is_workspace_admin`) + `api/test_users.py`; there is no backend login/redirect endpoint (SPA-side OIDC + JWT validation) — audit closed ✅ 2026-07-03 (#557/#558)
- [x] ✅ **Probe endpoints** (out-of-roadmap) — POST creates+dispatches, idempotent seed, GET results, 404 — against real Postgres — [PR 4c-ii](https://github.com/TheurgicDuke771/DataQ/pull/79)
- [x] ✅ Connection endpoints — CRUD happy paths + validation errors — **all six types covered** in `api/test_connections.py` (CRUD, 422/404/502/409, secret-never-leaks, auth gate, reauth) — [PR 5](https://github.com/TheurgicDuke771/DataQ/pull/85) + the W2 adapter PRs — audit closed ✅ 2026-07-03 (#557/#558)
- [x] ✅ Suite & check endpoints — CRUD, share, export / import, dry-run — shipped with W3: `api/test_suites.py` + `api/test_checks.py` + `api/test_shares.py` (~60 TestClient tests incl. the authz matrix, export/import round-trip, dry-run) — audit closed ✅ 2026-07-03 (#557/#558)
- [x] ✅ Execution endpoints — trigger run, poll progress, cancel, list history — shipped with W5: `api/test_runs.py` (trigger/422/403/404/503, progress, cancel incl. 409, list scoping + filters) + `api/test_schedules.py` + `api/test_trigger_bindings.py` — audit closed ✅ 2026-07-03 (#557/#558)
- [x] ✅ Results endpoints — dashboard data, drill-down, filters, download — `api/test_dashboard.py` (summary, windows, scoping) + the `test_runs.py` read surface + check-history tests (#333) + `api/test_notifications.py`; download is client-side CSV/JSON — audit closed ✅ 2026-07-03 (#557/#558)
- [x] ✅ ADF webhook endpoint — valid payload → 200 (recorded/ignored), missing+wrong token → 401, malformed/non-JSON → 422, secret-unconfigured → 503 (9 TestClient tests) — PR 7 (+ `api/test_orchestration_events_airflow.py` HMAC path, AlertPing tests #534); duplicate-runId idempotency asserted at the service layer — audit closed ✅ 2026-07-03 (#557/#558)

### Frontend unit tests (Vitest + RTL) (8 tasks — 8/8 ✅; audit closed 2026-07-03 — all-src coverage + 22 new tests in #558; 53 test files total)
- [x] ✅ **AuthGate** — 4 tests (dev_bypass renders children, unconfigured banner, real+unauth sign-in button, real+auth renders children) — [PR 3c](https://github.com/TheurgicDuke771/DataQ/pull/63)
- [x] ✅ **API client interceptor** — 3 tests (no-token in dev, Bearer in real-with-account, no-token in real-without-account) — [PR 3c](https://github.com/TheurgicDuke771/DataQ/pull/63); re-validated after the OIDC swap (`tests/auth/authClient.test.ts` + `config.test.ts`, ADR 0028 #509)
- [x] ✅ Login screen — no separate login screen exists (sign-in lives on `AuthGate`, covered above); the OIDC redirect/callback client is covered by `authClient.test.ts` — audited ✅ 2026-07-03: nothing remained (authClient/AuthGate cover it; providers added in #558)
- [x] ✅ Connection manager — card per type, status badge colours, re-auth flow — `tests/pages/Connections.test.tsx` + `ConnectionNew/ConnectionEdit` + `components/ReauthModal.test.tsx` — audit closed ✅ 2026-07-03 (#557/#558)
- [x] ✅ Check editor — form fields per check type, profiler loads, dry-run, severity tier toggle — `tests/pages/CheckNew/CheckEdit/CheckNewMonitor` + `components/ColumnProfilePanel/DryRunPreview/expectationCatalog/customSql` — audit closed ✅ 2026-07-03 (#557/#558)
- [x] ✅ Suite sharing panel — add/remove user, role assignment — `components/SharePanel.test.tsx` (6 tests: list-by-name, empty, search→grant with owner/existing filtered, change, revoke, read-only non-admin) — landed with PR-E3
- [x] ✅ Execution page — run now, progress bar updates, cancel button — `components/RunNowPanel.test.tsx` (3) + `LiveRunProgress.test.tsx` (7: poll-until-terminal, cancel happy/hidden) + `SchedulesPanel.test.tsx` (6) — landed with W5
- [x] ✅ Results dashboard — stat cards, filters, ADF panel, severity badges — `tests/pages/Dashboard/Results/RunDetail` + `components/RecentRuns/SuitePerformance/QualityTrends/resultsFormat` — audit closed ✅ 2026-07-03 (#557/#558)

### Test infrastructure (3 tasks — 3/3 ✅ — the gate, the week's real deliverable, is live)
- [x] ✅ Pytest fixtures — transactional Postgres `db_session` fixture + CI postgres service + fake `CheckRunner`/session landed ([PR 4b.1](https://github.com/TheurgicDuke771/DataQ/pull/77) + [PR 4c-ii](https://github.com/TheurgicDuke771/DataQ/pull/79)); "mock GX context" superseded — GX runs **in-process on real DataFrames** in the W3 runner tests (stronger than a mock); webhook mocking covered by the orchestration-events TestClient suites + the adversarial harness (`tests/support/adversarial.py`)
- [x] ✅ CI gate — PRs blocked if coverage drops below 80% — **LIVE 2026-07-03**: backend `--cov-fail-under=80` in `pyproject.toml` (every pytest run, local + CI — [#557](https://github.com/TheurgicDuke771/DataQ/pull/557), 98.3% at flip) + frontend `coverage.thresholds.lines: 80` over **all of `src/`** (`include: ['src']`, only the unmountable `main.tsx` waived) with CI running `pnpm test:coverage` ([#558](https://github.com/TheurgicDuke771/DataQ/pull/558), 87.8%). The frontend number previously counted only imported files — 7 src files were invisible; #558 made the gate honest before flipping it
- [x] ✅ Test data fixtures — sample suites, check results, run histories — `backend/scripts/demo_data.py` seeds a representative dataset (all six connection types + suites with varied checks + a cross-user share **+ runs/results/pipeline-runs since PR-C1, + seeded ADF pipelines for the triggers panel #526**) through the real service layer, wired into `seed_dev`. Paired with `backend/scripts/e2e_smoke.py` (full-stack API E2E — see below)

**Week 8 total: 29 ✅ / 29** _(closed 2026-07-03 via #556–#560. Most rows were substantially covered by feature-PR tests across W1–W7; the W8 audit confirmed each module against the ≥80% bar, closed the four genuine gaps (#557), made frontend coverage honest over all of `src/` (#558), and flipped both gates. Coverage at close: **98.3% backend / 87.8% frontend lines**, 1273 + 334 tests on merged main. The `Probe endpoints` API test is out-of-roadmap and excluded from the 29.)_

> **Full-stack E2E ([#128](https://github.com/TheurgicDuke771/DataQ/issues/128) — both halves landed):**
> - **API half** — `backend/scripts/e2e_smoke.py` drives the **real** running stack (docker compose + dev-bypass + the demo seed) with no mocks: asserts the six connection types list (secrets never returned), suites + checks read back, severity thresholds round-trip, the create→add-check→dry-run→delete authoring loop, and dry-run failing soft (502/422, not a crash). **12/12 pass.**
> - **Browser half** — `frontend/e2e/` (Playwright) drives the **React UI** the same way a user does (`browser → Vite proxy → api → Postgres`): app-shell loads under dev-bypass, sider nav, seeded connections grouped by type + "Test all" health path, seeded suite → its checks, and a full create-suite → add → delete authoring round-trip; **expanded to 25 specs in W7** (#526/#527: schedules/triggers/notifications panels, run-detail redacted sample, dashboard, check-editor variants, admin/settings). Wired into CI as the `frontend-e2e` job (Postgres + Redis services, migrate + seed, uvicorn on :8000, Playwright starts its own Vite server). See [frontend/e2e/README.md](../frontend/e2e/README.md).
>
> Live datasource `test()`/runs — the long-deferred smoke — **discharged 2026-07-02** via the opt-in live lane (#531): Flows A/B/C green against prod + the harness, browser lane 3/3, `e2e_smoke.py` bearer-mode 12/12.

---

## Aggregate

| Week | Done | In progress | Pending | Total |
|---|---|---|---|---|
| Week 1 | 10 | 0 | 0 | 10 |
| Week 2 | 17 | 0 | 2 | 19 |
| Week 3 | 18 | 0 | 0 | 18 |
| Week 4 | 26 | 0 | 0 | 26 |
| Week 5 | 18 | 0 | 0 | 18 |
| Week 6 | 28 | 0 | 0 | 28 |
| Week 7 | 41 | 0 | 0 | 41 |
| Week 8 | 29 | 0 | 0 | 29 |
| **TOTAL** | **187** | **0** | **2** | **189** |

> Week 2's 2 "pending" are the 🔵 scope-changed browse/list pickers (deferred post-v1, [#466](https://github.com/TheurgicDuke771/DataQ/issues/466)) — the only rows not ✅. Week 8 closed 29/29 on 2026-07-03. _(2026-07-03 re-tally: W1 10/10 and W2 17+2🔵 after the W7 close-outs retired their deferred tails; W7 recounted at 41 rows — see the W7 total note; W8's 🟡 rows re-based on the tests that shipped with feature PRs.)_

> Post-v1 items (marketing landing page, dark mode, and all deferred design themes) live in **[context/post-v1-roadmap.md](../context/post-v1-roadmap.md)** and are **excluded** from this weekly aggregate.

> 188 > 100 because ADR 0004 added Airflow tasks, ADR 0011 added two seam tasks (generic runner dispatch, `ResultPublisher`), ADR 0012 added three Week-3 monitor-kind / metric seam tasks, the W5 run-enablement gaps surfaced in review (check target-table #215, Suite Triggers UI #216), the GX-Cloud-style UI redesign added four UI-shape tasks (dedicated/classified connection + check pages), the **DataQ Design System prototype adoption** added fourteen tasks (ten Week-6 — the full screen set per ADR 0022, up from six once Profile content, the New-Connection source-select split, and the Admin layout-reconcile were counted and Settings was pulled in from Week 7; plus four Week-7 backend-gated/polish, of which two shipped early via #289), plus PR-review follow-ups not in the original roadmap. Tracked here for honesty. _(The Week-5 recent-runs audit table was **moved to Week 6** — net-neutral: W5 19→18, W6 22→23. Then ADR 0022's full-screen-set adoption took W6 23→27 — three genuinely new tasks (Profile, source-select split, Admin reconcile) + Settings moved in from W7 (W7 34→33); net +3 on the grand total.)_ _(**2026-06-29 full recount** — every week re-tallied from the actual section checkboxes (one task = one top-level checkbox; exclude fenced/tagged out-of-roadmap items and the W5→W6 "moved" placeholder; Done is **✅-only**, `[x] 🟡` partials count as in-progress). W1–W5 already matched. Adjustments: **W6 27→28** (the datasource-type filter is a distinct results-dashboard task the prior breakdown folded), **W7 33→40** (deployment + post-deploy DevOps + the added consistency/prototype-polish groups counted; the 8 fenced out-of-roadmap items excluded), **W8 30→29** (out-of-roadmap `Probe endpoints` test excluded). Grand total **181/183 → 188**; the older "23→27 / net +3" trace above is superseded by this recount.)_

---


## Active infrastructure issues

Issues that aren't roadmap tasks but block / risk the work.

| # | Title | Status | Will affect |
|---|---|---|---|
| ~~[#42](https://github.com/TheurgicDuke771/DataQ/issues/42)~~ | ~~Add FK indexes on join columns (backward-compat migration)~~ | **Closed** ([PR #70](https://github.com/TheurgicDuke771/DataQ/pull/70)) | n/a |
| ~~[#43](https://github.com/TheurgicDuke771/DataQ/issues/43)~~ | ~~Silence CodeQL false positives (Alembic + Protocol stubs)~~ | **Closed** ([PR #69](https://github.com/TheurgicDuke771/DataQ/pull/69)) | n/a |
| ~~[#50](https://github.com/TheurgicDuke771/DataQ/issues/50)~~ | ~~Bridge uvicorn access logs through structlog~~ | **Closed** ([PR #71](https://github.com/TheurgicDuke771/DataQ/pull/71)) | n/a |
| ~~[#51](https://github.com/TheurgicDuke771/DataQ/issues/51)~~ | ~~Emit per-request structured log from request_id middleware~~ | **Closed** ([PR #71](https://github.com/TheurgicDuke771/DataQ/pull/71)) | n/a |
| ~~[#54](https://github.com/TheurgicDuke771/DataQ/issues/54)~~ | ~~Consolidate mypy / type-check dep lists (3-file drift)~~ | **Closed** ([PR #68](https://github.com/TheurgicDuke771/DataQ/pull/68)) | n/a |
| ~~[#62](https://github.com/TheurgicDuke771/DataQ/issues/62)~~ | ~~MSAL redirect lifecycle (real-AAD smoke test deferred)~~ | **Closed** (completed 2026-05-28) | n/a |
| ~~[#65](https://github.com/TheurgicDuke771/DataQ/issues/65)~~ | ~~Vite 8 coordinated bump (vite + plugin-react + vitest)~~ | **Closed** ([PR #119](https://github.com/TheurgicDuke771/DataQ/pull/119)) | n/a — superseded Dependabot #111 |
| ~~[#92](https://github.com/TheurgicDuke771/DataQ/issues/92)~~ | ~~Surface the ADF webhook URL instead of hand-assembling a secret-bearing URL~~ | **Closed** (in-app webhook URL surface, #493/#496) | n/a |
| ~~[#129](https://github.com/TheurgicDuke771/DataQ/issues/129)~~ | ~~Snowflake connector 3→4 + Python 3.11→3.13 CVE refresh~~ | **Closed** ([#178](https://github.com/TheurgicDuke771/DataQ/pull/178), ADR 0017) | n/a — cleared 5 CVEs |
| ~~[#146](https://github.com/TheurgicDuke771/DataQ/issues/146)~~ | ~~Worker hardcodes `build_snowflake_runner` — dispatch `CheckRunner` by `connection.type`~~ | **Closed** (#165 + profiler registry) | n/a — runner + profiler registries |
| ~~[#201](https://github.com/TheurgicDuke771/DataQ/issues/201)~~ | ~~Undeclared direct dependency: pyarrow (Parquet flat-file relied on a transitive)~~ | **Closed** ([PR #202](https://github.com/TheurgicDuke771/DataQ/pull/202)) | n/a — unblocked databricks 4.x bump |
| ~~[#213](https://github.com/TheurgicDuke771/DataQ/issues/213)~~ | ~~compose frontend service never receives `VITE_*` vars → dev-bypass unreachable in the browser~~ | **Closed** (this PR) | n/a — `docker compose up` now reaches the UI under dev-bypass |
| ~~[#72](https://github.com/TheurgicDuke771/DataQ/issues/72)~~ | ~~ADR 0004 follow-up: document `trigger_bindings` one-orchestrator-per-(provider, env) assumption~~ | **Closed** ([PR #83](https://github.com/TheurgicDuke771/DataQ/pull/83)) | n/a — guard enforced in PR 6 ADF CRUD |
| ~~[#75](https://github.com/TheurgicDuke771/DataQ/issues/75)~~ | ~~Integration-assert request_id propagates FastAPI→Celery worker logs~~ | **Closed** ([PR 4c-ii](https://github.com/TheurgicDuke771/DataQ/pull/79)) | n/a |
| ~~[#86](https://github.com/TheurgicDuke771/DataQ/issues/86)~~ | ~~`EnvSecretStore.set` is per-process — Celery worker can't resolve API-written secrets (dev only)~~ | **Closed** ([PR #95](https://github.com/TheurgicDuke771/DataQ/pull/95)) | n/a — Redis-backed dev secret store |
| ~~[#87](https://github.com/TheurgicDuke771/DataQ/issues/87)~~ | ~~Map `SecretWriteError` → 502 in connection create/update (currently 500)~~ | **Closed** ([PR #94](https://github.com/TheurgicDuke771/DataQ/pull/94)) | n/a |

**Deferred polish** (Week-1 governance era): #8, #10, #12, #17, #18, #19, #20 — **all closed** (the last, #17, by #522; verified against GitHub 2026-07-03).

**Follow-up (closed):** the real-Snowflake DEV live-run smoke for `SnowflakeCheckRunner.run_checks` was **discharged 2026-07-02** — Flow A green ×3 against live Snowflake via the #531 lane.

---

## Out-of-roadmap work landed (not in the original 100-task roadmap)

> The roadmap denominator (188) already absorbs the ADR-driven scope additions (see the **Aggregate** note above). This appendix is the human-readable ledger of work that shipped **beyond** the original 100 tasks — captured for honesty. Per-week detail lives in each week's notes; this is the consolidated index.

| Category | What landed | Refs |
|---|---|---|
| **Governance & tooling** | flat-monorepo lock, Entire CLI hooks + allowlist, Dependabot triage (10 bumps + pyarrow direct-dep), CI promoted to an enforced 12-check merge gate, **mypy gate extended over `backend/tests`** (+78 type-error fixes) | #1–#24, #202/#201, #418 |
| **Runtime / deps** | Python 3.11→3.13 + Snowflake 3→4 CVE refresh; multi-stage `slim`+pip prod image (dropped conda from the image) | ADR [0017](adr/0017-python-313-runtime-upgrade.md) / #129, ADR [0025](adr/0025-production-image-pip-slim.md) |
| **Architecture seams (design-now, impl-later)** | provider-agnostic infra seams, extensibility seams (`ResultPublisher`, dbt-as-orchestration, generic runner dispatch), monitor-kind seam, reconciliation `comparison` kind, severity-derivation semantics | ADRs [0010](adr/0010-provider-agnostic-infrastructure-seams.md)/[0011](adr/0011-extensibility-seams-for-deferred-integrations.md)/[0012](adr/0012-monitor-kind-seam.md)/[0014](adr/0014-reconciliation-comparison-check-kind.md)/[0016](adr/0016-severity-derivation-semantics.md) |
| **Custom-SQL checks** | custom-SQL as a GX `UnexpectedRowsExpectation` (read-only single-statement guardrails); Monaco editor | ADR [0019](adr/0019-custom-sql-check-kind.md), #258/#259 |
| **History/audit** | per-entity Type-4 snapshot tables; first impl `connection_versions` + the check version-history drawer | ADR [0020](adr/0020-history-and-audit-strategy.md), #280/#281/#282, #310 |
| **Admin control centre** | workspace-admin authz primitive + `/admin/{suites,users,access}` + `/admin` page & 403 (pulled W7 prototype-admin tasks into W4) | #289 |
| **DataQ Design System prototype adoption** | full 13-screen page restructure (drawers→pages), Enhanced Monitoring Dashboard, Profile/Settings/Admin reconcile, error pages, recharts | ADR [0022](adr/0022-week6-prototype-adoption-and-chart-library.md), #333/#350/#374/#375/#376/#377 |
| **Alerting expansion** (W6→W7) | `ResultPublisher` seam + Teams adaptive card; severity routing, dedup, snooze, per-suite config + UI; **Slack + email publishers**; always-alert failed runs; upsert race fix | #366–#373/#378, #413, #419, #420 |
| **Redaction depth** | redacted sample failing rows (closes #226); **column-aware redaction** (surface non-PII, keep PII masked) | #365, #417 |
| **Results depth** | per-run check outcome in the runs table | #425 |
| **Monitor kinds — pulled forward** | **freshness + volume monitor-kinds end-to-end** (ADR 0012 post-v1 Theme A): run engine + authoring path + check-editor UI; SQL-datasource-gated; no migration | #426, #437 |
| **Cloud deploy & IaC** | GHCR registry (over ACR), in-repo Terraform app stack (ACA + SWA + shared env/Postgres), AAD SSO app regs, deploy workflow; post-deploy fixes (#393/#405/#406/#395); image `:v3`→`:v10` | ADRs [0023](adr/0023-container-image-registry-ghcr.md)/[0024](adr/0024-app-deployment-infrastructure.md), #379/#394/#398/#399/#403/#407/#408/#409/#414/#421/#438 |
| **Live test/demo harness** | external, not git-tracked — Terraform + retail mock-data + UC medallion SQL transforms (SQL-from-DAG, incl. the AI feedback-sentiment stage), live end-to-end (ADR 0021); the deferred live-smoke it existed for was **discharged 2026-07-02** (Flows A/B/C green) | ADR [0021](adr/0021-demo-test-data-environment-strategy.md) |
| **Post-v1 design docs** | admin/access/UI, DQ-intelligence, compliance posture — consolidated under [post-v1-roadmap.md](../context/post-v1-roadmap.md) | #422, #430, #436 |

---

## Open issues register (54 open, verified against GitHub 2026-07-04)

> One current-cycle issue remains (below). The **53 `Backlog (post-v1 / testing)`** issues are post-v1 and live, mapped by theme, in **[context/post-v1-roadmap.md](../context/post-v1-roadmap.md)** — not duplicated here ([#563](https://github.com/TheurgicDuke771/DataQ/issues/563), the mutation-spike survivors triage, moved Week 8 → Backlog on 2026-07-03).

**Milestone breakdown:** Week 8 → 1 · Backlog (post-v1) → 53. Closed 2026-07-03: [#385](https://github.com/TheurgicDuke771/DataQ/issues/385) (CORS activation tests, #556) · [#205](https://github.com/TheurgicDuke771/DataQ/issues/205) (catalog↔GX contract test, #559) · [#352](https://github.com/TheurgicDuke771/DataQ/issues/352) (dashboard KPI enrichment, #560) · [#128](https://github.com/TheurgicDuke771/DataQ/issues/128) (full-stack E2E — API + browser halves landed W5–W7; the coverage gates #557/#558 were the remaining half, closed with this close-out) · [#371](https://github.com/TheurgicDuke771/DataQ/issues/371)/[#567](https://github.com/TheurgicDuke771/DataQ/issues/567) (NUL-byte 500, the qa-verifier go-live blocker, by #570) · [#569](https://github.com/TheurgicDuke771/DataQ/issues/569) (deferred QA probes, discharged live same session, closed 2026-07-04). New Backlog filings from the qa-verifier pass: [#568](https://github.com/TheurgicDuke771/DataQ/issues/568), [#571](https://github.com/TheurgicDuke771/DataQ/issues/571), [#573](https://github.com/TheurgicDuke771/DataQ/issues/573). W7's eleven closed with its milestone (epic #176, #17, #92, #168, #169, #170, #288, #309, #382, #396, #397).

### Week 8 — Unit testing & coverage (≥80%) (1 open)
| # | Title |
|---|---|
| [#177](https://github.com/TheurgicDuke771/DataQ/issues/177) | Week 8 tracking epic — closes with the go-live checklist (remaining: ops timers · v1.0.0 tag + release + retro; #553/#562 ✅ · spike #563/#564 ✅ · deploy + smoke ✅ · decisions recorded ✅ · qa-verifier go-live blocker #567 fixed by #570 ✅) |

---

## Pending design decisions (must land before the week they affect)

| Decision | Affects | Deadline |
|---|---|---|
| ~~Severity tier weights (warn / fail / critical → health score)~~ | Week 3 Day 1 schema migration | ✅ Resolved — [ADR 0005](adr/0005-severity-tier-weights.md) (0.5 / 1.0 / 2.0; SQL-normalised health score) |
| ~~Monitor-kind seam (`check.kind` discriminator + numeric `metric_value` / `duration_ms`)~~ — ADR 0012 | Week 3 schema migration (rides the threshold migration) | ✅ Resolved — [ADR 0012](adr/0012-monitor-kind-seam.md) (`expectation` only in v1; rest reserved) |
| ~~ADF webhook auth (shared secret + rotation)~~ | Week 2 webhook receiver | ✅ Resolved — [ADR 0006](adr/0006-adf-webhook-authentication.md) (secret in URL, hard cutover, no v1 replay check) |
| ~~Airflow callback signing key (HMAC)~~ | Week 2 webhook receiver | ✅ Resolved — [ADR 0007](adr/0007-airflow-callback-model.md) (HMAC-SHA256 header + polling fallback) |
| ~~Azure tenant + app registration values~~ | Week 7 deployment | ✅ Resolved — the API + SPA app registrations are created by the in-repo Terraform (ADR 0024) and wired end-to-end; validated live with real Entra SSO |

---

## Post-v1 / deferred work

Everything deliberately deferred past v1 — design themes (admin/access, DQ-intelligence, compliance, the remaining monitor kinds, alerting depth, consistency, perf/scale, datasource depth, results/reporting, test-hardening, prototype deferrals) **and** the full `Backlog (post-v1 / testing)` GitHub-milestone issue list — lives in one place:

➡️ **[context/post-v1-roadmap.md](../context/post-v1-roadmap.md)**

It sits in `context/` (next to the v1 roadmap) because it's the input for the post-v1 week-wise task generator. Detailed design for the big themes stays in [docs/post-v1-admin-ui-notes.md](post-v1-admin-ui-notes.md), [docs/post-v1-dq-intelligence-notes.md](post-v1-dq-intelligence-notes.md), and [docs/compliance-posture.md](compliance-posture.md). **Don't re-create per-theme post-v1 sections in this file** — add to the roadmap doc instead.

---

## How to update this file

When merging a PR:

1. Find the task(s) it implements in the relevant week.
2. Flip `⬜` → `✅` (or `⬜` → `🟡` if partial).
3. Append the PR link: `— [PR #N](https://github.com/.../pull/N)`.
4. Update the per-week subtotal at the bottom of the week.
5. Update the **Snapshot** table at the top (task count, open PRs/issues) and the **Aggregate** table.
6. If the PR added an out-of-roadmap task (e.g. ADR-driven scope change), add a row with the note.

PR-template checkbox enforces this. If the change is purely tooling / docs that doesn't map to a roadmap task, tick the "N/A" checkbox.
