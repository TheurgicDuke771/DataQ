# DataQ — Progress tracker (v1.2 cycle)

> The **live task tracker**, active since `v1.1.0` (re-tagged at the true cycle close,
> 2026-08-21). The completed cycle ledgers are archived frozen at
> [progress-v1.md](progress-v1.md) (v1, Weeks 1–8) and
> [progress-v1.1.md](progress-v1.1.md) (v1.1, Weeks 1–7 + the W7 stretch); companions:
> [retro-v1.md](retro-v1.md), [retro-v1.1.md](retro-v1.1.md).
> **Updated at the end of every PR** — the PR template has a checkbox to enforce.
> Source of truth for "what's done vs. what's left" in the current cycle. CLAUDE.md §13
> carries only the headline.

## Status legend

| Symbol | Meaning |
|---|---|
| ✅ | Done — PR merged to `main` |
| 🟡 | In progress — open PR or partially shipped |
| ⬜ | Not started |
| 🔵 | Deferred / scope-changed (with note) |

---

## Snapshot

| | |
|---|---|
| **v1 baseline** | `v1.0.0` tagged 2026-07-04 — 187/189 roadmap tasks; deployed to Azure Container Apps; ledger at [progress-v1.md](progress-v1.md) |
| **v1.1 baseline** | `v1.1.0` — cycle ran 2026-07-04 → 2026-08-21 (6 weeks + the W7 stretch; first tagged 2026-08-15 at `b8d8278b`, then **re-tagged 2026-08-21 at the true close** — the stretch added 121 commits / 126 merged PRs / 55 closed issues, incl. RBAC ADR 0033, the MCP 8→46 expansion + honesty pass, the security-surface audit, and the compliance G1–G6 track). Ledger at [progress-v1.1.md](progress-v1.1.md); retro at [retro-v1.1.md](retro-v1.1.md) |
| **Current cycle** | **v1.2 — 8 weeks, 2026-08-22 → 2026-10-16** (planned 2026-08-21 at the v1.1 close; epic [#1518](https://github.com/TheurgicDuke771/DataQ/issues/1518)). The arc: **W1** clears the user's infra decision gates (#590/#588) + the MCP honesty follow-ups; **W2–W4** build the DQ-intelligence track from [docs/post-v1-dq-intelligence-notes.md](post-v1-dq-intelligence-notes.md) (catalog expansion → server-side allowlist → `LLMProvider` seam → SQL-gen → suggestions) alongside compliance G2; **W5** platform-native engines (Snowflake DMF, ADR 0036) + notification channels; **W6** perf/scale hardening; **W7** feature burn-down; **W8** spikes/decisions + **cycle close** (the closing week is deliberately last). See [Cycle plan](#cycle-plan--v12-8-weeks-2026-08-22--2026-10-16) below. |
| **Open issues** | **108** open repo-wide (2026-09-03 evening — #1845 (LLM in-app UI) and #1846 (live-warehouse LLM captures) filed from the AI-features docs work; arithmetic: 106 + 2 = 108). Prior: **106** open repo-wide (2026-09-03 — #1843 filed and closed by #1844 the same session, net 0). Prior: **106** open repo-wide (2026-09-02 evening, after the deploy — [#1826](https://github.com/TheurgicDuke771/DataQ/issues/1826) filed from the post-deploy worker-log triage; arithmetic: 105 + 1 = 106). Prior: **105** open repo-wide (2026-09-02, batch 2 — seven closed: #1806/#1792/#1803/#1809/#1814/#1804/#1731; two filed: #1819 (durations rendered four ways in `sanitize_json`, from #1818's review) and #1824; arithmetic: 110 − 7 + 2 = 105). Prior: **110** open repo-wide (2026-09-02 after the burn-down merges — ten closed: #1729/#1721/#1732/#1793/#1787/#1717/#1790/#1795/#1728/#1736; arithmetic: 120 − 10 = 110. Prior: **120** open repo-wide (2026-09-02, during the backlog burn-down — nothing merged yet, so no closures; six filed from the twelve `/code-review` passes: #1803/#1804 (jsonsafe follow-ups), #1806 (ER-diagram drift), #1809 (backfill uses current not historical check context), #1811 (worker memory headroom at concurrency 4), #1814 (marker-concat expression index); arithmetic: 114 + 6 = 120. Prior: **114** open repo-wide (2026-09-01, after [#1795](https://github.com/TheurgicDuke771/DataQ/issues/1795) was filed — data-subject-rights export/erase never reach `incidents.evidence`, a follow-on from the #1772 root-cause work; arithmetic: 113 + 1 filed = 114. Prior: **113**, after #1772 closed via [PR #1791](https://github.com/TheurgicDuke771/DataQ/pull/1791) and #1777 closed via [PR #1789](https://github.com/TheurgicDuke771/DataQ/pull/1789) — the last two of the four review-driven residuals from the #1780/#1786 tracking chain, completing user-directed order #1773→#1781→#1777→#1772. **All nine W4 review-driven follow-ups now closed** (#1724/#1725/#1726/#1727/#1730/#1773/#1777/#1781/#1772); #1791's four review passes filed [#1792](https://github.com/TheurgicDuke771/DataQ/issues/1792) (N+1 query, bounded) and [#1793](https://github.com/TheurgicDuke771/DataQ/issues/1793) (P1-adjacent — a pre-existing fail-open redaction asymmetry for scalar values, shared by every results surface) rather than expand the security PR further; #1789's review filed [#1790](https://github.com/TheurgicDuke771/DataQ/issues/1790) (unset worker `--concurrency`). Count arithmetic: 112 (prior) − 2 (closed) + 3 (#1790/#1792/#1793 filed) = 113. Prior: **112**, after #1781 closed via [PR #1786](https://github.com/TheurgicDuke771/DataQ/pull/1786) — `execute_invocation` now persists `DataQError.detail`; a 5-agent review pass found and fixed a size cap gap, a missing truncation signal, RCA's own missing total-clause cap, and sqlgen's dropped detail, filing [#1787](https://github.com/TheurgicDuke771/DataQ/issues/1787) for a separate pre-existing gap in `check_service.validate_monitor_check`. Continuing user-directed order: #1777 (dedicated Celery queue, next), #1772 (P1/security, last). Prior: **112**, after #1773 closed via [PR #1784](https://github.com/TheurgicDuke771/DataQ/pull/1784) — the admin LLM test-probe unaudited-call gap, fixed across two review rounds with no new follow-ups filed. User then directed work to continue on the remaining review-driven residuals: #1781 (next), #1777, #1772 (P1/security, last — biggest/most sensitive). Prior: **113**, after #1727 closed via [PR #1780](https://github.com/TheurgicDuke771/DataQ/pull/1780) and #1730 closed via [PR #1782](https://github.com/TheurgicDuke771/DataQ/pull/1782) — the last two of the five review-driven W4 follow-ups (#1724/#1725/#1726/#1727/#1730), **W4 now fully closed**. #1780's three `/code-review` rounds filed one more, [#1781](https://github.com/TheurgicDuke771/DataQ/issues/1781) (`execute_invocation` discards `DataQError.detail`; `llm_rca.py`'s own rejection path has no reason at all — architectural, spans a shared mechanism + a sibling file, deliberately not folded into #1780's narrower fix), `v1.2 Backlog`, not started. #1782 (test-only, beat-schedule coverage) got a clean `/code-review` pass, zero findings. Prior: **114**, after #1726 closed via [PR #1778](https://github.com/TheurgicDuke771/DataQ/pull/1778) — reaper threshold mis-calibration (Part A pending false-kill under a shared Celery queue, Part B running false-reap during checksuggest's live warehouse profiling); count held flat because the structurally correct queue-separation fix was deliberately scoped out as [#1777](https://github.com/TheurgicDuke771/DataQ/issues/1777) (deploy-coordination hazard across three targets, not a same-PR app-code change). #1727/#1730 remain open in W4. Prior: **114**, after #1725 closed via [PR #1771](https://github.com/TheurgicDuke771/DataQ/pull/1771) and #1724 closed via [PR #1774](https://github.com/TheurgicDuke771/DataQ/pull/1774) — both review-driven follow-ups from the #1632/#1723 tracking-hygiene check, closing out the last of the originally-planned W4 work; count held flat because #1724's own two `/code-review` rounds filed three more, [#1772](https://github.com/TheurgicDuke771/DataQ/issues/1772) (P1/security — RCA-narrative prompts bypass the G3 redaction mask `sql_generation`/`check_suggestion` use, a real design task not yet started), [#1773](https://github.com/TheurgicDuke771/DataQ/issues/1773) (the admin LLM connection-test probe is a live model call and is unlogged) and [#1775](https://github.com/TheurgicDuke771/DataQ/issues/1775) (no MCP tool lists which channel is linked to a suite) — all `v1.2 Backlog`, none started. #1726/#1727/#1730 remain open in W4, next up. Prior: **114**, after #1663 closed via [PR #1769](https://github.com/TheurgicDuke771/DataQ/pull/1769) — per-destination payload templates + auth header on the webhook publisher; no follow-ups filed. `/code-review`'s 8-agent pass completed fully this time (no spend-limit failures) and found 4 real, mutation-verified bugs, all fixed: `payload_template` could carry a de-facto credential yet was readable by any authenticated user (now Admin-only, `has_payload_template` for everyone else); `auth_header_value`/`auth_header_name` could be set independently, orphaning a dead secret (now validated as a pair, with cascade-clear); changing `webhook_url` silently redirected a stored auth-header credential to the new destination (the #1401 class — now blocked, re-supply required); and a template placeholder resolving to a dict/list inside a longer string produced invalid-JSON Python repr instead of `json.dumps`. Two findings considered and left as-is (documented in the row below): a URL-less webhook shell with auth-header fields set (matches the teams/slack no-webhook-yet precedent), and migration downgrade not purging SecretStore values (matches every other migration in this codebase)). Prior: **115**, after #1662 closed via [PR #1767](https://github.com/TheurgicDuke771/DataQ/pull/1767) — a fourth notification-channel type, a generic HMAC-signed outbound webhook for PagerDuty/Opsgenie/ServiceNow/Jira/self-hosted receivers; no follow-ups filed. Review's simplification pass (`/code-review`'s parallel agents mostly hit the session spend limit and failed with 429s — only one completed) was applied inline: dedup moved into `resolve_webhook_channels` from the publisher, a shared `_mint_hmac_secret` helper, named mint triggers, a table-driven field-mismatch check. Two automated push/commit security scanner flags (SSRF, credential exposure) verified inline as false positives with concrete evidence — see the PR description. Earlier: **116**, after #1515 closed via [PR #1765](https://github.com/TheurgicDuke771/DataQ/pull/1765) — incident/alert "Owner" attribution prefers the asset owner when set, falls back to suite owner; no follow-ups filed, but review caught the fix reimplementing the asset-resolution precedence rule a third/fourth time and it was consolidated into one canonical `run_service.resolve_asset`. Earlier: **117**, after #1514 closed via [PR #1763](https://github.com/TheurgicDuke771/DataQ/pull/1763) — reusable Teams/Slack/email notification channels, define once and reference from many suites; two review-scoped follow-ups filed, [#1761](https://github.com/TheurgicDuke771/DataQ/issues/1761) frontend UI and [#1762](https://github.com/TheurgicDuke771/DataQ/issues/1762) promote-an-existing-webhook, both `v1.2 Backlog`/W5. Earlier: **116**, after #1650 closed via [PR #1758](https://github.com/TheurgicDuke771/DataQ/pull/1758) and #1654 closed via [PR #1759](https://github.com/TheurgicDuke771/DataQ/pull/1759) — the MCP-honesty and evidence-card docs pages, closing out W4. Earlier: **118**, after #1626 closed via [PR #1756](https://github.com/TheurgicDuke771/DataQ/pull/1756) — the `get_doc` MCP tool. Earlier: **119**, after #1649 closed via [PR #1744](https://github.com/TheurgicDuke771/DataQ/pull/1744) — multi-table SQL generation. Earlier still: **120** (2026-08-30, after #1647 closed via [PR #1742](https://github.com/TheurgicDuke771/DataQ/pull/1742) — the alert-that-explains-itself — and #1635/#1633 closed via [PR #1739](https://github.com/TheurgicDuke771/DataQ/pull/1739)/[PR #1741](https://github.com/TheurgicDuke771/DataQ/pull/1741); the count held at 120 rather than dropping further because #1743 was filed the same session, a non-blocking follow-up from #1647's own review). Earlier: **109**, after #1513 closed via [PR #1719](https://github.com/TheurgicDuke771/DataQ/pull/1719) — three review-driven follow-ups filed, #1717/#1720/#1721 — and #1644 closed via [PR #1716](https://github.com/TheurgicDuke771/DataQ/pull/1716). Prior snapshot (2026-08-29 night) — the gap-review follow-through: forward roadmap page added at [context/roadmap-v1.2-v1.3.md](../context/roadmap-v1.2-v1.3.md) (v1.3 proposed theme: **Automation + Evidence**), story ledger started at [docs/stories.md](stories.md); five accepted never-asked items filed ([#1704](https://github.com/TheurgicDuke771/DataQ/issues/1704) backup/upgrade story, [#1705](https://github.com/TheurgicDuke771/DataQ/issues/1705) API compatibility policy, [#1706](https://github.com/TheurgicDuke771/DataQ/issues/1706) 10-minute eval path, [#1707](https://github.com/TheurgicDuke771/DataQ/issues/1707) opt-in telemetry seam, [#1708](https://github.com/TheurgicDuke771/DataQ/issues/1708) external security review); #1694/#1695 pulled into W7; adapter tail #1679–#1685 demand-gated behind #1678 (#1681 P2→P3). Earlier: the **admin control-centre track** is filed: epic [#1702](https://github.com/TheurgicDuke771/DataQ/issues/1702) (six routed sub-pages; mockup published 2026-08-29) over [#1693](https://github.com/TheurgicDuke771/DataQ/issues/1693) in-app membership (ADR 0043 to write; IdP stays a prerequisite — user direction), [#1694](https://github.com/TheurgicDuke771/DataQ/issues/1694) IA restructure, [#1695](https://github.com/TheurgicDuke771/DataQ/issues/1695) parity pass (4 backend-only admin endpoints), [#1696](https://github.com/TheurgicDuke771/DataQ/issues/1696) Overview health page, [#1697](https://github.com/TheurgicDuke771/DataQ/issues/1697) credential-health re-file of #954, [#1698](https://github.com/TheurgicDuke771/DataQ/issues/1698) write pass (supersedes #412), [#1699](https://github.com/TheurgicDuke771/DataQ/issues/1699) offboarding, [#1700](https://github.com/TheurgicDuke771/DataQ/issues/1700) admin lens (supersedes #411), [#1701](https://github.com/TheurgicDuke771/DataQ/issues/1701) Integrations; the [#1514](https://github.com/TheurgicDuke771/DataQ/issues/1514) channel model is user-confirmed (admin defines centrally, suites reference from a dropdown). Also includes the roadmap design-only sweep (user-directed): every remaining "_(no issue yet)_" marker in [context/post-v1-roadmap.md](../context/post-v1-roadmap.md) is now a filed issue — [#1667](https://github.com/TheurgicDuke771/DataQ/issues/1667)–[#1690](https://github.com/TheurgicDuke771/DataQ/issues/1690) (Theme 3 UI ×3, accessibility ×4, Theme 4 privacy pack ×3, Theme 8 datasource adapters ×9, Theme 9 results ×2, Theme 14 ecosystem ×3) — distributed 2026-08-29 by user direction: W5 +#1662/#1663/#1676, W6 +#1670, W7 +#1667/#1669/#1671/#1677/#1678/#1686/#1688/#1664, W8 +#1660/#1680/#1668 (spikes/onboarding), the other 12 stay `v1.2 Backlog`; verified-shipped rows marked instead of filed (asset view #760/#772+#782, OTLP endpoint #589, Vault→ADR 0039, RCA layers #1633/#1634/#1654); ITSM tier 2 deliberately unfiled (demand-gated on #1663). Also includes the issue-filing pass from the 2026-08-29 external competitive review (PR [#1665](https://github.com/TheurgicDuke771/DataQ/pull/1665)): [#1660](https://github.com/TheurgicDuke771/DataQ/issues/1660) automated coverage loop (the G-a remainder + the coverage metric), [#1661](https://github.com/TheurgicDuke771/DataQ/issues/1661) zero-config auto-baselines (unknown-unknowns), [#1662](https://github.com/TheurgicDuke771/DataQ/issues/1662)/[#1663](https://github.com/TheurgicDuke771/DataQ/issues/1663) the Theme-5 webhook publisher + PagerDuty/Opsgenie/ServiceNow/Jira payload templates (formerly "no issue yet" roadmap rows), [#1664](https://github.com/TheurgicDuke771/DataQ/issues/1664) control-plane positioning copy; scale-benchmark buyer questions added to [#1393](https://github.com/TheurgicDuke771/DataQ/issues/1393) as a comment. Earlier the same day: W3's core LLM track is DONE (#1511 + #1512 closed; admin UI #1641 merged). The competitive survey (internal doc, PR [#1646](https://github.com/TheurgicDuke771/DataQ/pull/1646)) drove a user-directed W4 feature set: [#1647](https://github.com/TheurgicDuke771/DataQ/issues/1647) alert-that-explains-itself, [#1648](https://github.com/TheurgicDuke771/DataQ/issues/1648) cadence-aware suggestions, [#1649](https://github.com/TheurgicDuke771/DataQ/issues/1649) multi-table SQL-gen, [#1650](https://github.com/TheurgicDuke771/DataQ/issues/1650) publish the MCP honesty discipline, plus #1635 promoted to W4 and #1644 (reaper). [#1651](https://github.com/TheurgicDuke771/DataQ/issues/1651) (opt-in DQ gate / circuit-breaker) filed **P3/Backlog by user direction — additive only, ADR required, existing orchestration contract unchanged**. Still open from W1: #590/#588 (user decision gates) + #1392/#1257. |
| **W2 close + deploy** | Milestone closed 2026-08-28 (22 closed / 0 open). **Both clouds deployed `4e13ecb1`** — per-service SHAs verified, migrate jobs green (delta incl. `5656bbfc1495`). The post-deploy WRITE probe (not the smoke) found [#1621](https://github.com/TheurgicDuke771/DataQ/issues/1621): the #1460 hash chain's seal UPDATE vs G1's append-only REVOKE 500ed every audited mutation — the known superuser-privilege-blindness class, on the chain's first deploy. Mitigated live on both DBs (column-scoped `GRANT UPDATE (prev_hash, row_hash)`), durable migration + privilege tests in [#1622](https://github.com/TheurgicDuke771/DataQ/pull/1622); Azure re-verified (suite create 200, #1607 gate 422s live, real SF run 2/2 on the new revision). **Standing rule: post-deploy verification includes an authenticated mutating request.** |
| **Open PRs** | **BOTH CLOUDS DEPLOYED `6c9588e2` 2026-09-02** (Azure [run 33668857934](https://github.com/TheurgicDuke771/DataQ/actions/runs/33668857934), AWS [run 33668860834](https://github.com/TheurgicDuke771/DataQ/actions/runs/33668860834)) — both burn-down batches are LIVE. Verified per the standing battery: per-service SHA on all six services, migrate job/task green with `alembic_version = 6bcf67868753` read back on BOTH DBs, both new indexes present and the marker index chosen by the planner, worker banner `concurrency: 4` + `celery`/`llm` queues on both clouds, public smoke 7/7 + 6/6 headers on both, Azure authenticated READ + WRITE probe 200, the #1772 backfill re-run on both DBs (0 rewrites — the #1809 as-of change confirmed a no-op on existing evidence), and the #1728/#1813 correlation confirmed on real Azure data (3 pipeline runs ↔ 3 DQ runs, 0 ambiguous). Local e2e before dispatch: OTP code queued at the 1.04s floor and delivered by the worker to Mailpit, sign-in completed, `/pipeline_runs` carries `triggered_run_ids`, DSR erase returns the per-store breakdown. AWS authenticated battery closed the same evening over user-supplied PATs (now in SSM `/dataq/demo/{admin,member,viewer}-pat`): `/me` + `PATCH /me` 200, viewer clamp 403, DSR breakdown, MCP `initialize` + `tools/list` 48 tools, and a **live Snowflake suite run through the AWS worker** (2/4, the known WARN + deliberate FAIL). Still open only on Azure: a live suite run (the Azure-CLI identity is a non-admin member with one view-only suite — use the KV `dataq-pat-w1-admin` PAT next time); post-deploy worker triage filed [#1826](https://github.com/TheurgicDuke771/DataQ/issues/1826) (pre-existing ADF poll 401). **1 open PR** (this docs record). Prior: **1 open** (2026-09-02, batch 2: the batch-2 docs PR). Prior: **1 open** (2026-09-02 after the merges: the batch-1 docs PR). Prior: **12 open** (2026-09-02, the backlog burn-down — #1798/#1799/#1800/#1801/#1805/#1807/#1808/#1810/#1812/#1813 fixes + #1802 the audit-gate unblocker + this docs PR; every fix PR reviewed with findings fixed or filed; #1807 is stacked on #1798 and #1801 needs #1802 first for a green gate). Prior: **0 app-work PRs** (2026-09-01; the 11-PR Dependabot version-bump queue — #1745–#1755, all patch/minor, zero open Dependabot security alerts — triaged and merged the same day. #1745 ruff and #1746 types-openpyxl each needed a companion `.pre-commit-config.yaml` pin bump to satisfy the #1473 sync checks (Dependabot never edits that file); ruff 0.16.5 was run over the whole repo first, clean. npm PRs merged one at a time with a `@dependabot rebase` between each and `pnpm install --frozen-lockfile` re-verified on `main` after every merge, per the lockfile cross-merge rule. The one open CodeQL alert — `py/unused-import` on the deliberate `llm_kinds` side-effect import in `worker/tasks.py` — dismissed won't-fix with rationale: importing IS the use, ruff carries the `noqa`, CodeQL ignores `noqa`). **W3 milestone CLOSED at 19/19** — all six scheduled items plus the in-week #1634/#1631; #1309 closed via #1638's review fixups. **BOTH CLOUDS DEPLOYED `41f20587`** (2026-08-29: Azure [run 33277875436](https://github.com/TheurgicDuke771/DataQ/actions/runs/33277875436) + AWS [run 33277876641](https://github.com/TheurgicDuke771/DataQ/actions/runs/33277876641), both green) — the #1624 pushdown batch + the whole LLM track are LIVE. Verified per the standing battery: per-service image SHA on all six services (3× ACA, 3× ECS — worker genuinely running, the #1361 check), Azure migrate job Succeeded (`dd652ae1ef85` applied), full public smoke on both (healthz/SPA/deep-link 200, `/api`+`/mcp`+`/admin/llm`+`/llm/sql_generation` all 401-gated, 6/6 security headers, `/docs` serves the SPA shell), **authenticated WRITE probe 200 on Azure** (audited PATCH `/me` — the #1621 class), `/admin/llm` correctly 403s a non-admin, beat heartbeats clean on both workers. AWS-side authenticated write not exercised (no non-interactive Cognito bearer; prior-practice limitation, noted not hidden). |
| **Docs redesign (unplanned, user-directed 2026-09-02/03)** | Epic [#1831](https://github.com/TheurgicDuke771/DataQ/issues/1831), plan `docs/docs-redesign-plan.md`. Shipped in nine PRs: Diátaxis IA + redirects (#1832) · screenshot/video capture lane + 16 screenshots + 5 clips (#1833) · generated check-type reference with drift guard (#1834) · **versioned docs via mike** — `latest` from main, one copy per `v*` tag, live (#1835) · every page as raw Markdown + `llms.txt` + Markdown/Copy/Ask-AI header row (#1836) · published OpenAPI + Swagger explorer + generated MCP tool reference (#1837) · hardening (#1839) · beginner landing, three new tutorials, concepts diagram, asset guard + three live fixes (#1841) · marketing refresh (this PR). Follow-ups: #1838 (ToolAnnotations), #1840 (stale Settings copy). SDK parked for v1.3 (#1829). **User-reported 2026-09-03, fixed same day:** [#1843](https://github.com/TheurgicDuke771/DataQ/issues/1843) — every looping clip froze on its poster after any instant navigation in Safari/WebKit (content adopted from an inert `DOMParser` document never runs media resource selection; Chromium/Firefox unaffected, which is how the capture lane missed it). `docs/site/assets/clips.js` re-kicks autoplay on `document$` with a `controls` fallback ([#1844](https://github.com/TheurgicDuke771/DataQ/pull/1844), verified in all three Playwright engines; review tightened the `load()` guard to `NETWORK_EMPTY`). **User-directed 2026-09-03 — AI features (LLM) guide** ([#1847](https://github.com/TheurgicDuke771/DataQ/pull/1847)): `guides/ai-features.md` with the Admin → LLM provider clip + screenshot and the incident evidence card, and a **real** `rca_narrative` output (local `qwen2.5:14b` via Ollama over the seeded incident) rendered through to the alert line. SQL-gen and check-suggestion payloads are labelled illustrative — both list columns live and the capture stack has no live SQL connection ([#1846](https://github.com/TheurgicDuke771/DataQ/issues/1846)); the page also made concrete that the three features have **no in-app UI** beyond the admin panel ([#1845](https://github.com/TheurgicDuke771/DataQ/issues/1845)). |
| **Coverage gates (CI-enforced, ≥80%)** | backend `--cov-fail-under=80` (~4,800 backend tests) · frontend all-src `lines: 80` (~1,000 tests) — every PR rides the same gates |

---

## Carried over from v1.1

Everything open at the v1.1 close, re-homed by name into the weekly milestones below
(GitHub is the source of truth for issue state; this register mirrors it so nothing
carried is lost). The two **decision gates** are the user's call, not the assistant's:
[#590](https://github.com/TheurgicDuke771/DataQ/issues/590) (Azure estate fate) and
[#588](https://github.com/TheurgicDuke771/DataQ/issues/588) (Snowflake leg retirement) —
W1 schedules the *decision*, never presumes the outcome. One item sits in `v1.2 Backlog`
rather than a week: [#1384](https://github.com/TheurgicDuke771/DataQ/issues/1384)
(CloudFront→ALB cleartext hop — blocked on buying a custom domain + ACM, not on
engineering).

**Unplanned, user-reported, shipped 2026-08-22 — the check-engine (gx/dmf) picker UI ([#1550](https://github.com/TheurgicDuke771/DataQ/issues/1550), PR [#1552](https://github.com/TheurgicDuke771/DataQ/pull/1552)).** The backend has supported per-check engine selection since #1529 (ADR 0036), but no UI ever surfaced it — every check authored through the app silently got `gx`, and Snowflake DMF was reachable only via API/MCP. Added a "Snowflake DMF" catalog category (4 types) plus an explicit Engine picker on Freshness (the one type both engines evaluate), gated to Snowflake connections. `/code-review` (4-agent pass) caught and fixed a version-history safety gap (a restore silently reinstating a different engine with no visible warning), a stale-form-value bug on an expectation-type switch, and a virtualized-Select accessibility bug; one review-suggested fix was reverted after mutation-testing proved it was dead code. [#1551](https://github.com/TheurgicDuke771/DataQ/issues/1551) filed for the remaining non-bug polish (engine not shown in the Suites list or RunDetail/RunReport).

---

## Cycle plan — v1.2 (8 weeks, 2026-08-22 → 2026-10-16)

> Forward view (v1.2 thesis + the proposed v1.3 **Automation + Evidence** cycle):
> [context/roadmap-v1.2-v1.3.md](../context/roadmap-v1.2-v1.3.md).

> Planned 2026-08-21 from [retro-v1.1.md](retro-v1.1.md), the v1.1 follow-up graph,
> [context/post-v1-roadmap.md](../context/post-v1-roadmap.md), and the 2026-08-21
> post-v1-notes sweep that filed #1509–#1516 (PR
> [#1517](https://github.com/TheurgicDuke771/DataQ/pull/1517)). GitHub mirror: milestones
> **`v1.2 Week 1..8`** (due Fridays), the cycle epic
> [#1518](https://github.com/TheurgicDuke771/DataQ/issues/1518), and the **DataQ Roadmap**
> project (every scheduled issue carries the `v1.2 week` single-select + Status). The
> closing week is the **last** week (user direction at planning), and the stretch tail is
> split across W7/W8 to keep weekly loads sane.

### v1.2 W1 — Decision gates + MCP honesty burn-down (due 2026-08-28)

⚡ **#590/#588 are the user's decision gates** (~2026-08-25 revisit): keep or tear down the
Azure estate and the harness Snowflake leg. Everything else in the week is independent of
the outcome, but #1392/#1257 are cheapest while the decision is fresh — and #895 (W5)
needs the Snowflake half decided.

| Status | Task | Theme |
|---|---|---|
| ⬜ | [#590](https://github.com/TheurgicDuke771/DataQ/issues/590) Azure estate: user decision + (if teardown) G-i + `tofu destroy` + credential retirement | ops / decision gate |
| ⬜ | [#588](https://github.com/TheurgicDuke771/DataQ/issues/588) Snowflake harness leg: user decision + clean retirement procedure (rehearsed 2026-07-04) | ops / decision gate |
| ⬜ | [#1392](https://github.com/TheurgicDuke771/DataQ/issues/1392) Verify the demo environment is actually disposable (`tofu destroy` never run) | ops |
| ⬜ | [#1257](https://github.com/TheurgicDuke771/DataQ/issues/1257) Narrow `DATAQ_LOADER`'s account-wide MANAGE GRANTS to the dbt hook's need | ops / least-priv |
| ✅ | [#1442](https://github.com/TheurgicDuke771/DataQ/issues/1442) MCP: time filter on `list_runs`/`list_incidents` ("what failed today?") | MCP honesty |
| ✅ | [#1445](https://github.com/TheurgicDuke771/DataQ/issues/1445) MCP: `list_incidents` auto-resolve makes "what failed today" silently empty | MCP honesty |
| ✅ | [#1443](https://github.com/TheurgicDuke771/DataQ/issues/1443) MCP: `get_adf_pipeline_status` misnamed (covers ADF/Airflow/dbt) | MCP honesty |
| ✅ | [#1446](https://github.com/TheurgicDuke771/DataQ/issues/1446) MCP: `list_connections` vs `test_connection` error-classification contradiction | MCP honesty |
| ✅ | [#1447](https://github.com/TheurgicDuke771/DataQ/issues/1447) MCP: trim two over-long descriptions + drop unlookuppable refs | MCP honesty |
| ✅ | [#1448](https://github.com/TheurgicDuke771/DataQ/issues/1448) MCP: `create_check` under-caveated relative to the surface | MCP honesty |

**Exit gate:** #590/#588 decisions recorded by the user (and executed if teardown);
#1392/#1257 discharged consistently with them; all six MCP honesty follow-ups closed.

**Unplanned, user-directed (2026-08-22, in-week):** codebase-wide prose trim, PR
[#1536](https://github.com/TheurgicDuke771/DataQ/pull/1536) — comment/docstring density
cut to ~≤0.2 prose-to-code across backend/tests/alembic/frontend/infra (−27k lines,
680 files; `mcp/server.py`'s LLM-facing docstrings deliberately untouched). Code proven
unchanged four independent ways (docstring-stripped AST, TS token stream, YAML structural
diff, heredoc-aware line diff); the /code-review pass returned 10 findings (garbled
truncations, erased guard comments, one `argparse --help` behavior change) — all fixed
pre-merge, load-bearing security/ops constraints restored in condensed form.

### v1.2 W2 — Expectation catalog + server-side allowlist (due 2026-09-04)

The DQ-intelligence track opens ([post-v1-dq-intelligence-notes.md](post-v1-dq-intelligence-notes.md), filed as #1509–#1513 by the 2026-08-21 sweep).

| Status | Task | Theme |
|---|---|---|
| ✅ | [#1509](https://github.com/TheurgicDuke771/DataQ/issues/1509) The 5 high-ROI GX built-ins (`mostly`, compound/cross-column, type, set/date) + the aggregate-stats kind decision — PR [#1608](https://github.com/TheurgicDuke771/DataQ/pull/1608): 7 new catalog types + `mostly` (floored 0.01 — `mostly=0` is a silent green), aggregates decided onto the monitor-kind path ([#1602](https://github.com/TheurgicDuke771/DataQ/issues/1602)); strftime is dataframe-only (gated on all three doors), set relations have inert thresholds ([#1607](https://github.com/TheurgicDuke771/DataQ/issues/1607)); cross-column types live-verified on Snowflake 2026-08-28 (found + fixed the compound-unique casing bug [#1616](https://github.com/TheurgicDuke771/DataQ/issues/1616) → [#1617](https://github.com/TheurgicDuke771/DataQ/pull/1617)) | intelligence |
| ✅ | [#1510](https://github.com/TheurgicDuke771/DataQ/issues/1510) Curated expectation superset (4a) + **server-side `expectation_type` allowlist** on REST + MCP — PR [#1614](https://github.com/TheurgicDuke771/DataQ/pull/1614): `datasources/expectation_allowlist.py` (25 vetted types, `dataframe_only` capability records) enforced on all FOUR author-time doors incl. the previously-ungated **dry-run preview**; refusal distinguishes "not a GX expectation" from "recognised, not enabled" and MCP forwards `detail.supported`; review fix: a stored legacy type stays editable/restorable (`permitted_stored_type`, mutation-verified) — only *changing* to an unvetted type refuses; export→import of legacy types is the one recorded exception; 10 SQL-capable additions share #1608's pre-deploy live-verification requirement (#953) | intelligence / security |
| ✅ | [#1505](https://github.com/TheurgicDuke771/DataQ/issues/1505) API honesty: `extra='ignore'` lets an invented knob validate cleanly and do nothing — PR [#1609](https://github.com/TheurgicDuke771/DataQ/pull/1609): `ApiRequestModel` (`extra='forbid'`) on 32 request models across 14 routers, export/import document models split (drift guard filed [#1610](https://github.com/TheurgicDuke771/DataQ/issues/1610)); ⚠️ breaking for clients sending unknown fields (CHANGELOG noted) | quality |
| ✅ | [#1320](https://github.com/TheurgicDuke771/DataQ/issues/1320) Suite-delete confirmation states its blast radius (N checks/runs/results) — PR [#1604](https://github.com/TheurgicDuke771/DataQ/pull/1604): `GET /suites/{id}/deletion_impact` (exact counts, delete-grade authz) + the confirm dialog states counts and irreversibility, fetch-failure fallback never blocks the delete | ux safety |
| ✅ | [#1326](https://github.com/TheurgicDuke771/DataQ/issues/1326) Migration-parity literal-set check: IS NULL vs IS NOT NULL — PR [#1603](https://github.com/TheurgicDuke771/DataQ/pull/1603): `_null_polarities()` extractor diffed beside `_literals()`; review caught multi-word-cast + outer-`NOT` regex bugs pre-merge; generalized operator class filed as [#1605](https://github.com/TheurgicDuke771/DataQ/issues/1605) | test |
| ✅ | [#1551](https://github.com/TheurgicDuke771/DataQ/issues/1551) Suites check-list card: engine/dimension/threshold badges + engine on RunDetail/RunReport — PR [#1611](https://github.com/TheurgicDuke771/DataQ/pull/1611): shared `checkBadges.tsx` (engine visual map typed against `CheckEngine` with unknown-engine fallback — dqx/dataplex no longer render as gx; dimension guard keeps ADR 0038's unclassified state visible), 3-way `ENGINE_LABEL` dedup incl. the check-editor Select, `DIMENSION_LABEL` + ScorecardPanel dedup; historical engine/dimension on `ResultRead` filed as [#1613](https://github.com/TheurgicDuke771/DataQ/issues/1613) | ux |
| ✅ | [#1616](https://github.com/TheurgicDuke771/DataQ/issues/1616) *(in-week, live-verification find)* compound-unique errors on live SF with uppercase columns — PR [#1617](https://github.com/TheurgicDuke771/DataQ/pull/1617): fold via the dialect's own `normalize_name`; review's index_columns sibling-door claim **refuted by live run** (locators work as authored — fold reverted); result-casing residual filed [#1618](https://github.com/TheurgicDuke771/DataQ/issues/1618) | bug |
| ✅ | [#1607](https://github.com/TheurgicDuke771/DataQ/issues/1607) *(in-week)* set-relation thresholds can never fire — PR [#1619](https://github.com/TheurgicDuke771/DataQ/pull/1619): AC1 settled on live SF (no bandable scalar, pass AND fail); `bandable` capability + refusal on every door incl. threshold-only PATCH (mutation-verified); editor hides the block; migration `5656bbfc1495` nulls the always-inert stored values in `checks` + `check_versions` (proven up+down on real Postgres); CheckEdit now surfaces unmatched-field 422s instead of silently no-opping | quality |

**Exit gate:** the built-in groups author end-to-end in the check editor; the allowlist
refuses an unknown `expectation_type` on every write surface; #1505 decided/landed.

### v1.2 W3 — LLMProvider seam + SQL generator — **CLOSED 2026-08-29, 6/6 + two in-week additions, ~2 weeks early** (was due 2026-09-11)

| Status | Task | Theme |
|---|---|---|
| ✅ | [#1511](https://github.com/TheurgicDuke771/DataQ/issues/1511) `LLMProvider` seam — **ADR [0042](site/adr/0042-llm-provider-seam.md) Accepted** (PR [#1636](https://github.com/TheurgicDuke771/DataQ/pull/1636)) + backend (PR [#1640](https://github.com/TheurgicDuke771/DataQ/pull/1640)): `backend/app/llm/` (anthropic SDK + openai_compat httpx impls; `native \| prompt_json` structured-output ladder, callers re-validate), `llm_settings` singleton + `llm_invocations` (poll + audit/cost record) behind `require_workspace_admin`, credential write-only with the #1403 destination-field rule, worker-side `llm_invoke` with a pending→running claim guard, new `llm` rate-limit class (+ ipall ceiling), G4 posture 3-state flip, orphan-sweep registry entry. **Live-verified against a real Ollama server both structured modes** (#953-class). `/code-review` found 10 real defects pre-merge incl. a prod-killer the fixture-built test schema hid (missing `gen_random_uuid()` default → every insert dies on a MIGRATED db) and a NUL-in-model-output path that stranded rows in `running`; three repo guards (migration-parity, sweep-registry, assert-hygiene) each caught one more. Admin UI panel: PR [#1641](https://github.com/TheurgicDuke771/DataQ/pull/1641) | intelligence |
| ✅ | [#1512](https://github.com/TheurgicDuke771/DataQ/issues/1512) LLM SQL generator — PR [#1643](https://github.com/TheurgicDuke771/DataQ/pull/1643): `services/llm_sqlgen.py` (first kind on the seam) + `POST /llm/sql_generation` (suite-edit, 202→poll) + the seam's `KIND_VALIDATORS` output gate — the model's SQL rides the ADR 0019 validator **on the already-NUL-scrubbed bytes** before storage; prompt = dialect + qualified target + column names + optional masked EGRESS profile stats (`top_n=0`, warehouse-tag floor), never sample rows. **Live-verified against Ollama both structured modes, injection string in the column list — read-only SQL out, gate holds.** The 8-angle `/code-review` returned ~20 real findings, headline classes: validate-then-mutate TOCTOU (NUL splitting `INTO` for the validator, scrub re-joining it in storage), name-only column egress with no access record on the DEFAULT path (guard-at-one-door), pre-model context failures misreported as model-output failures (new `llm_request_invalid`), a targetless suite 202-then-failing async (shared `check_generation_preconditions` at both altitudes), refuse-don't-guess on unlistable columns, dispatch-failure clobber race, CodeQL forcing the kind-registration imports to become load-bearing (`llm_kinds` + startup wiring guard). Deferred → [#1644](https://github.com/TheurgicDuke771/DataQ/issues/1644) (invocation reaper, W4) + [#1645](https://github.com/TheurgicDuke771/DataQ/issues/1645) (profiler double-login perf). Admin LLM settings panel also merged (PR [#1641](https://github.com/TheurgicDuke771/DataQ/pull/1641)) | intelligence |
| ✅ | [#1247](https://github.com/TheurgicDuke771/DataQ/issues/1247) Couple the near-miss read/write tuple derivation in code — PR [#1637](https://github.com/TheurgicDuke771/DataQ/pull/1637): one `near_miss_partner_envs()` used by both sides; review caught the matrix test passing green under over-inclusion drift (fixture had no same-env binding) — fixed + mutation-verified both directions | refactor |
| ✅ | [#1307](https://github.com/TheurgicDuke771/DataQ/issues/1307) Lineage floor-denial message: structural, not string-equality — PR [#1638](https://github.com/TheurgicDuke771/DataQ/pull/1638): `not_authorized_label` threaded at classification time; review found two MORE unlabelled sibling doors (seed enumeration misattributing to GET_LINEAGE; the no-top floor path reporting only an exception class name) — both fixed + mutation-verified | refactor |
| ✅ | [#1309](https://github.com/TheurgicDuke771/DataQ/issues/1309) Lineage: ACCESS_HISTORY denial mislabeled with the GET_LINEAGE grant message — closed by PR [#1638](https://github.com/TheurgicDuke771/DataQ/pull/1638)'s review fixups (the two remaining unlabelled not-authorized call sites got `not_authorized_label` threaded structurally) | bug |

| ✅ | [#1634](https://github.com/TheurgicDuke771/DataQ/issues/1634) *(in-week, RCA survey find)* the incident evidence card was dark in the UI — PR [#1639](https://github.com/TheurgicDuke771/DataQ/pull/1639): evidence drawer on IncidentsPanel + incident tags on RunDetail failing rows, explicit not-available states; review caught the frontend hand-copying `FAILING_TIERS` without `warn` (and a test locking the wrong behavior in) — the backend opens incidents on warn too | incidents |

| ✅ | [#1631](https://github.com/TheurgicDuke771/DataQ/issues/1631) *(in-week, filed at track start)* opt-in live-LLM lane — PR [#1656](https://github.com/TheurgicDuke771/DataQ/pull/1656): `backend/tests/e2e/test_llm_live.py`, gated on `DATAQ_LLM_LIVE=1` + base URL (8 skips by default, never a required check); proves the OpenAI-compat impl against a REAL Ollama server — usage/cost mapping, BOTH structured-ladder modes, wire error taxonomy (refused/timeout/unknown-model), and the full sql_generation worker body end-to-end per mode with an injection string in the column list. Live 8/8 twice (pre- and post-review); review fixed a strict-mode schema that would have failed against OpenAI/Azure — the lane's own portability proven before it's pointed anywhere else | intelligence |

**Exit gate:** LLM features degrade gracefully to fully-working with no provider
configured; generated SQL passes the same validation a human's would; the seam ADR is
Accepted. — **MET** (2026-08-29: default-off end-to-end — no `llm_settings` row means every LLM surface is absent and everything else works, feature endpoints 409 `llm_not_configured`; generated SQL rides the ADR 0019 validator on the exact stored bytes + the editor's dry-run path unchanged; ADR 0042 Accepted + live-verified against a real local model). **Milestone closed at 19/19 (0 open).**

### v1.2 W4 — LLM check suggestions + compliance G2 — **CLOSED 2026-09-01, 23/23, ~2.5 weeks early** (was due 2026-09-18)

| Status | Task | Theme |
|---|---|---|
| ✅ | [#1513](https://github.com/TheurgicDuke771/DataQ/issues/1513) LLM curated check suggestions — profiler-driven, catalog-constrained structured output ([PR #1719](https://github.com/TheurgicDuke771/DataQ/pull/1719)): `POST /llm/check_suggestions`, second kind on the seam; structured output constrained to a single-column-only vetted expectation vocabulary, every suggestion re-validated through the same gate a human's `create_check` reaches (bad ones dropped, not surfaced). Scope: SQL-queryable connections only, same as #1512. The review/apply UI ships next (#1512's own frontend is also still unbuilt) | intelligence |
| ✅ | [#432](https://github.com/TheurgicDuke771/DataQ/issues/432) Compliance G2: data-subject-rights machinery (erase / access / portability) — design agreed with user, [PR #1586](https://github.com/TheurgicDuke771/DataQ/pull/1586) merged | compliance |
| ✅ | [#1267](https://github.com/TheurgicDuke771/DataQ/issues/1267) `unparsed_value` scalar cell has no retention sweep (adjacent to #1253) — [PR #1585](https://github.com/TheurgicDuke771/DataQ/pull/1585) merged | compliance |
| ✅ | [#1477](https://github.com/TheurgicDuke771/DataQ/issues/1477) Audit retention sweep: a zero-row run is unobservable | compliance |
| ✅ | [#1644](https://github.com/TheurgicDuke771/DataQ/issues/1644) `llm_invocations` stuck-row reaper — [PR #1716](https://github.com/TheurgicDuke771/DataQ/pull/1716) merged: mirrors `reap_stuck_runs`, plus a symmetric guard on `execute_invocation`'s own terminal write (review found the reap-race cut both ways — a slow-not-dead worker could resurrect a row the reaper had already closed out). #1717 (missing status index) filed as a non-blocking follow-up | intelligence |
| ✅ | [#1626](https://github.com/TheurgicDuke771/DataQ/issues/1626) MCP: expose curated docs via a `get_doc` tool — `/mcp` goes 47→48 tools (25 read-only / 18 write / 5 live-probe, all three now derived from `mcp_gates.GATES` rather than hand-typed on every doc page). New `backend/app/mcp/docs_catalog.py` scans a curated allowlist (five top-level pages + all five `compliance/*.md`) live at call time rather than hand-listing slugs — the same "declare as data" shape `mcp_gates.GATES` itself exists to enforce — and deliberately excludes ADRs/`architecture.md` (contributor design-rationale, not this tool's audience). `get_doc` returns the page verbatim (no summarization); an unrecognized `page` restates the current valid list. `page`'s JSON-schema carries an `enum` of the live catalog, so a client sees the full valid set on every turn with no companion `list_doc_pages()` tool needed. `backend/Dockerfile` now copies `docs/site` into the runtime image (it never had been — `get_doc` would have 404'd in every deployed environment despite working locally) | intelligence |
| ✅ | [#1632](https://github.com/TheurgicDuke771/DataQ/issues/1632) Prompt-injection adversarial battery for #1513/#1512 — extends `tests/support/adversarial.py` with a shared injection-string battery, parametrized across column/top-value/range/table/schema slots, proving each reaches the prompt as inert data; the pre-existing per-feature output-gate tests are the other half of the posture (output validation, not prompt hygiene). Plus a structural guarantee neither builder can even import the model that carries sample rows, and that a response is never logged unredacted — both mutation-verified against a deliberately reintroduced leak | intelligence |
| ✅ | [#1633](https://github.com/TheurgicDuke771/DataQ/issues/1633) LLM root-cause narrative for failed checks — Layer 2 on the evidence card: third `rca_narrative` kind on the #1511 seam, `POST /llm/rca_narrative` (view-gated, unlike the edit-gated sql_generation/check_suggestions — nothing is ever saved to the suite). The prompt is exactly the incident's evidence card, run through the SAME `evidence_for_caller` per-caller redaction the read API applies, plus a longer ≤180-point check history via the existing `/checks/{id}/history` service function. Structured output `{summary, ranked_hypotheses: [{cause, confidence, evidence_refs}], suggested_next_checks}`, every hypothesis constrained to cite a real evidence-layer name (closed vocab). `blind_spots` (what the snapshot could NOT see — a deleted check, insufficient anomaly history, withheld cross-suite siblings, no lineage, no profile_diff) is computed deterministically server-side, never asked of the model, per the #1648 `coverage_warnings` precedent. Mutation-verified: the requester-erasure guard, the evidence-layer allowlist, and the redaction integration | intelligence |
| ✅ | [#1635](https://github.com/TheurgicDuke771/DataQ/issues/1635) Evidence-card enrichment — cross-suite same-asset siblings + kind-aware payloads: `same_asset_siblings` finds the latest result of every OTHER check on the SAME asset across ALL suites (window-bounded 7 days, `succeeded` runs only), the cross-suite root-cause signal the same-run-only `sibling_checks` layer can't see; `kind_detail` dispatches `observed_value`'s three monitor-kind JSONB shapes (freshness/volume/schema_drift/anomaly) into named fields. The card itself stays workspace-true (ADR 0037, built once at sync time with no caller); the new `incident_service.evidence_for_caller` redacts sibling entries from a suite the caller can't view (folded into `same_asset_siblings_restricted_count`) on both REST and MCP `get_incident` reads. `profile_diff` stays the documented null (out of scope, per the issue) | incidents |
| ✅ | [#1647](https://github.com/TheurgicDuke771/DataQ/issues/1647) The alert that explains itself — evidence summary + RCA narrative in the alert channel: `render.incident_line` now appends a deterministic `evidence_summary_clause` (upstream pipeline status + delay-vs-history, same-run sibling failures, downstream-blast-radius count — no LLM required, ADR 0042's default-off rule) plus, when one has already been generated for the incident, #1633's narrative one-liner tagged with the evidence layer its top hypothesis cites. Absence renders explicitly ("not pipeline-triggered", "no downstream lineage recorded") rather than being silently dropped — the same #828 discipline the MCP `get_incident` docstring already applies to this card. New `llm_rca.latest_narrative_for_incident` (a `succeeded`-only, most-recent lookup — RCA is on-demand, alert delivery never triggers one) feeds `IncidentCard.narrative`, itself now redacted same-suite-only via #1635's `evidence_for_alert`. Mutation-verified: the upstream-clause None-vs-unavailable distinction, the succeeded-only status filter (a newer in-flight retry must not shadow a still-valid older narrative), and the builder→card wiring | alerting |
| ✅ | [#1648](https://github.com/TheurgicDuke771/DataQ/issues/1648) Cadence-aware suggestions — bound-pipeline schedule + delay history drive thresholds: `orchestration_service.compute_pipeline_cadence` (median/max inter-run gap from `pipeline_runs`, insufficient-history honesty, a deterministic margined-default threshold) grounds an optional `monitor:freshness` suggestion only when an enabled trigger binding exists and its cadence is grounded — never offered ungrounded. Reuses the existing #1199 near-miss machinery as a deterministic `coverage_warnings` field. `GET /suites/{id}/cadence` exposes the same deterministic hint with no LLM involved — the check-editor UI wiring itself, like #1512/#1513's own review/apply UI, ships next. Review found and fixed a NaN/Infinity threshold crashing the whole suggestion batch (uncaught `decimal.InvalidOperation`) and a multi-binding pick with no `ORDER BY` at all | intelligence |
| ✅ | [#1649](https://github.com/TheurgicDuke771/DataQ/issues/1649) Multi-table SQL generation — cross-table checks on one connection: `POST /llm/sql_generation` gains `additional_tables` (≤4, same connection — no connection reference in the request shape at all, so cross-connection is refused structurally, not at runtime). `llm_prompt_context.py`'s column-listing/profiling helpers split into `_for_target` primitives (explicit table/schema/catalog) with the suite's-own-target versions now thin wrappers, so #1649 reuses the exact same masking/audit-event machinery #1512/#1513 already have — including `probed_other_target`, the existing live-probe convention (ADR-governed) for not trusting the suite's own warehouse tags verbatim against an unrelated table. Backend-only, consistent with #1512/#1513/#1633's own still-unbuilt frontend (no "Generate with AI" surface exists yet to extend with a table picker). Mutation-verified: dedup, the ≤4 cap, blank-name rejection (all three initially vacuous — passing only because an unmocked live-connection attempt failed first — caught and fixed), and the `probed_other_target` wiring | intelligence |
| ✅ | [#1650](https://github.com/TheurgicDuke771/DataQ/issues/1650) docs: publish the MCP honesty discipline as a docs-site page — new `docs/site/architecture/mcp-honesty.md` (nav'd under Reference): the six audit criteria (population / time window / truncation / freshness / null-zero semantics / adjacent blindness), the summary→returns→caveats docstring shape, the three safety annotations (read-only / state-changing / live-probe) derived from the same registry that enforces authorization, and the three standing rules (partial-as-final, unknown-as-zero, one-null-two-meanings). No ticket citations per the published-docs rule; `mcp-setup.md`'s existing "Reading the results honestly" section now points to it as the fuller treatment | intelligence |
| ✅ | [#1654](https://github.com/TheurgicDuke771/DataQ/issues/1654) docs: publish the evidence-card contract — new `docs/site/architecture/evidence-card.md` (nav'd under Reference, sibling of #1650): the nine layers and what each is computed from, the privacy guarantee (`failing_result` never carries a sample row, `same_asset_siblings` narrowed per-caller vs per-alert), the per-layer `null` semantics (three different meanings across `upstream_pipeline_run`/`kind_detail`/the list layers/`downstream_blast_radius`/the always-null `profile_diff`), and the closed-vocabulary provenance rule the RCA narrative inherits. No ticket citations, sourced directly from `incident_evidence.build_evidence` and the already-reviewed `get_incident` docstring wording rather than re-derived | incidents |
| ✅ | [#1725](https://github.com/TheurgicDuke771/DataQ/issues/1725) docs(compliance): outbound-LLM transfer vector documented as "not built" while shipped and deployed to both clouds — [PR #1771](https://github.com/TheurgicDuke771/DataQ/pull/1771) merged: `docs/site/security/overview.md`, `sub-processors.md`, `dpia-input-sheet.md` and `admin.py`'s own `_llm_intelligence_transfer` text all corrected. First draft copied `admin.py`'s existing "masked aggregate profiler statistics — never sample rows" claim verbatim; two independent review passes found it FALSE for the `rca_narrative` kind (#1633), which sends the triggering check's raw `observed_value`/`expected_value`, not routed through the G3 mask `sql_generation`/`check_suggestion` use. Rewrote all four texts precisely rather than ship the overclaim; filed [#1772](https://github.com/TheurgicDuke771/DataQ/issues/1772) (the actual redaction gap — P1/security, real design task) and [#1773](https://github.com/TheurgicDuke771/DataQ/issues/1773) (the admin connection-test probe is a live LLM call and is unlogged) rather than rush a fix to a security-sensitive path | compliance |
| ✅ | [#1724](https://github.com/TheurgicDuke771/DataQ/issues/1724) `get_notification_config`'s SMTP gate over-required `email_from` (delivery works via `EmailPublisher`'s documented `sender or username` fallback) — [PR #1774](https://github.com/TheurgicDuke771/DataQ/pull/1774) merged, expanded across two `/code-review` rounds into a broader honesty pass on the same tool: round 1 found suite-linked `NotificationChannel`s (#1514/#1662/#1663) were invisible to every field — a suite relying solely on one read as having nothing configured, the tool's own "nobody" false answer through an undiscovered fourth door; round 2 found the tool never reported the 4th channel *type* (`"webhook"`, generic HMAC-signed, #1662/#1663 — no suite/workspace fallback of its own, so `has_generic_webhooks` added) and that Teams/Slack/email deliver to the **union** of the suite-or-workspace primary and any linked channel, not either/or (`_channel()`'s single-source pick hid simultaneous delivery — three new independent `*_channel_linked` fields added, reported regardless of `*_source`). 7 regression tests total, all mutation-verified; `mcp-honesty-reviewer` ran clean on both rounds. Filed [#1775](https://github.com/TheurgicDuke771/DataQ/issues/1775) (no MCP tool lists/identifies which channel is linked to a suite) rather than invent a docstring pointer to a tool that doesn't exist | intelligence |
| ✅ | [#1726](https://github.com/TheurgicDuke771/DataQ/issues/1726) `llm_invocations` reaper thresholds mis-calibrated — [PR #1778](https://github.com/TheurgicDuke771/DataQ/pull/1778) merged: Part A, `llm_invoke` shares its Celery queue with `run_suite` (no `task_routes`), so a busy worker (this deployment class has 1M-200M-row UC perf suites) can leave a dispatch queued-but-intact past the 15m pending cutoff, reaped as "the dispatch was likely lost" — false; Part B, `execute_invocation` marks `running` BEFORE calling the kind builder, and the check-suggestion builder profiles the warehouse live inside that window, so cold-warehouse-resume + a wide-table profile + the provider's own worst case (120s × up to 2 attempts) can plausibly cross the 10m running threshold for a healthy call. Fixed: both defaults raised (30m/20m) and both reap-reason strings reworded to admit the ambiguity rather than assert an unverifiable cause. Review round 2 found the code fix hadn't propagated to `.env.app.example`, which ships these two keys active (not blank) and which `setup.sh` copies verbatim into every fresh stack — every new local/dev/eval environment would have silently kept the pre-fix exposure; fixed, plus a new regression test loads the template as `Settings` and asserts it matches the code default so either side drifting alone fails it. The structurally correct Part-A fix (a dedicated Celery queue) is deliberately scoped out as [#1777](https://github.com/TheurgicDuke771/DataQ/issues/1777) — it needs the worker command changed in three deploy targets in the same coordinated step as the routing change, or an app-code-only deploy would silently stop all `llm_invoke` processing | intelligence |
| ✅ | [#1727](https://github.com/TheurgicDuke771/DataQ/issues/1727) LLM check-suggestions discarded the "rejected" reasons in the all-rejected case the docstring points users at — [PR #1780](https://github.com/TheurgicDuke771/DataQ/pull/1780) merged: when every suggestion fails validation, the built `rejected` list was thrown away since `execute_invocation`'s `DataQError` branch stores only a flat error string and leaves `response` NULL. Fixed by folding reasons into the exception message, capped at `MAX_SUGGESTIONS`. Kept the existing hard-failure design (a run that suggests nothing runnable is a failure, not an empty success) rather than reversing it into a soft "succeed with empty suggestions" outcome. Three review rounds then found and fixed two more real bugs on the same code path: no TOTAL-length cap on the assembled message before `execute_invocation`'s own 1024-char truncation (up to 8 individually-fine reasons joined together could still silently lose the "(+N more)" suffix — the exact #1727 defect, recreated one layer down) and an unbounded, untrusted `expectation_type` echoed with no cap (unlike `check_service.py`'s own `_ERROR_ECHO_MAX_CHARS` convention). Also caught and fixed a crash the fix's own first draft introduced: `expectation_type` isn't guaranteed to be a string, and slicing a non-str value raises `TypeError`. A larger architectural finding — `DataQError.detail` is discarded by `execute_invocation` and `llm_rca.py`'s own rejection path has no reason at all, worse than checksuggest's pre-fix state — filed as [#1781](https://github.com/TheurgicDuke771/DataQ/issues/1781) rather than folded in | intelligence |
| ✅ | [#1730](https://github.com/TheurgicDuke771/DataQ/issues/1730) No test asserts either reaper's beat-schedule entry exists — [PR #1782](https://github.com/TheurgicDuke771/DataQ/pull/1782) merged: `test_celery_app.py` had dedicated tests for five beat entries but none for `reap-stuck-runs` (#309) or `reap-stuck-llm-invocations` (#1644); the whole-schedule iterators only inspect entries that exist, so a dropped/renamed key silently disables either compensator and still passes CI — the #405 silent-death class, on the exact tasks built to catch silent worker death. Two new tests close the gap, mutation-verified against a deliberately-deleted entry. `/code-review` returned zero findings | reliability |
| ✅ | [#1773](https://github.com/TheurgicDuke771/DataQ/issues/1773) `POST /admin/llm/test` made a live LLM call with no `llm_invocations` audit row — [PR #1784](https://github.com/TheurgicDuke771/DataQ/pull/1784) merged: every feature-kind call lands a terminal-state row via `execute_invocation`, but the admin test probe (`test_settings`) called the provider directly with no row created anywhere, falsifying the "every call is recorded" compliance claim (`admin.py`'s `_llm_intelligence_transfer`, `docs/site/security/overview.md`). Fixed via a new `kind="ping"` row (already reserved in `LLM_INVOCATION_KINDS`, previously unused) landed once terminal — round 1 review consolidated a duplicate-write draft into a single insert and removed a pointless non-durable `flush()`; round 2 (8 more agents) found two real bugs in THAT refactor: `LLMConfigInvalidError` (a pure config-shape defect, zero network attempt) was being recorded as a completed call, and the round-trip's narrow except let any OTHER exception propagate uncaught — meaning a real, billed call could leave zero trace, the exact gap this issue exists to close. Both fixed; a catch-all now matches `execute_invocation`'s own posture. Plus `_scrub_nul` on the stored error (the #1273 NUL-byte class) and two stale sibling compliance texts corrected. 7 regression tests, all mutation-verified | intelligence |
| ✅ | [#1781](https://github.com/TheurgicDuke771/DataQ/issues/1781) `execute_invocation` discarded `DataQError.detail`; `llm_rca.py`'s own rejection path had no reason at all — [PR #1786](https://github.com/TheurgicDuke771/DataQ/pull/1786) merged: `DataQError.__init__` already carries a structured `detail`, but `execute_invocation`'s failure branch discarded it unconditionally and `response` was never set on a failed invocation. Now persisted (scrubbed) alongside `error`; `llm_checksuggest.py`'s all-rejected raise and `llm_rca.py`'s new `_valid_hypothesis` (previously bare `None` on rejection, zero reason captured — worse than checksuggest's own pre-#1780 state) both use it. A 5-agent review pass then found and fixed: no size cap on the persisted `response` (unlike `error`'s `[:1024]` — added `_DETAIL_MAX_CHARS`, oversized detail becomes `{"truncated": True}`); the structured `detail` had no truncation signal of its own (added a `truncated` bool to both payloads); RCA's own message had no total-clause cap unlike checksuggest's (latent today, added anyway); `llm_sqlgen.py` dropped its own validator's `detail` when re-raising (forwarded, closing the 3rd `KIND_VALIDATORS` registrant's inconsistency); RCA's route docstring left stale relative to checksuggest's. Filed [#1787](https://github.com/TheurgicDuke771/DataQ/issues/1787) (a related but separate pre-existing gap in `check_service.validate_monitor_check`, human-authored-check REST path). 12 regression tests total, all mutation-verified | intelligence |
| ✅ | [#1777](https://github.com/TheurgicDuke771/DataQ/issues/1777) `llm_invoke` shared the default Celery queue with `run_suite`, false-killable by the pending reaper behind a long `run_suite` backlog — [PR #1789](https://github.com/TheurgicDuke771/DataQ/pull/1789) merged: `task_routes={LLM_INVOKE_TASK_NAME: {"queue": "llm"}}`, worker command updated to `-Q celery,llm` in all **four** deploy targets (three known + `docker-compose.ghcr.yml`, found by a broader search). 5-agent review found: the task name was independently hardcoded in three places (plus a test) — consolidated into one `LLM_INVOKE_TASK_NAME` constant; two comments this fix itself made stale (a threshold-rationale note, the persisted `_PENDING_REAP_REASON` string); the new comment's own "no longer starves" claim overclaimed (queue separation prevents FIFO fetch-blocking, not execution-slot contention — filed [#1790](https://github.com/TheurgicDuke771/DataQ/issues/1790), no deploy config sets `--concurrency` despite `perf-baseline.md` claiming 4); and the deploy-ordering hazard (routine image-only rolls would silently orphan `llm_invoke` until Terraform is reapplied) live-confirmed and elevated into an explicit pre-deploy checklist item. 2 regression tests, mutation-verified | intelligence |
| ✅ | [#1772](https://github.com/TheurgicDuke771/DataQ/issues/1772) P1/security: RCA-narrative evidence carried an unmasked `observed_value`, reaching `get_incident` (REST/MCP), the RCA LLM prompt, AND outbound alert delivery, not just the RCA path the issue named — [PR #1791](https://github.com/TheurgicDuke771/DataQ/pull/1791) merged: `Incident.evidence` is a stored JSONB snapshot computed ONCE at incident-open/attach time and never recomputed, so all three readers inherited the exposure from one root — `incident_evidence.py::_failing_result_layer`, now routing `observed_value` through `run_service.redact_observed_value` (the same G3 column-policy/warehouse-tag floor every other results surface applies) before the snapshot is ever stored. `expected_value` stays unmasked by design (a check-authored threshold, not warehouse data). Four review passes (efficiency, conventions/docs-honesty, correctness, `mcp-honesty-reviewer`) then found the build-time fix alone does nothing for incidents that already existed — added `incident_service.redact_stale_evidence`, an idempotent one-time backfill (`backend/scripts/redact_stale_incident_evidence.py`), plus a required post-deploy step in `deploy/README.md`. Also caught and fixed a false claim in the `get_incident` MCP docstring's first draft (pointed at `get_run_results`'s `redaction` field as a way to check masking state — that field covers `sample_failures` only, never `observed_value`). Corrected six now-stale "not yet masked" references across `admin.py`, `llm_rca.py`, and three published compliance docs. Filed [#1792](https://github.com/TheurgicDuke771/DataQ/issues/1792) (N+1 `historical_check_context` query per failing result, bounded but real) and [#1793](https://github.com/TheurgicDuke771/DataQ/issues/1793) (a pre-existing, higher-severity fail-open asymmetry in `redact_observed_value` for scalar values when `tested_column` can't be resolved — shared by every results surface, not introduced here, newly exercised by this PR's new caller) rather than expand an already-large security PR further. 5 new regression tests, all mutation-verified | intelligence |

**Exit gate:** suggestions can only emit what the runner can run; G2's erasure/export
levers exist with the design recorded; both audit residuals closed. **W4 fully closed
2026-08-31** — all originally-planned items plus **all seven** review-driven follow-ups
(#1724/#1725/#1726/#1727/#1730/#1773/#1777/#1781/#1772 — nine in total) shipped.
Remaining residuals: #1787/#1790/#1793 have open PRs in the 2026-09-01/02 burn-down (W5 table); #1775 and #1792 stay `v1.2 Backlog`, not started (#1792 waits on #1801 — same lines).

### v1.2 W5 — Native engines (DMF) + notification channels (due 2026-09-25)

| Status | Task | Theme |
|---|---|---|
| ⬜ | [#895](https://github.com/TheurgicDuke771/DataQ/issues/895) `check.engine` seam + Snowflake DMF as the first platform-native engine (ADR 0036) — **gated on the W1 Snowflake decision keeping a live warehouse** | engines |
| ✅ | [#1514](https://github.com/TheurgicDuke771/DataQ/issues/1514) Reusable notification channels — define once, reference from many suites: new `notification_channels` + `suite_notification_channels` tables (additive only — the legacy per-suite `webhook_secret_ref`/`slack_webhook_secret_ref`/`email_recipients` fields keep working unchanged), `channel_service.py` (Admin-gated CRUD per the user-confirmed model; suite link/unlink on the suite's own view/edit ladder, idempotent; delete refused while any suite still references the channel — app-level guard backed by an `ON DELETE RESTRICT` FK, same pairing as the ADR 0015 comparison-source FK). `POST/PATCH/DELETE /notification-channels` + `PUT/DELETE /suites/{id}/notification-channels/{channel_id}`; the webhook URL is never echoed back, only `has_webhook`. Teams/Slack/email publishers now fan out to every linked channel of the matching type IN ADDITION to the legacy destination — deduped against it (a channel that happens to resolve to the same URL sends once), and each destination's failure is isolated so one bad webhook never blocks delivery to the others or aborts the whole `publish()` call (a deliberate behavior change from the prior single-destination "raises on failure" contract, two existing tests updated to match). Secret-sweep ownership registry extended (`channel-` prefix, `NotificationChannel.webhook_secret_ref` in `_OWNER_COLUMNS` — the same "two refs, one row" trap #633 already burned, now a third table). Frontend UI and the "promote an existing per-suite webhook to a channel" migration path both deliberately deferred and filed: [#1761](https://github.com/TheurgicDuke771/DataQ/issues/1761), [#1762](https://github.com/TheurgicDuke771/DataQ/issues/1762) | alerting |
| ✅ | [#1515](https://github.com/TheurgicDuke771/DataQ/issues/1515) Incident/alert "Owner" attribution prefers the asset owner when set, falls back to suite owner: `alerting/builder.py` resolves the run's owner from the asset (`asset.owner_user_id`) when set, else `suite.created_by` — display-only (Teams card / Slack message / email "Owner" row); delivery routing itself is unchanged. Review caught the first pass reimplementing the `run.asset_id`-then-`suite.asset_id` precedence rule a THIRD time in the same file (and a fourth overall, beside `run_service.asset_column_tags`'s own copy) — the exact "third spelling of the governance floor" shape `mcp/server.py` already warns against for #1419/#1479; consolidated into one canonical `run_service.resolve_asset`, with `asset_column_tags` and `builder.py`'s owner resolution both delegating to it, resolved once per report build. Regression tests mutation-verified: the "prefers asset owner" test genuinely fails against the unfixed one-liner, the fallback (no asset owner) case is unchanged | incidents |
| ✅ | [#1460](https://github.com/TheurgicDuke771/DataQ/issues/1460) G1 residual: tamper-evidence anchor for `audit_events` — hash chain + `TamperAnchor` seam (webhook, unanchored by default), [PR #1598](https://github.com/TheurgicDuke771/DataQ/pull/1598) merged | compliance |
| ✅ | [#1554](https://github.com/TheurgicDuke771/DataQ/issues/1554) Audit log has no UI — `GET /admin/audit-events` reachable from Admin, [PR #1599](https://github.com/TheurgicDuke771/DataQ/pull/1599) merged | compliance |
| ✅ | [#1555](https://github.com/TheurgicDuke771/DataQ/issues/1555) Deployment/data-residency posture has no UI — `GET /admin/deployment` reachable from Admin, [PR #1599](https://github.com/TheurgicDuke771/DataQ/pull/1599) merged | compliance |

| ✅ | [#1662](https://github.com/TheurgicDuke771/DataQ/issues/1662) Generic HMAC-signed outbound-webhook `ResultPublisher` (Theme 5 row, filed from the 2026-08-29 review pass): a 4th `notification_channels.type` (`webhook`) — a plain admin-supplied HTTPS `webhook_url` (plaintext, not a secret — the HMAC signature is what secures it) plus a DataQ-generated `hmac_secret_ref` signing key, shown exactly once in the create/rotate response and never again. `WebhookPublisher` fans out to every linked webhook channel: `hmac.new(key, body, sha256).hexdigest()` in the `X-DataQ-Signature` header, mirroring `api/v1/orchestration.py`'s inbound Airflow-callback HMAC scheme in reverse. Since a generic webhook has no fixed vendor allowlist (unlike Teams/Slack), the SSRF guard is a DESTINATION-RANGE denylist instead — `notification_service.is_safe_generic_webhook_url` requires https and resolves the hostname, rejecting any address that is private/loopback/link-local/reserved/multicast (blocks the cloud metadata endpoint, localhost, internal networks), checked both at channel-save time AND again at every send (DNS-rebinding defense-in-depth, mutation-verified). `publish_health`/`publish_poll_staleness` are honest no-ops — a generic webhook has no workspace-level concept, only per-suite-linked channels. Secret-sweep registry + audit field allowlist extended | alerting |
| ✅ | [#1663](https://github.com/TheurgicDuke771/DataQ/issues/1663) Per-destination payload templates + auth header on the webhook publisher (PagerDuty/Opsgenie/ServiceNow/Jira) — stacked on #1662: `notification_channels` gains `payload_template` (JSONB), `auth_header_name` + `auth_header_secret_ref`. A template's `{{field.path}}` placeholders resolve by KEY LOOKUP ONLY against the already-redacted generic payload — no expression language, so a template can rename/reshape/select fields already exposed and never reach anything new (the #1118/#1401 "no new exfiltration primitive" class, proven by a mutation-verified test that a Python-syntax placeholder like `{{__import__('os')...}}` just fails to match and passes through as a literal string). `auth_header_name` validated as a plain HTTP token (rules out CR/LF header injection) and rejected if it collides with a header DataQ sets itself; `auth_header_value` follows the same write-only tri-state secret pattern as every other channel credential. An unresolvable auth-header secret is fail-soft (drops just that header, mutation-verified) — unlike a missing HMAC key, which drops the whole destination, since the header is optional-extra beside the signature that actually secures the request. **`/code-review`'s 8-agent pass this time completed fully (no spend-limit failures) and converged on four real findings, all mutation-verified and fixed:** (1) `payload_template` can carry a de-facto credential (the PR's own PagerDuty worked example embeds a literal routing key) yet was echoed to any authenticated user including Viewers, recorded in cleartext in `audit_events`, and untracked by the secret sweep — three independent agents found this; fixed by gating `payload_template` to workspace-Admin readers only (`ChannelRead.include_payload_template`), with `has_payload_template` for everyone else, plus a prominent "never paste a real secret in here" warning on the field. (2) `auth_header_value` could be set with no `auth_header_name` (or vice versa via clearing), silently minting/orphaning a dead SecretStore entry while `has_auth_header: true` misreported it as configured — fixed with an effective-post-call-name validation check plus a cascade-clear (clearing the name now also deletes the orphaned secret). (3) Changing `webhook_url` on a channel with a stored auth header silently redirected that credential to the new destination with no re-supply requirement — the exact #1401 credential-redirect class already closed for `Connection`; fixed with a scoped `ChannelCredentialRedirectError` guard. (4) `_interpolate`'s `str()` on a non-string resolved value (e.g. `{{checks}}` embedded in a longer template string) produced Python repr — single-quoted, invalid JSON — instead of `json.dumps()`; fixed. Also applied from the review's simplification pass: a shared `_mint_channel_secret` helper (HMAC + auth-header secrets no longer each inline their own mint/rotate/purge sequence) and moving `json.dumps` of the base payload outside the per-destination loop (recomputed only when a template is actually set). Two findings considered and deliberately left as-is: a webhook-type channel can still be created with `auth_header_*` set but no `webhook_url` yet (an inert, incrementally-configured shell — consistent with the pre-existing precedent that a Teams/Slack channel can be created with no webhook either) and migration downgrade doesn't purge SecretStore values it stops referencing (consistent with how every other migration in this codebase treats downgrade — the sweep is the intended cleanup path, not the migration itself) | alerting |
| ✅ | **Backlog burn-down 2026-09-01/02 (bugs · security · P1-adjacent first)** — all eleven fix PRs merged in sequence 2026-09-02 (#1736's [PR #1800](https://github.com/TheurgicDuke771/DataQ/pull/1800) needed one re-run of a Playwright browser-install flake); every one carried a `/code-review` pass with findings fixed in-PR or filed. All had been blocked at the pnpm-audit gate until [PR #1802](https://github.com/TheurgicDuke771/DataQ/pull/1802) landed (two new HIGH browserslist GHSAs, dev-only transitive, pinned via the existing `pnpm.overrides`). Ten issues closed, six filed (#1803/#1804/#1806/#1809/#1811/#1814). Post-merge sanity on `main`: the overlapping modules (runs/mcp/incident evidence/redaction/markers) 449 tests green. **Not yet deployed.** | hygiene |
| ✅ | [#1793](https://github.com/TheurgicDuke771/DataQ/issues/1793) bug/security — `redact_observed_value`'s scalar branch failed OPEN when `tested_column` was unresolvable (custom SQL, deleted check): the whole ladder was short-circuited by `or`, on REST/MCP/alerts/incident evidence/dry-run — [PR #1801](https://github.com/TheurgicDuke771/DataQ/pull/1801): `expectation_type` threaded to all 7 call sites (resolved historically via `historical_check_context`, alert builder now aligned with REST/MCP); a column-less scalar shows only when its type makes it a statistic, cell types (max/min) and unknown types fail closed. **Review caught the fix's own blind spot:** the first cut ran the ladder under an empty column name, so G3 fail-closed suites would have masked every row count and custom-SQL count permanently (no tag/identifier can clear an unnamed column) and the in-place incident backfill would have erased those counts from their only copy — statistics now consult only the value-shape signal; regression tests mutation-checked. Filed [#1809](https://github.com/TheurgicDuke771/DataQ/issues/1809) (backfill uses current not historical check context) | compliance |
| ✅ | [#1729](https://github.com/TheurgicDuke771/DataQ/issues/1729) bug — `sanitize_json` sanitised dict values but not KEYS (bytes/Decimal/numpy key → JSONB insert crash, the #1273 class) — [PR #1798](https://github.com/TheurgicDuke771/DataQ/pull/1798): `_json_key` renders keys the way the value branch does then stringifies as JSON; `memoryview` handled like bytes (live-verified against a psycopg2 BYTEA read). Seven review angles: NaN/±Inf keys had been collapsed onto `"null"` (a regression — the stdlib kept them distinct), `np.str_` slipped the str fast-path, tuple/frozenset keys became repr garbage — all fixed; filed [#1803](https://github.com/TheurgicDuke771/DataQ/issues/1803) (`np.datetime64[ns].item()` → int) and [#1804](https://github.com/TheurgicDuke771/DataQ/issues/1804) (Decimal renders `100.0` vs `_to_native`'s `100`) | core |
| ✅ | [#1721](https://github.com/TheurgicDuke771/DataQ/issues/1721) bug — flat-file profiler rendered BINARY as Python repr while the SQL path hex-encoded — [PR #1807](https://github.com/TheurgicDuke771/DataQ/pull/1807) (stacked on #1798): one shared `bytes_to_hex` helper in `core/jsonsafe.py`, parity test | profiler |
| ✅ | [#1736](https://github.com/TheurgicDuke771/DataQ/issues/1736) bug — every 401 said "Azure AD token" on Cognito/generic-OIDC/OTP deployments — [PR #1800](https://github.com/TheurgicDuke771/DataQ/pull/1800): message derived from the configured auth mode (13 tests over six ladder shapes; review clean) | auth |
| ✅ | [#1732](https://github.com/TheurgicDuke771/DataQ/issues/1732) security — `update_connection` accepted `secret=""` as "re-supplied" at the service layer (blank credential + #1401 redirect-guard bypass rested on one API-model `min_length`) — [PR #1799](https://github.com/TheurgicDuke771/DataQ/pull/1799): `EmptyCredentialError` floor in create/update/reauth before any store write; review fixed a doc line for an unreachable REST code and a misleading hint | security |
| ✅ | [#1787](https://github.com/TheurgicDuke771/DataQ/issues/1787) bug — `validate_monitor_check` echoed the whole unbounded caller config in `CheckConfigInvalidError.detail`; `_ident` echoed `name!r` uncapped — [PR #1805](https://github.com/TheurgicDuke771/DataQ/pull/1805): `_reject_oversized_config` + bounded `_echo`; review found the same uncapped echo on `anomaly_params.target_metric` (reachable from dry-run with no cap in front) and on `snowflake_dmf._quoted` (now delegates to `_ident`) | checks |
| ✅ | [#1717](https://github.com/TheurgicDuke771/DataQ/issues/1717) perf — `llm_invocations` had no `status` index; the #1644 reaper seq-scanned a retained-forever table — [PR #1808](https://github.com/TheurgicDuke771/DataQ/pull/1808): partial index on `status IN (pending, running)` + additive migration `4a5f1e4d5daa` (up/down on real Postgres) + an `EXPLAIN` test proving the planner uses it for the reaper's two real predicates. Review: the "only non-PK read" claim was false (`llm_rca`'s succeeded-narrative lookup, tracked by #1743), the running-branch test had used `created_at` not `started_at`, open-status literal now one constant; filed [#1806](https://github.com/TheurgicDuke771/DataQ/issues/1806) (ER-diagram `ondelete` drift) | perf |
| ✅ | [#1790](https://github.com/TheurgicDuke771/DataQ/issues/1790) reliability — no launch location set Celery `--concurrency`; live banners read **4 on Azure, 2 on AWS** for identical 1-vCPU tasks (prefork default = host cores) while `perf-baseline.md` claimed 4 — [PR #1810](https://github.com/TheurgicDuke771/DataQ/pull/1810): `worker_concurrency` pinned in `celery_app.conf` from `WORKER_CONCURRENCY` (default 4) so it ships with the image roll and needs no IaC step (review moved it out of the four launch commands); four-way launch-command drift test; filed [#1811](https://github.com/TheurgicDuke771/DataQ/issues/1811) (4 workers × 1.2–1.7 GiB single-run peaks under a 2 GiB limit — OOM takes beat down too) | worker |
| ✅ | [#1795](https://github.com/TheurgicDuke771/DataQ/issues/1795) security — data-subject export/erase never reached `incidents.evidence`, a third persisted copy of a warehouse cell outside the retention clock — [PR #1812](https://github.com/TheurgicDuke771/DataQ/pull/1812): both endpoints scan and scrub the snapshot in place with the same match rules; responses carry totals + a per-store breakdown so "erased" never silently means results-only; runbook/DPIA/security page describe coverage instead of disclosing the gap; matching uses the check's current column (no `Result` row on the incident — #1809) | compliance |
| ✅ | [#1728](https://github.com/TheurgicDuke771/DataQ/issues/1728) bug — the dbt colon-collision fail-closed guard (#1714) sat in `incident_evidence` only; MCP `get_pipeline_status` used `.first()` and the Results page rebuilt the marker client-side — [PR #1813](https://github.com/TheurgicDuke771/DataQ/pull/1813): shared `orchestration/markers.py` (reconstruct-and-compare, `[]` on ambiguity) behind all three readers; REST `/pipeline_runs` carries server-computed visibility-scoped `triggered_run_ids`, MCP adds `dq_run_ambiguous`. Review: pick the newest VISIBLE run (a two-suite trigger read as restricted), one ambiguity query per call, provider pre-filter; filed [#1814](https://github.com/TheurgicDuke771/DataQ/issues/1814) (expression index for the concat scan) | orchestration |
| ✅ | **Backlog burn-down, batch 2 (2026-09-02)** — the follow-ups the first batch filed plus the two picks it left: all seven merged. The four worktree agents were killed by the spend limit mid-flight again; everything was finished inline from their worktrees. | hygiene |
| ✅ | [#1806](https://github.com/TheurgicDuke771/DataQ/issues/1806) ER diagram said `llm_invocations.suite_id` CASCADE, model/migration SET NULL — [PR #1816](https://github.com/TheurgicDuke771/DataQ/pull/1816): two-line doc fix + the ER-sync test now compares every quoted delete policy (entity FK lines incl. `PK,FK` composites AND relationship labels) against the model's `ondelete`, NO ACTION distinct from RESTRICT; mutation-checked both halves | docs |
| ✅ | [#1792](https://github.com/TheurgicDuke771/DataQ/issues/1792) `incident_evidence` re-ran `historical_check_context` + the `Suite` load per failing result — [PR #1817](https://github.com/TheurgicDuke771/DataQ/pull/1817): resolved once per run as a frozen `RedactionContext`, threaded down; query-count test (4 → 1 `check_versions` queries); review added skip-when-nothing-fails and degrade-per-card-if-resolution-raises, both tested | incidents |
| ✅ | [#1803](https://github.com/TheurgicDuke771/DataQ/issues/1803) `np.datetime64[ns]` rendered as a bare epoch int — [PR #1818](https://github.com/TheurgicDuke771/DataQ/pull/1818): ISO string via the datetime64 branch; review found the same bug's second root in `profile_service._to_native` (its own bare `.item()`), which now delegates to `sanitize_json`; filed [#1819](https://github.com/TheurgicDuke771/DataQ/issues/1819) (np.timedelta64 / timedelta rendered four ways) | core |
| ✅ | [#1809](https://github.com/TheurgicDuke771/DataQ/issues/1809) the incident-evidence backfill and the DSR incident scan classified by the check's CURRENT column/type — [PR #1820](https://github.com/TheurgicDuke771/DataQ/pull/1820): `historical_check_context` gained an as-of variant resolved at `last_seen_at` (the evidence write time); three regression tests each proven to fail on the unfixed code; runbook §5 updated; review clean | compliance |
| ✅ | [#1814](https://github.com/TheurgicDuke771/DataQ/issues/1814) the #1728 marker correlation scanned `pipeline_runs` via an unindexed concat — [PR #1821](https://github.com/TheurgicDuke771/DataQ/pull/1821): `ix_pipeline_runs_marker` expression index (CONCURRENTLY, migration `6bcf67868753`), ORM expression switched to `\|\|` so the planner matches (EXPLAIN-asserted), and the migration-parity test taught to compare expression indexes textually since `compare_metadata` cannot (mutation-checked against a drifted migration) | perf |
| ✅ | [#1804](https://github.com/TheurgicDuke771/DataQ/issues/1804) `_to_native` rendered Decimal as the string `"100"` vs the SQL path's `100.0` — [PR #1822](https://github.com/TheurgicDuke771/DataQ/pull/1822): `_to_native` collapsed onto `sanitize_json` + a `str()` fallback (one root for bytes/numpy/Decimal; pd.NA now nulls instead of `"<NA>"`) | profiler |
| ✅ | [#1731](https://github.com/TheurgicDuke771/DataQ/issues/1731) security — OTP request floor is a minimum; a slow relay made members measurably slower than strangers — [PR #1823](https://github.com/TheurgicDuke771/DataQ/pull/1823): the mail send moved to the worker (`send_otp_code`, routed onto the already-consumed `llm` queue, redacted `kwargsrepr`, expires at the code TTL, single-attempt publish); ADR 0032 decision 7 amended; the OTP e2e lane starts a worker. Review found the lane's Redis index was exported after the api launched (fixed), the queue-starvation and publish-retry timing residuals (fixed), and filed [#1824](https://github.com/TheurgicDuke771/DataQ/issues/1824) (broker outage burns the per-email quota) | auth |
| ⬜ | [#1676](https://github.com/TheurgicDuke771/DataQ/issues/1676) Zero-sample "privacy mode" — deployment-level switch, samples never persisted | compliance |
**Exit gate:** a DMF check runs and persists through the normal result path (or the ADR's
trigger conditions are re-recorded if the W1 decision removed Snowflake); a channel
defined once delivers for two suites; #1460 shipped a decision, not a shrug.

### v1.2 W6 — Perf/scale hardening (due 2026-10-02)

| Status | Task | Theme |
|---|---|---|
| ✅ | [#1624](https://github.com/TheurgicDuke771/DataQ/issues/1624) Convert the 14 eligible UC frame-batch expectation types to Databricks-SQL pushdown — shipped early, out of week order, at user request — [PR #1629](https://github.com/TheurgicDuke771/DataQ/pull/1629): all 14 joined `SQL_PUSHDOWN_EXPECTATION_TYPES`, live-Databricks-SQL-verified per type (`unexpected_percent` matched independently-computed ground truth on every check); `expect_compound_columns_to_be_unique` got a Databricks-dialect reflection-casing fold mirroring #1616's Snowflake fix, shared into `datasources/sql.py`; `/code-review` caught and fixed a real gap — the index-column clash workaround only checked the `column` kwarg, missing the new `column_A`/`column_B`/`column_list`-keyed types | perf |
| ⬜ | [#1393](https://github.com/TheurgicDuke771/DataQ/issues/1393) Systematic scale baseline + CI perf-regression budget | perf |
| ⬜ | [#1328](https://github.com/TheurgicDuke771/DataQ/issues/1328) Iceberg runner size probe / scan cap — measured against #1393, not inherited | perf |
| ⬜ | [#1329](https://github.com/TheurgicDuke771/DataQ/issues/1329) Efficiency batch: sampled read paths | perf |
| ⬜ | [#1330](https://github.com/TheurgicDuke771/DataQ/issues/1330) Reuse/simplification batch: CSV stream construction first | refactor |
| ⬜ | [#1331](https://github.com/TheurgicDuke771/DataQ/issues/1331) Comparison-source sampling (coherent key-set, not two draws) | perf |
| ⬜ | [#1245](https://github.com/TheurgicDuke771/DataQ/issues/1245) `(created_at, id)` index for deep offset paging | perf |
| ⬜ | [#1243](https://github.com/TheurgicDuke771/DataQ/issues/1243) Batch-target preview: bound the listing + move regex off the API process | perf / safety |
| ⬜ | [#1236](https://github.com/TheurgicDuke771/DataQ/issues/1236) Lineage: durable backstop for a suspended snapshot prune | reliability |

| ⬜ | [#1670](https://github.com/TheurgicDuke771/DataQ/issues/1670) a11y: axe-core CI ratchet (Playwright + vitest-axe) — the regression-stopping floor | quality |
**Exit gate:** a perf regression is *detectable* (the budget fails CI); the sampled-read
batches land measured against the new baseline.

### v1.2 W7 — Feature burn-down (due 2026-10-09)

| Status | Task | Theme |
|---|---|---|
| ⬜ | [#505](https://github.com/TheurgicDuke771/DataQ/issues/505) GCP deploy IaC (AWS shipped 2026-08-15 — GCP is the remainder) | deploy |
| ⬜ | [#466](https://github.com/TheurgicDuke771/DataQ/issues/466) Interactive datasource browsing (ADLS/S3 container browser + UC catalog picker) | ux |
| ⬜ | [#244](https://github.com/TheurgicDuke771/DataQ/issues/244) Suite-on-suite triggering | orchestration |
| ⬜ | [#888](https://github.com/TheurgicDuke771/DataQ/issues/888) Tagging for suites/assets/connections/checks | platform |
| ⬜ | [#1516](https://github.com/TheurgicDuke771/DataQ/issues/1516) Profile / Workspace-Settings IA pickup | ux |
| ⬜ | [#685](https://github.com/TheurgicDuke771/DataQ/issues/685) Purge/redact path for connection version history | security |
| ⬜ | [#682](https://github.com/TheurgicDuke771/DataQ/issues/682) Webhook-auth metadata onto the `OrchestrationProvider` seam | refactor |
| ⬜ | [#1314](https://github.com/TheurgicDuke771/DataQ/issues/1314) Alerting: already-logged-traceback downgrade leaks through log filtering | reliability |
| ⬜ | [#1274](https://github.com/TheurgicDuke771/DataQ/issues/1274) Iceberg over S3-compatible storage: pyarrow ACCESS_DENIED + Test Connection blind | bug |
| ⬜ | [#1385](https://github.com/TheurgicDuke771/DataQ/issues/1385) AWS: ElastiCache at-rest encryption + restrict subnet egress | security |

| ⬜ | [#1667](https://github.com/TheurgicDuke771/DataQ/issues/1667) Global search / command palette (⌘K) | ux |
| ⬜ | [#1669](https://github.com/TheurgicDuke771/DataQ/issues/1669) Bulk operations on checks (multi-select enable/disable/severity/snooze) | ux |
| ⬜ | [#1671](https://github.com/TheurgicDuke771/DataQ/issues/1671) a11y: non-color severity cues + colorblind-safe palette | ux |
| ⬜ | [#1677](https://github.com/TheurgicDuke771/DataQ/issues/1677) JSON flat-file support (csv/parquet + json) | datasource |
| ⬜ | [#1678](https://github.com/TheurgicDuke771/DataQ/issues/1678) Generic PostgreSQL adapter — the G-f cheap first win, dogfoodable | datasource |
| ⬜ | [#1686](https://github.com/TheurgicDuke771/DataQ/issues/1686) Run comparison — diff two runs of a suite | results |
| ⬜ | [#1688](https://github.com/TheurgicDuke771/DataQ/issues/1688) Checks-as-code: dataq.yaml/JSON authoring contract + validate/apply/drift (phase 1 can ship alone) | gitops |
| ⬜ | [#1664](https://github.com/TheurgicDuke771/DataQ/issues/1664) "Data Quality Control Plane" positioning copy (marketing + docs site) | docs |
| ⬜ | [#1694](https://github.com/TheurgicDuke771/DataQ/issues/1694) Admin control centre phase 1a: routed sub-pages IA restructure (epic #1702; pulled in 2026-08-29) | admin |
| ⬜ | [#1695](https://github.com/TheurgicDuke771/DataQ/issues/1695) Admin control centre phase 1b: parity pass — DSR / SMTP test / webhooks / chain verify UI | admin |
**Exit gate:** each closed or rolled by name with a rationale comment.

### v1.2 W8 — Spikes, decisions & cycle close (due 2026-10-16)

| Status | Task | Theme |
|---|---|---|
| ⬜ | [#717](https://github.com/TheurgicDuke771/DataQ/issues/717) Iceberg v3 revisit (deletion vectors, row lineage) behind a capability gate | spike |
| ⬜ | [#732](https://github.com/TheurgicDuke771/DataQ/issues/732) Marketplace-listing readiness checklist | docs |
| ⬜ | [#1239](https://github.com/TheurgicDuke771/DataQ/issues/1239) OTP uniform-response anti-enumeration tradeoff — decide | decision |
| ⬜ | [#980](https://github.com/TheurgicDuke771/DataQ/issues/980) Redis 8 gate: Vector Sets module stays unloaded | watch |
| ⬜ | [#970](https://github.com/TheurgicDuke771/DataQ/issues/970) OTel stack → 1.44 when the Azure exporter supports it | watch |
| ⬜ | [#1327](https://github.com/TheurgicDuke771/DataQ/issues/1327) Profiler: `sqlalchemy.values()` for the rank driver — consider | spike |
| ⬜ | [#1334](https://github.com/TheurgicDuke771/DataQ/issues/1334) Check ordinal (same-transaction inserts tie on `created_at`) | quality |
| ⬜ | [#1660](https://github.com/TheurgicDuke771/DataQ/issues/1660) **Spike/ADR:** automated coverage loop — fleet-wide monitor bootstrap design (the G-a remainder) | spike |
| ⬜ | [#1680](https://github.com/TheurgicDuke771/DataQ/issues/1680) **Spike:** OneLake flat-file — ADLS adapter + endpoint override + Entra auth | spike |
| ⬜ | [#1668](https://github.com/TheurgicDuke771/DataQ/issues/1668) First-run onboarding + empty-state pass (pairs with #732 marketplace checklist) | ux |
| ⬜ | [#1518](https://github.com/TheurgicDuke771/DataQ/issues/1518) **Cycle close:** every remaining item closed or rolled by name; zero open PRs; retro-v1.2; freeze this file; tag + release `v1.2.0` | close |

**Exit gate:** the close checklist in #1518 — retro written, ledger frozen, `v1.2.0`
tagged + released, epic closed.

---

## How to update this file

When merging a PR:

1. Find the task(s) it implements in the relevant week above.
2. Flip `⬜` → `✅` (or `⬜` → `🟡` if partial).
3. Append the PR link: `— [PR #N](https://github.com/.../pull/N)`.
4. Update the **Snapshot** table (open PRs/issues).
5. If the PR closes a carried-over item, strike it through with the closing ref.
6. If the PR added out-of-scope work, add a row with a note (same honesty rule as v1).

PR-template checkbox enforces this. If the change is purely tooling / docs that doesn't map
to a tracked task, tick the "N/A" checkbox.
