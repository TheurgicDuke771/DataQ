# DataQ backend — dev/code-quality tooling (NOT runtime; never shipped).
#
# The single home for these versions: environment.yml pulls them via
# requirements-dev.txt, and the CI lint / type-check / SAST jobs install this
# file directly (it's lightweight, so installing all of it per job is cheap).
#
# The ruff/black/mypy pre-commit hooks are `language: system`: they run the
# conda env's copies of these pins rather than fetching their own by `rev:`, and
# scripts/run-pinned-tool.py fails a hook whose env has drifted (#2080). So a
# bump here (e.g. from Dependabot) is the only edit needed.

black==26.5.1
ruff==0.16.5
mypy==2.3.1
bandit[toml]==1.9.4
pre-commit==4.6.2
